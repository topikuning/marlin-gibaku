# Audit branch `dev` — 2026-09-15

Audit menyeluruh atas permintaan user: *"audit semua kode di branch dev, pastikan
tidak ada bug atau yang tidak sesuai alur. terutama di alur pelaporan termasuk alur
datanya, RAB dan Adendum"*.

**Cara kerjanya.** 11 pemeriksa paralel membaca subsistem + lensa masing-masing
(laporan harian ×3, RAB, adendum ×2, baseline/kurva-S, otorisasi, konsumen angka,
keuangan, kualitas tes). 90 temuan mentah yang lolos dedup lalu diserahkan ke
pembantah skeptis — satu per temuan, menjalankan tiga lensa berurutan: penelusuran
kode dari titik masuk pengguna, **reproduksi merah-dulu** (berkas uji sementara,
dijalankan, lalu dihapus), dan pemeriksaan spesifikasi (PROJECT.md + arsip
`docs/decisions/`). Temuan hanya lolos kalau pembantah GAGAL membantahnya. Instruksi
tegasnya: *ragu = terbantah*.

**Hasil: 74 terkonfirmasi, 9 terbantah, 7 belum sempat diverifikasi.**

| Tingkat | Jumlah |
|---|---|
| 🔴 Kritis | 3 |
| 🟠 Tinggi | 23 |
| 🟡 Sedang | 37 |
| 🟢 Rendah | 11 |

Semua temuan di bawah **belum diperbaiki** — dokumen ini daftar kerja, bukan
laporan perbaikan. Nomor dalam kurung (`laporan-angka-13`) adalah id internal
audit, dipakai supaya rujukan antar-temuan tidak kabur.

---

## Yang DIPERIKSA dan TIDAK bermasalah

Supaya daftar di bawah tidak terbaca sebagai "semuanya rusak":

- **Mesin transisi status laporan harian utuh.** Semua perpindahan status lewat
  `transition()` (`canTransitionReport` + histori + `audit()` dalam satu
  transaksi). Tidak ada jalur pintas.
- **Verifikasi eksternal Wakil PPK benar-benar append-only** — tidak menyentuh
  status maupun angka resmi, auditnya di dalam transaksi.
- **`requireCapability` + `requireLocationAccess` + `audit()` ada di seluruh
  server action laporan harian dan verifikasi.**
- **`COUNTED_REPORT_STATUSES` konsisten** di `progress.ts`, queries, verifikasi,
  dan antrean (satu penyimpangan dilaporkan sebagai A-8).
- **Guard volume saat kirim memakai advisory lock per lokasi**; `upsertItem`
  idempoten per `(reportId, lineageKey)`.
- **Snapshot final dibaca beku** untuk angka volume (dikonfirmasi
  `tests/integration/kkp-harian-final.test.ts`).

---

## Ringkasan per tema

| Tema | 🔴 | 🟠 | 🟡 | 🟢 |
|---|--:|--:|--:|--:|
| A. Laporan harian – alur & status | 0 | 4 | 4 | 0 |
| B. Laporan harian – angka & aliran data | 0 | 4 | 4 | 0 |
| C. Laporan harian – masukan (rekap, WA, foto) | 1 | 2 | 5 | 1 |
| D. RAB – parser, lineage, ekspor | 0 | 3 | 3 | 1 |
| E. Adendum – inti | 0 | 2 | 6 | 0 |
| F. Adendum – templat & CCO KKP | 0 | 3 | 4 | 0 |
| G. Baseline, kurva-S, rencana | 2 | 3 | 2 | 1 |
| H. Otorisasi & audit | 0 | 1 | 2 | 4 |
| I. Konsumen angka (PDF, Excel, WA, AI, komponen) | 0 | 0 | 3 | 3 |
| J. Keuangan & kesiapan termin | 0 | 1 | 4 | 1 |

---

## A. Laporan harian – alur & status

### A-1. 🟠 Laporan hari nihil yang FINAL kehilangan pernyataan "TIDAK ADA KEGIATAN" di semua keluaran resmi (layar cetak, PDF, Excel, WA, Drive)

`src/lib/daily-report/queries.ts:850` · tinggi · bug · `laporan-transisi-1`

**Skenario.** Pelaksana menyatakan 2026-08-11 nihil (hujan) → dikirim → disetujui → FINAL. Buka /cetak/harian/<slug>/2026-08-11 (atau PDF harian-kkp.ts, Excel daily-xlsx.ts, berkas mingguan mingguan-kkp.ts, unggahan Drive otomatis pasca-finalisasi). getKkpDailyData masuk cabang `report.status === "final" && report.finalSnapshot` (baris 850) yang merakit data dari `snapshotToKkp(snap)` + spread lain, TANPA `noActivity/noActivityReason/noActivityNote` – ketiganya hanya ada di cabang live (baris 959-962), dan tipe `FinalSnapshot` (service.ts 696-759) memang tidak pernah membekukannya. Di kkp-daily-report.tsx:576 `teksNihilCetak(!!d.noActivity, …)` → false → `barisRealisasiKkp([])` → kolom realisasi KOSONG. Persis cacat yang DECISIONS 396 §"Blanko cetak" larang: "Blanko yang dibiarkan kosong terbaca seperti ada yang lupa mengisi" – dan yang rusak justru versi FINAL, satu-satunya yang dikirim ke KKP (pratinjau benar).

**Bukti.** queries.ts 850-897 (cabang final): if (report?.status === "final" && report.finalSnapshot) { const snap = report.finalSnapshot as unknown as FinalSnapshot; const base = snapshotToKkp(snap); ... return { ...base, items: ..., draftItemCount: ..., ...signatories, ...owner, rencana, ...lampiran, weekNo, ...periode(weekNo) }; } queries.ts 959-962 (cabang live saja): noActivity: report?.noActivity ?? false, noActivityReason: ..., noActivityNote: ... kkp-daily-report.tsx:576 const teksNihil = teksNihilCetak(!!d.noActivity, d.noActivityReason, d.noActivityNote);

**Tes terkait.** tests/integration/kkp-harian-final.test.ts membandingkan pratinjau vs final hanya untuk laporan ber-item; tests/integration/hari-nihil.test.ts tidak pernah memanggil getKkpDailyData/finalizeReport. Tidak ada tes yang menangkapnya.

**Usul perbaikan.** Ambil `noActivity*` dari baris DailyReport hidup di cabang final juga (kolomnya tetap tersimpan; ini pernyataan, bukan angka beku – pola sama dengan kategori/foto yang sudah diambil dari data hidup), ATAU bekukan ketiganya ke FinalSnapshot dan baca dari snap. Tambah uji di kkp-harian-final.test.ts: laporan nihil sebelum/sesudah final harus sama-sama memuat teks nihil.

**Reproduksi.** MERAH (direproduksi). Berkas uji sementara tests/integration/_audit-laporan-transisi-1.test.ts (DATABASE_URL marlin_test, APP_ENV=test) – sudah DIHAPUS. Alur: getOrCreateDraft → setHariNihil({nihil:true, alasan:"hujan", catatan:"deras sejak subuh"}) → submitReport (mandor) → approveReport (PM) → finalizeReport (super_admin) → getKkpDailyData(slug, "2026-08-11") → barisRencanaRealisasi(d). Keluaran: pratinjau (dikirim): noActivity=true, realisasi baris 1 = "TIDAK ADA KEGIATAN – Hujan – deras sejak subuh" → LULUS sesudah final: kolom DB daily_reports.no_activity = true, tetapi `"noActivity" in f

### A-2. 🟠 setHariNihilAction mengubah pernyataan nihil pada laporan yang sudah `dikirim`/`disetujui` (counted) hanya dengan daily_report.create, tanpa histori status

`src/lib/daily-report/service.ts:1050` · tinggi · authz · `laporan-transisi-2`

**Skenario.** Laporan 2026-08-11 nihil "hujan" sudah DISETUJUI Site Manager (counted, jadi dasar hitungan hari hujan/klaim perpanjangan waktu). Pelaksana (field_supervisor, hanya `daily_report.create`) mengirim setHariNihilAction {locationId, dateKey:2026-08-11, nihil:1, alasan:libur} – atau nihil:0. actions.ts 1415-1435: requireCapability(create) → getOrCreateDraft mengembalikan laporan yang ADA apa pun statusnya (service.ts 70-73) → setHariNihil hanya menolak `final` (1050). Hasil: alasan pada laporan yang sudah diverifikasi berubah (hujan→libur), atau laporan disetujui menjadi kosong & non-nihil (persis "laporan hampa" yang submitReport tolak, kini lolos SESUDAH persetujuan). Tidak ada baris DailyReportStatusHistory, penyetuju tidak tahu. Bandingkan pagar sejenis: upsertItem (113) EDITABLE_STATUSES, saveEnrichmentAction (954-959) CREATOR_ENRICHABLE_STATUSES. UI menyembunyikan panel (report-editor.tsx hanya saat editable) – tapi CLAUDE.md #3: frontend hanya menyembunyikan menu.

**Bukti.** service.ts 1049-1052: const report = await getReportOrThrow(reportId); if (report.status === "final") { throw new DailyReportError("Laporan sudah final – tidak bisa diubah."); } actions.ts 1415, 1428, 1431: const user = await requireCapability("daily_report.create"); const report = await getOrCreateDraft(d.locationId, d.dateKey, user.id); await setHariNihil(ctx.id, { nihil: d.nihil, ... }, user.id);

**Tes terkait.** tests/integration/hari-nihil.test.ts tidak menguji status selain draft (fixture `nihilkan` menulis status lewat db.update); tidak ada tes yang menuntut penolakan pada dikirim/disetujui.

**Usul perbaikan.** Di setHariNihil: tolak bila status tidak termasuk EDITABLE_STATUSES (draft/perlu_koreksi) – sama dengan item; kalau reviewer perlu mengubah nihil saat `dikirim`, gerbang eksplisit lewat `daily_report.review` seperti enrichment. Tambah uji MERAH: setHariNihil pada laporan `disetujui` → rejects.

**Reproduksi.** Berkas uji sementara tests/integration/_audit-laporan-transisi-2.test.ts (integrasi, DB marlin_test) – SUDAH DIHAPUS, `git status --short` kosong. Skenario: field_supervisor membuat draft 2026-08-11, setHariNihil {hujan}, submitReport, lalu approveReport oleh site_manager lain (pemisahan tugas lolos, status `disetujui`). Hasil 3 uji: 1 hijau, 2 MERAH persis seperti diklaim temuan. (a) `can("field_supervisor","daily_report.create")`=true, `can(...,"daily_report.review")`=false → lolos (peta peran sesuai klaim). (b) `setHariNihil(id,{nihil:true,alasan:"libur"},pelaksanaId)` pada laporan `disetuj

### A-3. 🟠 Pagar "nihil vs isi" hanya satu arah: laporan yang SUDAH dinyatakan nihil masih bisa menerima item (impor rekap/aksi) dan material/alat (form pelengkap tetap tampil), lalu terkirim dengan dua pernyataan yang saling menyangkal

`src/lib/daily-report/service.ts:111` · tinggi · data-integritas · `laporan-transisi-3`

**Skenario.** Jalur UI normal: pelaksana menekan "Nyatakan tidak ada kegiatan" (noActivity=true). Form pelengkap tetap dirender karena `enrichable` di page.tsx 65-68 tidak memeriksa nihil → ia mengisi material "Semen 50 zak" → setEnrichment (264) tidak memeriksa noActivity → tersimpan. submitReport (564-586) hanya memeriksa nihil bila item+material+alat SEMUA nol; di sini materialCount=1 → langsung dikirim. Jalur kedua: admin impor rekap Excel (recap-import.ts 98-121) untuk tanggal yang mandor tandai nihil → getOrCreateDraft mengembalikan draft nihil → upsertItem (111) tidak memeriksa noActivity → item masuk → submitReport lolos (itemCount>0). Laporan berstatus dikirim/disetujui dengan noActivity=true DAN item bervolume: cumulativeVolumeByLineage/getLocationProgress MENGHITUNG item itu, sementara blanko KKP (kkp-daily-report.tsx 576-579) mencetak "TIDAK ADA KEGIATAN" dan menyembunyikan seluruh baris item; penjadwal nihil menghitungnya sebagai hari berhenti. Kebalikan arahnya (setHariNihil saat sudah ada item) ditolak oleh alasanTidakBisaNihil – pagarnya tidak simetris.

**Bukti.** service.ts 564-571 (submitReport): if (itemCount === 0 && materialCount === 0 && equipmentCount === 0) { if (!laporan?.noActivity) { throw ... } ... } // tidak ada cabang: noActivity && (itemCount>0 || materialCount>0 || equipmentCount>0) upsertItem 111-119 & setEnrichment 264-267: hanya memeriksa EDITABLE/ENRICHABLE_STATUSES, tidak membaca report.noActivity page.tsx 65-68: const enrichable = !!report && ((canReview && ...) || (canCreate && (status === "draft" || status === "perlu_koreksi")));

**Tes terkait.** tests/integration/hari-nihil.test.ts hanya menguji arah setHariNihil→ditolak bila ada item (baris 161-168); arah sebaliknya dan submitReport pada laporan nihil+isi tidak diuji.

**Usul perbaikan.** Jadikan invariannya dua arah: upsertItem/setEnrichment (material/alat bernama) menolak bila report.noActivity, dan submitReport menolak `noActivity && (item|material|alat > 0)` dengan pesan yang menyebut sebabnya; sembunyikan EnrichmentForm saat nihil aktif. Uji MERAH: nihil → upsertItem/setEnrichment/submitReport harus menolak.

**Reproduksi.** Berkas sementara tests/integration/_audit-laporan-transisi-3.test.ts (sudah DIHAPUS, `git status --porcelain` kosong), dijalankan sendiri dengan DATABASE_URL=...marlin_test APP_ENV=test. Fixture meniru tests/integration/hari-nihil.test.ts (org/paket/lokasi/user/RAB aktif satu item volume 100, harga 10). Hasil: 4/4 MERAH (semua menegaskan perilaku yang seharusnya = menolak): 1) nihil(hujan) → upsertItem(volume 5): promise RESOLVED, item tersimpan (volumeDone "5", valueDone 50n). 2) nihil → setEnrichment material "Semen 50 zak": RESOLVED, idMaterial berisi 1 id. 3) nihil + item → submitReport: l

### A-4. 🟠 Buka kunci final → koreksi → final ulang TIDAK membangun ulang snapshot laporan final bertanggal sesudahnya — kebalikan dari aturan yang dipakai pindah tanggal

`src/lib/daily-report/service.ts:939` · tinggi · data-integritas · `laporan-transisi-5`

**Skenario.** Laporan 8 Jun final volume 10; laporan 10 Jun final, snapshot-nya membekukan volumeBefore=10, volumeCumulative=30, progress.realizedValue as-of 10 Jun. Super admin membuka kunci 8 Jun (unfinalizeReport 939) → SM mengembalikan (returnReport) → pelaksana mengoreksi 10→5 → kirim → setujui → finalizeReport (893). Snapshot 8 Jun dibangun ulang, tetapi snapshot 10 Jun tetap volumeBefore=10/kumulatif=30 padahal kebenaran barunya 5/25; cetak/PDF/Drive 10 Jun menampilkan kumulatif yang MARLIN sendiri tahu sudah salah. pindahTanggalLaporan (pindah-tanggal-service.ts 172-202) justru menghitung ulang rentang terdampak dengan alasan tertulis "Dibiarkan basi berarti MARLIN sengaja mencetak angka kumulatif yang ia tahu salah" (DECISIONS 415), sedangkan DECISIONS 149 diam soal laporan lain dan tombol "Bangun ulang snapshot" (DECISIONS 148, system.manage saja) tidak pernah ditawarkan/dipicu pada jalur ini. Tidak ada angka kanonik yang berubah – yang salah adalah dokumen beku milik hari lain.

**Bukti.** service.ts 948-956 (unfinalizeReport): transition(reportId, "disetujui", userId, { finalizedById: null, finalizedAt: null, finalSnapshot: Prisma.DbNull }, ...) // hanya laporan ini service.ts 903-917 (finalizeReport): const snapshot = await buildFinalSnapshot(reportId); ... transition(reportId, "final", ...) // hanya laporan ini pindah-tanggal-service.ts 179-201: if (basi) { const terdampak = await db.dailyReport.findMany({ where: { locationId, status: "final", id: { not: report.id }, reportDate: { gte: ..., lt: ... } } }); for (...) { buildFinalSnapshot(r.id) ... } }

**Tes terkait.** tests/integration/pindah-tanggal-laporan.test.ts membuktikan rebuild untuk pindah tanggal; tidak ada tes setara untuk unfinalize→koreksi→final (daily-report-flow.test.ts hanya satu laporan).

**Usul perbaikan.** Setelah finalizeReport ulang (atau pada saat laporan yang pernah final berubah volumenya), bangun ulang snapshot laporan final lokasi itu dengan reportDate > reportDate laporan ini (fungsi yang sama dengan pindah tanggal), dan sebutkan jumlahnya di pesan sukses. Minimal: tulis keputusan eksplisit di docs/decisions bila memang sengaja dibiarkan.

**Reproduksi.** Berkas sementara tests/integration/_audit-laporan-transisi-5.test.ts (sudah DIHAPUS), dijalankan sendiri dengan DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test. Skenario: kontrak mulai 1 Jun, RAB 1 item "I#1" volume 100; laporan A 8 Jun final volume 10 (mandor→PM setujui→admin finalkan); laporan B 10 Jun final volume 20. Lalu: unfinalizeReport(A, admin) → returnReport(A, pm) → upsertItem(A, 5) → submitReport → approveReport(pm) → finalizeReport(admin). Hasil: 1 lulus, 1 GAGAL (merah). SNAPSHOT A (dibangun ulang): { status:'final', tanggal:'2026-06-08', sebelum:0

### A-5. 🟡 Penagih nihil beruntun memakai `status: { not: "draft" }` alih-alih COUNTED_REPORT_STATUSES: laporan nihil yang DIKEMBALIKAN (perlu_koreksi) tetap dihitung sebagai pernyataan berhenti kerja

`src/lib/daily-report/penjadwal-nihil.ts:78` · sedang · formula-duplikat · `laporan-transisi-8`

**Skenario.** 18, 19 Agu nihil dikirim; 20 Agu nihil dikirim lalu SM mengembalikannya (perlu_koreksi) dengan alasan "20 Agu ada pengecoran, laporan salah". Cron kirimPengingatNihilTerjadwal: query baris 78 memuat status perlu_koreksi → nihilBeruntun = 3 → grup WA paket ditagih "Pekerjaan berhenti 3 hari" atas klaim yang sedang dibantah pemeriksanya. Komentar di baris 76-77 menulis "hanya laporan yang SUDAH DIKIRIM yang boleh dihitung", DECISIONS 396 menyebut draft "bisa berubah" – perlu_koreksi persis sama sifatnya (EDITABLE_STATUSES) – dan lifecycle.ts 92 sudah menyediakan daftar kanonik untuk itu. Ini daftar status ketiga yang ditulis tangan (CIP §Source-of-truth gate: COUNTED_REPORT_STATUSES).

**Bukti.** penjadwal-nihil.ts 72-81: const laporan = await db.dailyReport.findMany({ where: { locationId: l.id, reportDate: { gte: mulai, lte: hariIni }, // Draft belum menyatakan apa pun; hanya laporan yang SUDAH DIKIRIM // yang boleh dihitung sebagai pernyataan. status: { not: "draft" }, },

**Tes terkait.** tests/integration/hari-nihil.test.ts:258-278 hanya menguji draft di tengah; tidak ada kasus perlu_koreksi.

**Usul perbaikan.** Ganti dengan `status: { in: [...COUNTED_REPORT_STATUSES] }` dari lifecycle.ts; tambah uji: hari nihil berstatus perlu_koreksi memutus hitungan.

**Reproduksi.** Berkas uji sementara tests/integration/_audit-laporan-transisi-8.test.ts (sudah DIHAPUS; `git status --porcelain` kosong). Fixture meniru tests/integration/hari-nihil.test.ts tapi memakai jalur service asli: getOrCreateDraft → setHariNihil(hujan) → submitReport (dikirim) untuk 18, 19, 20 Agu, lalu returnReport(id20, "20 Agu ada pengecoran, laporan salah") → perlu_koreksi. Hasil: MERAH, 2/2 gagal. - Uji 1 (20 Agu dikembalikan): assert status=perlu_koreksi OK, noActivity=true OK (returnReport meneruskan data `{}` – service.ts:602-609 – jadi pernyataan nihil tidak dibersihkan), lalu `expect(h.ter

### A-6. 🟡 Pindah tanggal laporan final meninggalkan PDF & jejak Drive bertanggal LAMA: berkas lama tetap di folder Drive KKP dan papan status tanggal lama menampilkan "Sudah di Drive" tanpa laporan

`src/lib/daily-report/pindah-tanggal-service.ts:155` · sedang · data-integritas · `laporan-transisi-6`

**Skenario.** Laporan final 21 Jul sudah diunggah otomatis: Drive berisi `Laporan Harian - <Lokasi> - 2026-07-21.pdf` (kirim.ts 177), GDriveJob (laporan_harian, slug:2026-07-21) sukses, GDriveUpload refKey slug:2026-07-21 sukses. Super admin memindahkannya ke 22 Jul: update reportDate → finalizeReport → antrekanLaporanHarian meng-upsert job BARU slug:2026-07-22 saja (antrean.ts 298-301); job & log lama tidak disentuh, berkas lama tidak dihapus/diganti nama (unggah ulang hanya mengganti berkas SENAMA, DECISIONS 146). Akibat: (1) di folder KKP ada dua blanko harian resmi untuk pekerjaan yang sama, salah satunya bertanggal salah; (2) /laporan/status-harian?tanggal=2026-07-21 (status-harian.ts 109-116 mencocokkan refKey slug:dateKey) menampilkan Laporan="Belum lapor" tapi Drive="Sudah" dengan tautan ke PDF tanggal salah; (3) HasilPindah menyebut cuaca, WA, foto, snapshot – tidak menyebut Drive, padahal komentar modulnya berjanji menyebut semua akibat yang tidak ikut benar sendiri.

**Bukti.** pindah-tanggal-service.ts 155-170: await db.dailyReport.update({ where: { id: report.id }, data: { reportDate: tanggalBaru, weather: null, ..., waSentAt: null, waSentById: null } }); if (lewatFinal) { await finalizeReport(report.id, input.userId); } // tidak ada penanganan GDriveJob/GDriveUpload tanggal lama gdrive/antrean.ts 298-301: const periode = r.reportDate.toISOString().slice(0, 10); const refKey = refKeyHarian(r.location.slug, periode); await db.gDriveJob.upsert({ where: { kind_refKey: { kind: "laporan_harian", refKey } }, ... gdrive/kirim.ts 177: fileName: safeFileName(`Laporan Harian - ${c.locationName} - ${dateKey}.pdf`)

**Tes terkait.** tests/integration/pindah-tanggal-laporan.test.ts tidak menyentuh GDriveJob/GDriveUpload; tests/integration/gdrive-antrean-otomatis.test.ts tidak menguji pindah tanggal.

**Usul perbaikan.** Saat pindah tanggal laporan yang pernah diunggah: tandai job/log refKey lama sebagai dicabut (atau hapus berkas lama di Drive lewat jalur unggah), dan laporkan di HasilPindah ("berkas Drive bertanggal lama perlu dihapus/diganti"). Papan status seharusnya tidak menampilkan "Sudah di Drive" untuk tanggal tanpa laporan.

**Reproduksi.** MERAH (temuan terbukti). Uji integrasi sementara tests/integration/_audit-laporan-transisi-6.test.ts (sudah dihapus, git status bersih): paket ber-driveFolderId → laporan 2026-06-08 difinalkan (finalizeReport → antrekanLaporanHarian membuat GDriveJob slug:2026-06-08) → job disimulasikan `sukses` + satu baris GDriveUpload sukses "Laporan Harian - Lokasi AU6 - 2026-06-08.pdf" dengan webLink → pindahTanggalLaporan ke 2026-06-12 oleh super_admin. Keluaran: - `HasilPindah keys: ['dari','ke','lewatFinal','cuacaDibuang','waDilepas','foto','snapshotDibangunUlang']` – tidak ada apa pun tentang Drive. -

### A-7. 🟡 Workspace harian: kolom Kumulatif baris basis draft_adendum berbalik saat laporan dikirim (20/50 → 0/50) karena kumulatif dibaca dengan cakupan `aktif`

`src/lib/daily-report/queries.ts:269` · sedang · bug · `laporan-transisi-4`

**Skenario.** Fixture persis tests/integration/laporan-basis-adendum.test.ts: item LK_BARU hanya ada di draft adendum (volume 50), dilaporkan 20 (basis draft_adendum). Saat status draft: `includesSelf=false` → volumeCumulative = base(0) + 20 = 20 → layar "Kumulatif: 20 / 50 (40%)". Setelah dikirim (counted): `includesSelf=true` → volumeCumulative = base = cumulativeVolumeByLineage(location, reportDate) dengan cakupan default "aktif" → baris draft_adendum TIDAK ikut → 0 → layar "Kumulatif: 0 / 50 (0%)" pada baris yang volumenya 20, baik di ReportEditor (perlu_koreksi) maupun tabel read-only (page.tsx 146-173). Angka berubah tanpa satu pun data berubah. Jalur guard (upsertItem 159-163) dan daftar pilihan (getLeafNodeOptions 90) sudah memakai cakupan "semua" untuk basis draft; hanya penyaji workspace yang tidak.

**Bukti.** queries.ts 268-270: const [cumulative, photoViews] = await Promise.all([ cumulativeVolumeByLineage(location.id, reportDate), // cakupan default = "aktif" buildPhotoViews(report.photos), ]); queries.ts 320-327: const includesSelf = counted.has(report.status); const base = cumulative.get(it.lineageKey) ?? 0; const volumeCumulative = Math.round((includesSelf ? base : base + volumeDone) * 1000) / 1000;

**Tes terkait.** tests/integration/laporan-basis-adendum.test.ts tidak memanggil getWorkspaceData; tidak ada tes penyaji workspace untuk basis draft.

**Usul perbaikan.** Ambil dua peta kumulatif (aktif & semua) dan pilih per baris menurut `it.basis` (draft_adendum → "semua"), konsisten dengan upsertItem. Uji MERAH: getWorkspaceData sebelum vs sesudah submit harus memberi volumeCumulative sama untuk baris draft.

**Reproduksi.** GAGAL (merah) – uji integrasi sementara tests/integration/_audit-laporan-transisi-4.test.ts (sudah dihapus). Fixture: RAB aktif I#1 vol 100; draft adendum I#1 vol 150 + I#9 vol 50; laporan 2026-08-01 status draft; upsertItem(I#9 lewat node draft, volumeDone 20) → basis draft_adendum. Lalu getWorkspaceData(slug,'2026-08-01') di tiap status (status diubah langsung di DB): DRAFT : { volumeDone: 20, volumeCumulative: 20, pct: 40 } DIKIRIM : { volumeDone: 20, volumeCumulative: 0, pct: 0 } KOREKSI : { volumeDone: 20, volumeCumulative: 20, pct: 40 } SETUJU : { volumeDone: 20, volumeCumulative: 0, pct

### A-8. 🟡 Papan status harian menganggap laporan hari nihil sebagai "Kosong" sehingga tombol Unggah Drive & Kirim WA dimatikan — dan unit test justru mengunci perilaku itu

`src/lib/daily-report/status-harian-tabel.ts:74` · sedang · alur · `laporan-transisi-7`

**Skenario.** Lokasi menyatakan 2026-08-11 nihil (hujan), final. Di /laporan/status-harian, barisTabel menghitung `adaIsi = itemCount>0 || kegiatanCount>0 || fotoCount>0` = false (getStatusHarian tidak membaca noActivity sama sekali). tabel-status.tsx 139-143 & 195 mengubahnya jadi halangan "Kosong" berupa teks mati bersilang merah: Unggah dan Kirim tidak bisa ditekan, judulnya "Belum ada isi yang bisa diunggah/dikirim". Padahal DECISIONS 396 memposisikan hari nihil sebagai pernyataan resmi yang dicetak untuk pemeriksa PPK, dan server (unggahLaporanHarian/kirim.ts 140-200, sendDailyReportPdfToWaAction) sama sekali tidak menolak laporan tanpa item. Pengawas lintas lokasi tidak punya jalan mengirim blanko nihil dari papan ini – hanya jalur otomatis Drive pasca-final yang tetap berjalan. Unit test status-harian-tabel.test.ts:86-93 menegaskan `adaIsi=false` untuk 0 item/0 foto/0 kegiatan, ditulis sebelum konsep nihil ada, sehingga perbaikannya akan memerahkan tes yang salah lapis.

**Bukti.** status-harian-tabel.ts 74: const adaIsi = r.itemCount > 0 || r.kegiatanCount > 0 || r.fotoCount > 0; tabel-status.tsx 139-143: const halangan = !d.punyaFolderDrive ? "Tanpa folder" : !d.adaIsi ? "Kosong" : null; status-harian.ts 91-97 (select laporan): select: { status: true, waSentAt: true, _count: { select: { items: true, photos: true } } } // tanpa noActivity tests/unit/status-harian-tabel.test.ts:89: expect(barisTabel(sumber({ itemCount: 0, fotoCount: 0, kegiatanCount: 0 })).adaIsi).toBe(false);

**Tes terkait.** tests/unit/status-harian-tabel.test.ts:86-93 ("`adaIsi` false hanya bila BENAR-BENAR kosong") – tes yang menjaga cacatnya untuk kasus nihil.

**Usul perbaikan.** Bawa `noActivity` ke StatusHarianRow/SumberBaris dan hitung `adaIsi = ... || noActivity` (atau `status !== null` bila memang laporan apa pun boleh diunggah); perbarui tes agar laporan nihil dianggap punya isi.

**Reproduksi.** GAGAL (merah) – tests/integration/_audit-laporan-transisi-7.test.ts (sudah DIHAPUS; `git status --porcelain` bersih). Fixture: org+paket (driveFolderId+waGroupId) + lokasi + user super_admin; getOrCreateDraft → setHariNihil(hujan) → submitReport → status final via DB → baris GDriveUpload sukses "Laporan Harian - … .pdf" dengan refKey `<slug>:2026-08-11`. Lalu getStatusHarian(user,null,"2026-08-11") + barisTabel + ekspresi halangan persis tabel-status.tsx. Keluaran: ROW: {"status":"final","itemCount":0,"fotoCount":0,"kegiatanCount":0,"drive":"sukses","keys":[...,"waSentAt","drive"]} ← tidak ada

---

## B. Laporan harian – angka & aliran data

### B-1. 🟠 /hari-ini 'Target minggu ini' membandingkan target MINGGUAN dengan realisasi KUMULATIF sepanjang proyek (definisi berbeda dari halaman RAB/rencana mingguan)

`src/lib/daily-report/queries.ts:499` · tinggi · bug · `laporan-angka-13`

**Skenario.** Rencana mingguan minggu ke-6 menargetkan 'Pasangan batu' 20 m³. Item itu sudah dikerjakan 25 m³ pada minggu 2–4, dan minggu ini 0 m³. getHariIniLocation memanggil cumulativeVolumeByLineage(locationId) TANPA rentang → realizedVolume=25 → layar pelaksana (hari-ini/page.tsx:129) menulis '25/20 m3' seolah target minggu ini tercapai, padahal minggu ini belum ada pekerjaan. Halaman lokasi › RAB (rab/page.tsx:199-213) untuk rencana minggu yang SAMA menghitung realisasi dalam [weekStart, weekEnd] → '0/20'. Dua layar, satu target, dua 'realisasi'. Ini persis kelas cacat yang sudah dicatat dan diperbaiki di tempat lain (progress.ts:409-415 tentang DECISIONS 459: 'membalik kesimpulannya').

**Bukti.** queries.ts:482-483, 495-501 getRecentDays(locationId, 7), cumulativeVolumeByLineage(locationId), ... weeklyTargets: (weeklyPlan?.items ?? []).map((it) => ({ name: it.rabNode.name, targetVolume: Number(it.targetVolume), realizedVolume: cumulative.get(it.rabNode.lineageKey) ?? 0, rab/page.tsx:206-210: reportDate: { gte: weekStart, lte: weekEnd }

**Tes terkait.** Tidak ada tes untuk getHariIniLocation.weeklyTargets (tests/unit/hari-ini-ringkas.test.ts hanya memakai weeklyTargets: []).

**Usul perbaikan.** Pakai volumeDalamRentangByLineage([{locationId, sejak: weekStart, sampai: weekEnd}]) (progress.ts:426) untuk realizedVolume, seperti rab/page.tsx. Tes integrasi merah-dulu: realisasi 25 sebelum minggu, 0 di minggu → realizedVolume harus 0.

**Reproduksi.** GAGAL (merah, temuan terbukti). Uji integrasi sementara tests/integration/_audit-laporan-angka-13.test.ts (sudah DIHAPUS, `git status --short` bersih): kontrak berjalan, RAB aktif item 'Pasangan batu' lineage I#1 vol 100; laporan DIKIRIM 25 m3 bertanggal weekStart−10 hari; rencana minggu ke-6 (weekStart=hari ini−3, weekEnd=hari ini+3) target 20; tidak ada laporan di dalam minggu. Hasil: (1) volumeDalamRentangByLineage([{locationId, sejak: weekStart, sampai: weekEnd}]) → 0 (lulus); (2) getHariIniLocation(locationId).weeklyTargets → [{"name":"Pasangan batu","unit":"m3","targetVolume":20,"realize

### B-2. 🟠 Guard volume saat kirim & 'Volume Kontrak' di workspace/blanko/snapshot memakai node RAB yang menempel saat item disimpan (bisa revisi 'digantikan'), bukan revisi aktif → volume melebihi kontrak lolos dan blanko harian menyebut volume kontrak yang berbeda dari laporan mingguan

`src/lib/daily-report/service.ts:461` · tinggi · bug · `laporan-angka-12`

**Skenario.** Item lineage X di revisi #1 volume 100. Laporan counted sebelumnya Σ=50. Mandor menyimpan draft hari ini dengan X=40 (upsertItem menaut rabNodeId ke node revisi #1). Sebelum draft dikirim, adendum revisi #2 diaktifkan: volume X turun jadi 60 (lineage sama). Aktivasi hanya menaikkan `basis` (import.ts:201-204) dan menyesuaikan laporan COUNTED (50 ≤ 60 → tidak diubah); rabNodeId draft tidak pernah dipetakan ulang. Draft dikirim → assertVolumeWithinRab membaca `it.rabNode.volume` = 100 (node revisi #1 yang sudah 'digantikan'): others 50 + 40 = 90 ≤ 100 → LOLOS, padahal kontrak berlaku 60 (fixture emas #13 'volume melebihi kontrak ditolak' terlewati). Akibat lanjutan: progress.ts memang membatasi 100%, tetapi laporan mingguan menulis volSd 90 atas volK 60; blanko harian pratinjau (queries.ts:941) dan finalSnapshot (service.ts:822) menulis 'Volume Kontrak 100' dan pctCumulative 90% untuk item yang di laporan mingguan tercetak 100% atas kontrak 60 – dua dokumen resmi, dua volume kontrak. Workspace (queries.ts:328) untuk semua laporan yang dibuat sebelum adendum juga terus memakai volume revisi lama.

**Bukti.** service.ts:421-427, 460-465 const items = await tx.dailyReportItem.findMany({ where: { reportId: report.id }, select: { lineageKey: true, basis: true, volumeDone: true, rabNode: { select: { code: true, name: true, unit: true, volume: true } } }, ... const volK = it.rabNode.volume != null ? Number(it.rabNode.volume) : null; import.ts:24-26 (klaim): "layer progress ... mencocokkan by lineage_key terhadap node revisi AKTIF, bukan by rabNodeId" – klaim itu tidak berlaku untuk guard & volumeContract di atas.

**Tes terkait.** tests/integration/laporan-basis-adendum.test.ts / adendum-sesuaikan-realisasi.test.ts tidak menguji draft yang tersimpan SEBELUM aktivasi lalu dikirim SESUDAHNYA; tidak ada tes yang membandingkan volumeContract blanko harian dengan volK laporan mingguan sesudah adendum.

**Usul perbaikan.** Di assertVolumeWithinRab, getWorkspaceData, getKkpDailyData, dan buildFinalSnapshot ambil volK/volumeContract dari node revisi AKTIF via lineageKey (pola yang sudah dipakai ringkas.ts:312-329), dan bila lineage tidak ada di revisi aktif tulis null + tandai. Alternatif: saat aktivasi petakan ulang rabNodeId item basis aktif ke node revisi baru dengan lineage sama (audit per baris). Tes merah-dulu: skenario di atas harus menolak kirim dengan pesan sisa 10.

**Reproduksi.** Berkas sementara tests/integration/_audit-laporan-angka-12.test.ts (SUDAH DIHAPUS; `git status --short` kosong). Skenario: revisi #1 aktif I#1 vol 100 → laporan A (2026-08-01, disetujui) 50 → laporan B (2026-08-02, draft) 40 via `upsertItem` pada node revisi #1 → revisi #2 draft I#1 vol 60 → `activateRevision` → `submitReport(B)`. Dijalankan: `DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-laporan-angka-12.test.ts`. Hasil: 7 tests | 5 failed | 2 passed. - prasyarat LULUS: A tetap 50; item B masih `rabNodeId` = node revis

### B-3. 🟠 Kurva-S laporan periodik menghitung minggu berjalan TANPA weekMode → pada kontrak senin_minggu realisasi kurva berhenti satu minggu lebih awal dari tabel di dokumen yang sama

`src/lib/periodic-report.ts:682` · tinggi · bug · `laporan-angka-10`

**Skenario.** Kontrak weekMode=senin_minggu, SPMK Kamis 2026-09-10. Hari ini Senin 2026-09-14 (minggu kalender ke-2; M1 = Kamis–Minggu). User membuka/cetak/unduh laporan mingguan ke-2 (weekIndex=2 dari baris 705/454, bucket volume di baris 693 memakai weekMode). Baris 682 memanggil currentWeekNumber(startDate, seriesLen, today) TANPA mode → jatuh ke default tujuh_hari → currentWeek=1 → cutoffWeek=min(1,2)=1 (baris 709) → scurve.actualPct[1]=null dan scurve.currentWeek=1, padahal actualPct (=totals.bobotSd) dan deviationPct sudah memuat laporan 2026-09-14. PDF periodik (pdf/periodik-kkp.ts:128-129), Excel (export/xlsx.ts:116-117), layar scurve-kkp-sheet.tsx:61-62, dan rincian-jadwal-data.ts memakai scurve.currentWeek/actualPct itu: garis realisasi & kolom deviasi minggu ke-2 kosong sementara tabel dan angka deviasi di halaman yang sama menyebut minggu ke-2. Terulang tiap Senin–Rabu sepanjang kontrak (3 dari 7 hari) untuk SPMK yang bukan Senin. Dibuktikan skrip scratch (vitest, 5/5 lulus): currentWeekNumber(2026-09-10, 18, 2026-09-14)=1, weekOfDate(...,'senin_minggu')=2, currentWeekNumber(..., 'senin_minggu')=2.

**Bukti.** periodic-report.ts:681-682 const today = new Date(`${jakartaDateKey(new Date())}T00:00:00.000Z`); const currentWeek = currentWeekNumber(startDate, seriesLen, today); bandingkan baris 412 (getPeriodBounds) & 693/705: const currentWeek = currentWeekNumber(startDate, totalWeeks, now, weekMode); const wk = Math.min(seriesLen, Math.max(1, weekOfDate(startDate, r.report.reportDate, weekMode))); progress.ts:56-61 signature: (startDate, totalWeeks, now = new Date(), mode: WeekPeriodMode = "tujuh_hari")

**Tes terkait.** tests/integration/periodic-report.test.ts – fixture SPMK Senin 2026-01-05 (kedua mode identik) dan baris 285 `if (atN == null) continue;` menoleransi titik kurva yang kosong, jadi cacat ini tidak pernah merah. tests/integration/sampul-harian-mode-minggu.test.ts memakai senin_minggu tapi tidak menyentuh getPeriodReport().scurve.

**Usul perbaikan.** Teruskan weekMode: `currentWeekNumber(startDate, seriesLen, today, weekMode)` (atau pakai bounds.currentWeek yang sudah benar). Tambah tes integrasi: SPMK Kamis + senin_minggu, laporan bertanggal Senin minggu ke-2, assert scurve.actualPct[1] != null dan scurve.currentWeek === 2 (merah dulu), dan ganti `continue` di periodic-report.test.ts:285 dengan assertion eksplisit `n <= currentWeek ⇒ atN != null`.

**Reproduksi.** MERAH. Berkas uji sementara tests/integration/_audit-laporan-angka-10.test.ts (sudah DIHAPUS; `git status --porcelain` kosong) dijalankan dengan DATABASE_URL=...marlin_test APP_ENV=test. Fixture: kontrak weekMode=senin_minggu, SPMK Kamis 2026-09-10, durasi 120 hari, RAB 1 kategori/1 item (bobot 100%), satu laporan `final` bertanggal Senin 2026-09-14 volume 10/100; jam dinding dipatok vi.useFakeTimers 2026-09-14T03:00Z (10:00 WIB Senin). Hasil 3 tes: 2 lulus, 1 gagal. - Lulus: currentWeekNumber(2026-09-10, 18, 2026-09-14) = 1; weekOfDate(..., 'senin_minggu') = 2; currentWeekNumber(..., 'senin_m

### B-4. 🟠 Aktivasi adendum menurunkan volumeDone/valueDone laporan FINAL tetapi finalSnapshot tidak dibangun ulang → blanko harian final tetap mencetak volume lama, berbeda dari laporan mingguan/progres

`src/lib/rab/import.ts:330` · tinggi · data-integritas · `laporan-angka-11`

**Skenario.** Persis kasus user 2026-09-03 (sesuaikan-realisasi.ts:10-19): item dilaporkan 45,7 m³ lintas 3 hari, laporan terakhir sudah FINAL. Adendum diaktifkan dengan volume 32,149. activateRevision → sesuaikanRealisasiKeVolumeBaru menulis ulang volumeDone/valueDone ketiga baris (termasuk yang final) dan mengaudit. TIDAK ada pemanggilan buildFinalSnapshot (pemanggilnya hanya service.ts:903, pindah-tanggal-service.ts:192, system/actions.ts:272). getKkpDailyData untuk laporan final membaca `report.finalSnapshot` (queries.ts:850-852: snapshotToKkp) → blanko harian yang dicetak/dikirim ke Drive & WA tetap menulis volumeToday/volumeCumulative/pctCumulative/valueDone lama (mis. 45,7 m³ atas kontrak 32,15), sedangkan laporan mingguan (periodic-report.ts:496-519 membaca baris DB) dan progres memakai angka baru. Dokumen yang justru menjadi alasan fitur ini ("blanko harian menuliskan 45,7 m³ terpasang atas baris kontrak 32,15 m³") tetap salah sampai admin tahu harus menekan 'Bangun ulang snapshot' di menu Sistem.

**Bukti.** import.ts:328-336 for (const [i, r] of rows.entries()) { if (sesudah[i] === sebelum[i]) continue; await tx.dailyReportItem.update({ where: { id: r.id }, data: { volumeDone: sesudah[i]!, valueDone: hitungNilai(sesudah[i]!, harga) }, }); queries.ts:850-852 if (report?.status === "final" && report.finalSnapshot) { const snap = report.finalSnapshot as unknown as FinalSnapshot; const base = snapshotToKkp(snap);

**Tes terkait.** tests/integration/adendum-sesuaikan-realisasi.test.ts:147-153 hanya membaca dailyReportItem.volumeDone (lapisan DB), tidak memanggil getKkpDailyData/menguji finalSnapshot – 'tes hijau di lapisan yang salah' (CARA_KERJA_AGEN §5). tests/integration/kkp-harian-final.test.ts:231 justru menegaskan angka blanko final SELALU dari snapshot beku, sehingga tanpa pembangunan ulang keduanya 'lulus' sambil dokumennya salah.

**Usul perbaikan.** Di activateRevision (setelah penyesuaian, masih dalam transaksi atau tepat sesudahnya) bangun ulang finalSnapshot untuk setiap laporan final yang barisnya berubah ATAU yang tanggalnya ≥ tanggal baris terawal yang disesuaikan di lokasi itu (kumulatif 's/d' ikut bergeser) – pola yang sama dengan pindah-tanggal-service.ts:179-202. Tambah tes: setelah activateRevision, getKkpDailyData(slug, tanggal final).items[].volumeToday harus sama dengan volumeDone di DB (merah dulu). Kalau user memutuskan snapshot final TIDAK boleh berubah, konflik dengan keputusan 2026-09-03 harus dicatat sebagai DECISIONS.

**Reproduksi.** Berkas uji sementara tests/integration/_audit-laporan-angka-11.test.ts (DATABASE_URL=...marlin_test APP_ENV=test; sudah dihapus, `git status` bersih). Fixture: RAB aktif item I#1 vol 45,7; tiga laporan (10/8=20, 12/8=15,7 disetujui, 15/8=10 FINAL); snapshot laporan final dibekukan lewat `buildFinalSnapshot` persis seperti finalizeReport; draft adendum vol 32,1493; lalu `activateRevision`. Hasil: 1 failed | 2 passed. ✓ sebelum aktivasi snapshot & DB sama (10 m3; s/d 45,7) × setelah aktivasi: blanko harian final harus sama dengan volumeDone di DB AssertionError: expected 10 to be 7.035 DB volume

### B-5. 🟡 Aktivasi adendum menurunkan volume laporan harian pengguna secara diam-diam di layar: jumlah item yang disesuaikan hanya masuk audit, tidak dikatakan di UI (DECISIONS 203 / CLAUDE.md 'penyesuaian wajib dikatakan di UI')

`src/app/(app)/lokasi/[slug]/rab/actions.ts:129` · sedang · konflik-spesifikasi · `laporan-angka-17`

**Skenario.** PM menekan 'Aktifkan' pada draft adendum yang menurunkan volume 3 item di bawah realisasi tercatat. activateRevision menulis ulang volumeDone/valueDone 9 baris laporan (termasuk final) dan mengembalikan hanya `activated.revisi` (import.ts:247) – angka `disesuaikan` tinggal di audit log. activateDraftAction menampilkan 'Revisi #2 aktif. Baseline kurva-S di-regenerate.' tanpa menyebut bahwa laporan harian lapangan baru saja diubah. Pelaksana yang besok membuka laporannya melihat volume yang bukan angka yang ia ketik, tanpa penjelasan di layar mana pun. KODE: import.ts:210-247 & rab/actions.ts:108-129. DOKUMEN: DECISIONS 203 + CLAUDE.md aturan coding ('penyesuaiannya harus seragam DAN dikatakan di UI'; CARA_KERJA_AGEN §5 baris 'Angka pengguna dibetulkan diam-diam'). TES: adendum-sesuaikan-realisasi.test.ts hanya memeriksa audit log, bukan pesan aksi.

**Bukti.** import.ts:244-247 itemDisesuaikan: activated.disesuaikan, }); return activated.revisi; rab/actions.ts:128-129 revalidateRab(rev.location.slug); return { success: `Revisi #${rev.revisionNo} aktif. Baseline kurva-S di-regenerate.` };

**Tes terkait.** tests/integration/adendum-sesuaikan-realisasi.test.ts:165-182 (audit saja).

**Usul perbaikan.** activateRevision mengembalikan {revisi, laporanDinaikkan, disesuaikan}; activateDraftAction & impor adendum (rab/import/actions.ts:758) menyebutnya di pesan sukses ('N item realisasi diturunkan mengikuti volume adendum – lihat riwayat'), dan idealnya panel konfirmasi sebelum aktivasi memperlihatkan daftar item/tanggal yang akan berubah. Tes: pesan sukses memuat angka disesuaikan (merah dulu).

**Reproduksi.** Berkas sementara tests/integration/_audit-laporan-angka-17.test.ts (sudah DIHAPUS; `git status` bersih). Data: RAB aktif item I#1 volume 45,7; tiga laporan harian 20 / 15,7 / 10 (yang terakhir status `final`); draft #2 volume 32,1493, draft #3 volume 20. Sesi, `pastikanBolehAktivasi`, dan `regenerateBaseline` di-mock supaya jalur SUKSES aksi yang teruji. Hasil: 2/2 MERAH. (1) `activateRevision` mengembalikan hanya record revisi – keys: id, locationId, revisionNo, source, amendmentId, status, totalValue, note, sourceDocumentId, createdById, createdAt, updatedAt, supersededAt; `expect(hasil).toH

### B-6. 🟡 Bobot foto di halaman dokumentasi blanko harian dihitung valueDone/grandTotal (harga beku saat lapor, tanpa batas 100%) sedangkan PDF ringkas harian memakai vol/volK × amount revisi aktif → dua bobot berbeda untuk foto yang sama; RAB tanpa harga satuan mencetak 'Bobot 0,00%'

`src/lib/daily-report/queries.ts:774` · sedang · formula-duplikat · `laporan-angka-14`

**Skenario.** (a) RAB impor dengan kolom harga satuan kosong (hps-parser.ts:740 unit_price boleh null; progress-calc.ts:245-247 mengakui ini) tapi amount terisi: service.ts:182 menghitung valueDone = round(vol × 0) = 0 → bobotPerLineage = bobotPct(0, grandTotal) = 0 → kkp-daily-photos.tsx:262 mencetak '0,00%' di bawah foto pekerjaan yang nyata bernilai; PDF ringkas (ringkas.ts:339-343) untuk hari yang sama menulis 0,5%. (b) Adendum menaikkan harga 400rb→500rb: foto blanko harian 0,40%, ringkas 0,50%. (c) Volume hari itu melebihi volK: ringkas dibatasi prestasiPct ≤100, blanko harian tidak. Dibuktikan skrip scratch: bobotPct(0,1e9)=0 vs (prestasiPct(10,100)/100)×bobotPct(50e6,1e9)=0,5; 0,4 vs 0,5. Audit 2026-08-28 (I-1) sudah memperbaiki hal yang sama di ringkas.ts tetapi situs ini terlewat.

**Bukti.** queries.ts:770-775 const { grandTotal } = await getLocationProgress(location.id, { asOf: reportDate }); if (grandTotal > 0n) for (const it of report?.items ?? []) { if (it.basis !== "aktif") continue; bobotPerLineage.set(it.lineageKey, bobotPct(Number(it.valueDone), Number(grandTotal))); ringkas.ts:339-343 function bobotHarian(lineageKey: string, volumeHariIni: number): number { return (prestasiPct(volumeHariIni, n.volK) / 100) * bobotPct(n.amount, Number(grandTotal));

**Tes terkait.** tests/unit/kkp-cetak-lengkap.test.tsx & pdf-dokumentasi-tautan.test.ts menerima `bobot` sebagai input jadi; tidak ada tes rekonsiliasi bobot foto blanko harian vs ringkas harian (Reconciliation gate CIP).

**Usul perbaikan.** Hitung bobot per lineage di getKkpDailyData dengan cara yang sama dengan ringkas.ts (node revisi aktif via lineageKey, prestasiPct × bobotPct), idealnya diangkat ke satu helper di progress-calc.ts agar dua penyaji tak bisa berbeda. Tes unit merah-dulu: unitPrice null + amount > 0 → bobot > 0.

**Reproduksi.** Uji integrasi sementara tests/integration/_audit-laporan-angka-14.test.ts (sudah DIHAPUS; `git status --short` kosong) memakai jalur masuk nyata getOrCreateDraft → upsertItem → submitReport, foto tertaut ke baris item, lalu memanggil getKkpDailyData dan getRingkasHarian pada laporan yang SAMA. Hasil: 2 tests | 2 failed (MERAH). (a) item unitPrice NULL, amount 100 jt, grand 125 jt, volume hari ini 20/100 → `valueDone tersimpan (harga null): 0`; `ringkas.bobotToday = 16 | kartu foto bobot = 0` → AssertionError: expected +0 to be close to 16. Karena 0 !== null, kkp-daily-photos.tsx:262 / harian-k

### B-7. 🟡 Definisi 'hari tanpa laporan terkirim' di EWS (status counted saja) bertentangan dengan DECISIONS 483 dan kolom 'Terakhir lapor' /progress (submittedAt, termasuk perlu_koreksi)

`src/lib/ews/builder.ts:65` · sedang · konflik-spesifikasi · `laporan-angka-16`

**Skenario.** Lokasi mengirim laporan tiap hari, tetapi 10 laporan terakhir semuanya dikembalikan ke perlu_koreksi; laporan counted terakhir 10 hari lalu. /progress (queries.ts:1152-1166, groupBy submittedAt not null) menulis 'Terakhir lapor: kemarin'; pada saat yang sama /perlu-tindakan (EWS) menghitung max reportDate dengan status ∈ COUNTED → hariTanpaLaporan=10 → warning TINGGI 'tanpa_laporan_tinggi: 10 hari tanpa laporan harian terkirim' (rules.ts:177-186). Dashboard KPI 'submittedToday' (dashboard.ts:118-119) juga memakai status counted. Tiga papan, dua definisi 'sudah lapor'. KODE: ews/builder.ts:63-67 (COUNTED). DOKUMEN: DECISIONS 483 butir 1 – 'Yang dihitung laporan yang DIKIRIM (submittedAt), bukan yang disetujui ... membuangnya akan menuduh lapangan tidak melapor justru pada hari mereka melapor lalu diminta membetulkan' (ditulis untuk /progress, tidak menyebut EWS). TES: tests/unit/ews-rules.test.ts:53-55 menerima hariTanpaLaporan sebagai fakta jadi; tests/integration/progress-terakhir-lapor.test.ts menjaga definisi submittedAt untuk /progress. Belum ada keputusan apakah EWS harus ikut 483.

**Bukti.** ews/builder.ts:63-67 db.dailyReport.groupBy({ by: ["locationId"], where: { locationId: { in: ids }, status: { in: [...COUNTED_REPORT_STATUSES] } }, _max: { reportDate: true }, }), queries.ts:1156-1159 const baris = await db.dailyReport.groupBy({ by: ["locationId"], where: { locationId: { in: locationIds }, submittedAt: { not: null } },

**Tes terkait.** tests/unit/ews-rules.test.ts (fakta disuntik, tidak menguji builder); tests/integration/progress-terakhir-lapor.test.ts (hanya /progress).

**Usul perbaikan.** Tanya user apakah 'tanpa laporan terkirim' di EWS/Dashboard mengikuti DECISIONS 483 (submittedAt). Bila ya: EWS memakai getLastSubmittedReportDates dan alasannya diberi label 'terkirim' vs 'terhitung'; bila tidak: ubah label EWS menjadi 'tanpa laporan TERHITUNG' supaya dua papan tidak saling bantah, lalu catat di arsip keputusan.

**Reproduksi.** MERAH (direproduksi, berkas uji sementara tests/integration/_audit-laporan-angka-16.test.ts sudah DIHAPUS; `git status --porcelain` kosong). Fixture: kontrak mulai 60 hari lalu, lokasi `berjalan`, user field_supervisor ber-penugasan; 1 laporan `disetujui` 10 hari lalu, lalu 9 laporan `perlu_koreksi` (submittedAt terisi) untuk 9 hari terakhir berturut-turut. Hasil: - `getLastSubmittedReportDates` (/progress, DECISIONS 483): terakhir lapor = 1 hari lalu → uji LULUS. - `bangunEws(user)`: `[{"ruleId":"tanpa_laporan_tinggi","severity":"tinggi","alasan":"10 hari tanpa laporan harian terkirim."},{"ru

### B-8. 🟡 Mode tujuh_hari menghitung minggu berjalan dari jam dinding UTC (bukan tanggal Asia/Jakarta) → antara 00.00–07.00 WIB pada hari pertama tiap minggu kontrak dashboard/progress/EWS memakai rencana minggu sebelumnya, berbeda dari laporan periodik

`src/lib/progress.ts:69` · sedang · bug · `laporan-angka-15`

**Skenario.** Kontrak tujuh_hari, SPMK Senin 2026-08-03. Site Manager membuka Beranda/Progress/Perlu Tindakan pukul 06.00 WIB Senin 2026-08-10 (hari ke-7 = awal minggu 2 menurut tanggal kerja). getLocationsProgress tanpa asOf → currentWeekNumber(start, totalWeeks, new Date()) → (2026-08-09T23:00Z − 2026-08-03T00:00Z) = 6,96 hari → minggu 1 → planPct minggu 1, deviasi ikut. Laporan periodik pada jam yang sama (periodic-report.ts:411/681 memakai jakartaDateKey) → minggu 2. Pada hari SPMK sebelum 07.00 WIB bahkan terbaca minggu 0 ('belum mulai'). Cabang senin_minggu di baris 63-66 sudah mengakui masalah ini ('jam dinding UTC bisa masih kemarin padahal di Jakarta sudah Senin') tapi cabang tujuh_hari tidak ikut diperbaiki. Situs serupa: mingguan/kirim.ts:33-37 mingguKontrak memakai tengahMalam(now) = tanggal UTC, dan asOfMinggu (baris 103-106) meneruskan `now` mentah ke SQL `report_date <= asOf::date` (progress.ts:227) sehingga tombol manual 'pratinjau/kirim minggu berjalan' yang ditekan pagi hari mengecualikan laporan hari itu dan nomor minggunya bisa mundur satu. Dibuktikan skrip scratch: currentWeekNumber(2026-08-03, 6, 2026-08-10T06:00+07:00, 'tujuh_hari') = 1, jam 08:00 = 2, mode senin_minggu jam 06:00 = 2; hari SPMK 06:00 WIB = 0.

**Bukti.** progress.ts:62-71 if (mode === "senin_minggu") { const today = new Date(`${jakartaDateKey(now)}T00:00:00.000Z`); const wk = weekOfDate(startDate, today, "senin_minggu"); return wk === 0 ? 0 : Math.min(wk, Math.max(totalWeeks, 1)); } const wk = Math.floor((now.getTime() - startDate.getTime()) / WEEK_MS) + 1; if (wk < 1) return 0;

**Tes terkait.** tests/unit/pengingat-harian.test.ts:83-89 hanya memakai jam 08:00 WIB (sesudah 07:00) sehingga jendela cacatnya tidak pernah diuji; CIP 'Date-as-of gate: timezone Asia/Jakarta, dilarang new Date() tersembunyi' dan uji wajib 'timezone tidak menggeser reportDate' tidak mencakup minggu rencana.

**Usul perbaikan.** Normalisasi `now` ke tanggal kerja Jakarta untuk KEDUA mode di currentWeekNumber (const today = jakartaDateKey(now) → Date UTC-midnight, lalu weekOfDate(start, today, mode)); di mingguan/kirim.ts pakai jakartaDateKey untuk tengahMalam/asOf. Tes merah-dulu: 06:00 WIB hari ke-7 → minggu 2.

**Reproduksi.** Berkas sementara tests/unit/_audit-laporan-angka-15.test.ts (dijalankan dengan TZ=UTC, lalu DIHAPUS; git status bersih). Hasil: 4 GAGAL | 2 lulus. × currentWeekNumber(SPMK 2026-08-03, 6, 2026-08-10T06:00+07:00, "tujuh_hari") → expected 2, received 1 × currentWeekNumber(SPMK, 6, 2026-08-03T06:00+07:00, "tujuh_hari") → expected 1, received 0 (hari SPMK sendiri terbaca "belum mulai") × mingguKontrak(SPMK, 2026-08-10T06:00+07:00, "tujuh_hari") → expected 2, received 1 (kirim.ts:33-37; per pembacaan kode cabang senin_minggu di baris 34 juga memakai tengahMalam(now)=tanggal UTC sehingga sama-sama mu

---

## C. Laporan harian – masukan (rekap, WA, foto)

### C-1. 🔴 Impor rekap: kode item ganda antar kategori → last-wins, uraian diabaikan, volume mendarat di item lain (nondeterministik)

`src/lib/daily-report/recap-parse.ts:166` · kritis · data-integritas · `laporan-input-18`

**Skenario.** RAB KNMP lazim memakai kode item yang berulang antar kategori/sub (mis. item "1" di kategori I dan "1" di kategori II; fixture nyata di tests/unit/rab-xlsx.test.ts:163-166 dan rab-ekspor-impor-ulang.test.ts memuat tiga item berkode "1"; lineageKey membedakannya via path, kode-nya tidak). Template rekap (import/template/route.ts:44) menulis `kode: l.code` apa adanya sehingga admin mengisi Kode=1, Uraian=Galian. `matchRows` membangun `byCode` dengan `byCode.set(codeKey, l)` tanpa cek duplikat → leaf TERAKHIR yang menang, urutan `db.rabNode.findMany` (recap-import.ts:47) tanpa `orderBy` → bisa berbeda antara request pratinjau dan request commit. Di `findLeaf`, bila kode cocok, NAMA tidak pernah dilihat. Hasil: baris "1 / Galian / 5 m3" tersimpan sebagai item "Beton" (lineage II#1#1), lalu ikut `dikirim` → masuk reportedProgress resmi; item Galian tetap 0. Dibuktikan MERAH dengan uji sekali-jalan: `matchRows([{code:"1", name:"Galian"}], [Galian(I#1), Beton(II#1#1)])` → `matchedNodeId: "n-beton"`.

**Bukti.** recap-parse.ts:166-175: for (const l of leaves) { const codeKey = normalize(l.code); if (codeKey) byCode.set(codeKey, l); // last-wins const nameKey = normalize(l.name); if (nameKey && !byName.has(nameKey)) byName.set(nameKey, l); // first-wins } const findLeaf = (row) => { const codeKey = normalize(row.code); if (codeKey && byCode.has(codeKey)) return byCode.get(codeKey)!; // nama diabaikan

**Tes terkait.** tests/unit/recap-import.test.ts (semua fixture berkode unik, jadi tidak pernah menyentuh kasus ini)

**Usul perbaikan.** Jangan mengandalkan `code` sebagai kunci unik. Opsi: (a) template menulis kolom tersembunyi/eksplisit `lineageKey` (atau `id` node) dan parser mencocokkan lewat itu lebih dulu; (b) bila kode tidak unik di revisi aktif, `byCode` tidak boleh dipakai – cocokkan kode+nama, dan bila tetap ambigu tandai status baru `ambiguous` (bukan `ok`) dengan menyebut kandidatnya; (c) `getRecapLeaves` beri `orderBy: sortOrder` supaya pratinjau dan commit deterministik. Tambah uji: dua leaf berkode sama, baris dengan kode itu harus ditolak/ambigu, bukan dicocokkan ke salah satu.

**Reproduksi.** Berkas uji SEMENTARA tests/unit/_audit-laporan-input-18.test.ts (sudah DIHAPUS; `git status --porcelain` kosong). Leaves: Galian {code:"1", lineageKey:"I#1", unitPrice 10.000} dan Beton {code:"1", lineageKey:"II#1#1", unitPrice 1.000.000}; baris rekap {code:"1", name:"Galian", volume:5}. `pnpm vitest run tests/unit/_audit-laporan-input-18.test.ts` → Test Files 1 failed, Tests 2 failed (2): - Uji 1 "harus mendarat di Galian atau ditolak": AssertionError expected false to be true. console: `urutan [galian, beton] → ok n-beton Beton 5000000` (status "ok", node Beton, valueDone Rp 5.000.000; sehar

### C-2. 🟠 Impor rekap: dua baris tanggal sama + item sama → pratinjau menjumlahkan, commit menimpa (volume hilang diam-diam, itemsSaved dihitung dua)

`src/lib/daily-report/recap-import.ts:108` · tinggi · data-integritas · `laporan-input-19`

**Skenario.** File rekap memuat dua baris untuk tanggal 12/07 pada pekerjaan yang sama (mis. pagi 3 m3 dan sore 4 m3, atau dua bangunan dengan kode/nama sama). `matchRows` menaruh keduanya `ok` (running total 3+4 ≤ RAB) dan `buildRecapPreview.totalValue` = 7 × harga; admin menyetujui pratinjau. `commitRecap` memanggil `upsertItem` per baris; `upsertItem` meng-upsert pada kunci unik (reportId, lineageKey) dengan `update: { volumeDone }` sehingga baris kedua MENIMPA baris pertama: yang tersimpan 4, bukan 7. `result.itemsSaved` tetap bertambah 2 dan tidak ada peringatan. Angka yang diunggah pengguna berubah diam-diam (DECISIONS 203) dan masuk status `dikirim`. Bagian murni dibuktikan MERAH dengan uji sekali-jalan (dua baris → status [ok, ok], total 70.000); bagian DB terbaca langsung dari upsert di service.ts:185-198.

**Bukti.** recap-import.ts:108-112: for (const row of rows) { await upsertItem(report.id, { rabNodeId: row.matchedNodeId!, volumeDone: row.volume }, userId); result.itemsSaved += 1; service.ts:185-187: const item = await db.dailyReportItem.upsert({ where: { reportId_lineageKey: { reportId, lineageKey: node.lineageKey } }, update: { rabNodeId: node.id, basis, volumeDone, valueDone, notes, reportedById: userId },

**Tes terkait.** tests/unit/recap-import.test.ts (kasus over_volume lintas baris memakai TANGGAL BERBEDA, jadi jalur timpa ini tidak pernah diuji)

**Usul perbaikan.** Di `matchRows` (lapisan murni) deteksi (dateKey, lineageKey) ganda: gabungkan volumenya menjadi satu baris dengan keterangan "digabung dari N baris", ATAU tandai baris kedua dengan status baru `duplicate_row` (bukan `ok`) dan jangan dihitung di totalValue. Tambah uji unit: dua baris tanggal+lineage sama tidak boleh keduanya `ok` tanpa penggabungan yang disebut. Tambah uji integrasi: volume tersimpan harus sama dengan yang dijanjikan pratinjau.

**Reproduksi.** Berkas uji SEMENTARA tests/integration/_audit-laporan-input-19.test.ts (DB marlin_test, APP_ENV=test) – SUDAH DIHAPUS, `git status --porcelain` kosong. Fixture: 1 lokasi, RAB aktif 1 item "Galian Tanah" kode 1.1 volume 100 m3 harga 10.000, lineageKey "I#1.1". Excel rekap 2 baris: (2026-07-12, 1.1, Galian Tanah, 3) dan (2026-07-12, 1.1, Galian Tanah, 4). Dipanggil buildRecapPreview lalu commitRecap (jalur sama dengan commitRecapAction). Keluaran: PREVIEW {"statuses":["ok","ok"],"okCount":2,"totalValue":70000,"dates":[{"dateKey":"2026-07-12","count":2}]} COMMIT {"reportsTouched":1,"itemsSaved":2

### C-3. 🟠 Laporan yang sudah dinyatakan NIHIL tetap bisa menerima item/material lalu dikirim → blanko mencetak "TIDAK ADA KEGIATAN" bersama volume, hari dihitung nihil beruntun

`src/lib/daily-report/service.ts:111` · tinggi · data-integritas · `laporan-input-21`

**Skenario.** Mandor menyatakan tanggal D "Tidak ada kegiatan – hujan" (draft, `noActivity=true`). Admin lalu mengimpor rekap yang memuat baris tanggal D (jalur `commitRecap` → `getOrCreateDraft` mengambil draft itu → `upsertItem` → `submitReport`). `upsertItem` (dan `setEnrichment` untuk material/alat) tidak memeriksa `noActivity`; `submitReport` hanya menegakkan aturan nihil bila ketiga hitungan nol (baris 564-586), jadi laporan ber-item DAN ber-`noActivity=true` lolos ke `dikirim`. Penjaga satu arah hanya ada di `setHariNihil` (tidak boleh nihil kalau sudah ada item) dan di UI (`report-editor.tsx:116` menyembunyikan form) – padahal CLAUDE.md #3: frontend hanya menyembunyikan menu. Akibat: `kkp-daily-report.tsx:576` (`teksNihilCetak`) mencetak "TIDAK ADA KEGIATAN – Hujan" di blanko resmi yang juga memuat baris volume; `penjadwal-nihil.ts:86` menghitungnya sebagai hari nihil (peringatan "pekerjaan berhenti 3 hari" ke grup PPK padahal ada progres); `paparan/snapshot.ts:352` menaikkan `hariNihil`. Jalur manual juga terbuka lewat dua tab (form item dimuat sebelum nihil dinyatakan di tab lain). Belum bisa dijalankan sebagai uji integrasi di sini (tidak ada PostgreSQL), tetapi jalur kodenya tidak bercabang.

**Bukti.** service.ts:564-571 (submitReport): if (itemCount === 0 && materialCount === 0 && equipmentCount === 0) { if (!laporan?.noActivity) { throw ... } ... } // bila ada item, noActivity=true tidak pernah diperiksa service.ts:111-119 (upsertItem): hanya memeriksa EDITABLE_STATUSES & volume>0 – tidak ada pemeriksaan report.noActivity.

**Tes terkait.** tests/integration/hari-nihil.test.ts:161-168 hanya menguji arah 'ada item → tidak boleh nihil', bukan sebaliknya

**Usul perbaikan.** Tegakkan dua arah di service (bukan UI): `upsertItem`/`setEnrichment` menolak bila `report.noActivity` ("Hari ini dinyatakan tidak ada kegiatan – batalkan pernyataannya dulu"), DAN `submitReport` menolak bila `noActivity && (item|material|alat) > 0`. `commitRecap` harus melaporkan baris tanggal itu sebagai dilewati dengan sebab. Tambah uji integrasi arah sebaliknya di tests/integration/hari-nihil.test.ts (nyatakan nihil → upsertItem harus ditolak; nihil+item → submit harus ditolak).

**Reproduksi.** Uji integrasi sementara tests/integration/_audit-laporan-input-21.test.ts (sudah DIHAPUS, git status bersih dari berkas saya; berkas _audit-laporan-input-22.test.ts milik agen lain dibiarkan) dijalankan dengan DATABASE_URL=...marlin_test APP_ENV=test. Hasil: 4/4 asersi harapan-spek MERAH. 1. nihil (setHariNihil hujan) → upsertItem(volume 5): "promise resolved {…(11)} instead of rejecting" – item TERSIMPAN ([AUDIT] item tersimpan setelah nihil: 1). 2. nihil + 1 item → submitReport: resolved, status=dikirim, noActivity=true, noActivityReason=hujan. 3. nihil → setEnrichment(materials:[Semen 40 sa

### C-4. 🟡 Impor rekap mengambil alih draft/perlu_koreksi milik mandor dan mengirimkannya (termasuk item yang bukan dari rekap, dan laporan yang sedang dikoreksi)

`src/lib/daily-report/recap-import.ts:98` · sedang · alur · `laporan-input-22`

**Skenario.** Tujuan fitur (komentar berkas: "kasus lapangan lupa lapor") adalah merekonstruksi hari yang tidak dilaporkan. Tetapi `getOrCreateDraft` mengembalikan laporan yang SUDAH ADA: (1) draft mandor yang masih diisi (item X sudah tersimpan, foto belum) – admin mengimpor item Y untuk tanggal itu → `submitReport` mengirim X dan Y sekaligus, `submittedById` = admin, dan mandor tidak lagi bisa menambah foto ("Laporan sudah dikirim – foto tidak bisa ditambah lagi"); (2) laporan `perlu_koreksi` yang dikembalikan SM dengan alasan "volume X salah" – rekap menambah Y lalu mengirim ulang TANPA X diperbaiki, sehingga alasan pengembalian terlewati dan SM menerima laporan yang sama lagi. Tidak ada pesan di `RecapCommitResult` yang menyebut bahwa laporan berisi item lain ikut terkirim.

**Bukti.** recap-import.ts:98 & 119-122: report = await getOrCreateDraft(locationId, dateKey, userId); ... if (savedForDate > 0 && (report.status === "draft" || report.status === "perlu_koreksi")) { await submitReport(report.id, userId);

**Tes terkait.** belum ada uji integrasi commitRecap sama sekali

**Usul perbaikan.** Hanya auto-submit laporan yang DIBUAT oleh impor ini atau yang seluruh itemnya berasal dari baris rekap; untuk draft/perlu_koreksi yang sudah berisi item lain (atau `noActivity`), simpan itemnya tapi biarkan statusnya dan laporkan di hasil: "tanggal D tidak dikirim otomatis – laporan sudah berisi N item/ sedang dikoreksi". Pertimbangkan menolak tanggal berstatus `perlu_koreksi` sama sekali.

**Reproduksi.** Berkas sementara tests/integration/_audit-laporan-input-22.test.ts (sudah DIHAPUS; `git status --short` bersih). Setup: 1 lokasi, RAB aktif dengan item X (I#1) dan Y (I#2), user mandor/SM/admin (project_manager). Rekap Excel dibuat via ExcelJS (header Tanggal·Kode·Uraian·Volume). Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-laporan-input-22.test.ts Hasil: 2 tests | 2 failed (MERAH = temuan terbukti). (1) draft mandor berisi X=5 (belum dikirim), admin commitRecap dengan baris Y=7 tanggal sama: HASIL (1): {"rep

### C-5. 🟡 Impor rekap: sel teks berdesimal titik ("12.5") dibaca 125 – angka pengguna diubah 10×/100× tanpa peringatan

`src/lib/daily-report/recap-parse.ts:74` · sedang · bug · `laporan-input-20`

**Skenario.** Admin menempel "12.5" (hasil salin dari WA/aplikasi lain) ke kolom Volume di Excel berlokal Indonesia; Excel tidak bisa mengurainya sebagai angka dan menyimpannya sebagai TEKS. `toNum` menghapus SEMUA titik lebih dulu (`replace(/\./g, "")`) lalu mengganti koma → "125" → volume 125. Kalau sisa RAB ≥ 125, baris lolos `over_volume` dan masuk laporan `dikirim` sebagai 125 m3. Dibuktikan MERAH: workbook dengan sel teks "12.5" → `parseRecapWorkbook` mengembalikan `volume: 125` (sedangkan "1,5" benar → 1.5). Melanggar DECISIONS 203: angka pengguna dipakai apa adanya atau ditolak dengan sebab, bukan dibetulkan diam-diam.

**Bukti.** recap-parse.ts:66-76: function toNum(v) { ... const n = Number(String(v).replace(/\./g, "").replace(",", ".").trim()); return Number.isFinite(n) ? n : 0; }

**Tes terkait.** tests/unit/recap-import.test.ts (hanya sel numerik yang diuji)

**Usul perbaikan.** Untuk sel bertipe string, jangan menebak konvensi: terima hanya bentuk yang tidak ambigu (angka murni, satu pemisah desimal), dan bila memuat titik DAN tidak memuat koma dengan pola "d.d" (bukan ribuan "1.234"), tandai baris `bad_number`/tolak dengan menyebut nilai mentahnya. Tambah uji: "12.5" tidak boleh menjadi 125.

**Reproduksi.** MERAH, direproduksi dengan berkas uji sementara tests/unit/_audit-laporan-input-20.test.ts (sudah dihapus; `git status --porcelain` kosong). Workbook ExcelJS dengan header "Tanggal · Kode · Uraian Pekerjaan · Volume" dan sel Volume bertipe STRING, lalu `parseRecapWorkbook` + `matchRows` (leaf volume 200, unitPrice 50.000): PARSED: [[2,125],[3,1.5],[4,1500],[5,3333]] ← "12.5"→125, "1,5"→1.5, "1.500"→1500, "3.333"→3333 MATCH: [[2,"ok",125,6250000],[3,"ok",1.5,75000],[4,"over_volume",1500,…],[5,"over_volume",3333,…]] AssertionError: expected 125 to be 12.5 Baris "12.5" berstatus `ok` dengan value

### C-6. 🟡 "Kemajuan hari ini" di ringkasan harian (PDF & WA) dihitung ulang di luar lapisan kanonik dan berbeda dari aturan itemAchievement (batas 100% pada kumulatif, periode diturunkan dengan pengurangan)

`src/lib/daily-report/ringkas.ts:342` · sedang · formula-duplikat · `laporan-input-25`

**Skenario.** `bobotHarian` = prestasiPct(volumeHariIni, volK)/100 × bobotPct(...) – prestasi dihitung LANGSUNG dari volume hari ini, dengan batas 100% per hari. Aturan kanonik (progress-calc.ts:44-62 `itemAchievement`, CIP: "Batas 100% dipasang di KUMULATIF saja. Kolom periode diturunkan dengan pengurangan") mendefinisikan kontribusi periode = prestasiSd − prestasiLalu. Keduanya identik hanya selama kumulatif ≤ volK; begitu adendum mengurangi volK di bawah realisasi (kasus yang justru melahirkan DECISIONS 151), untuk tanggal yang SAMA: blanko mingguan/bulanan menyebut item itu menyumbang 0%, sedangkan PDF harian (`pdf/harian-ringkas.ts:514,714` "Kemajuan hari ini") dan pesan WA (`ringkas-wa.ts:106`) menyebut >0%. Dibuktikan MERAH dengan uji murni: volK=10, volLalu=10, volIni=2, bobot 5% → `itemAchievement().bobotIni` = 0, rumus ringkas.ts = 1%. Ini melanggar reconciliation gate ("perbedaan hanya boleh bila dataAsOf/level status berbeda") dan aturan "formula hanya di lima berkas kanonik"; komposisi yang sama juga disalin di baseline.ts:853 dan periodic-report.ts:721 (di luar lingkup audit ini, disebut sebagai bukti penyalinan).

**Bukti.** ringkas.ts:339-343: function bobotHarian(lineageKey: string, volumeHariIni: number): number { const n = nodeAktif.get(lineageKey); if (!n) return 0; return (prestasiPct(volumeHariIni, n.volK) / 100) * bobotPct(n.amount, Number(grandTotal)); } progress-calc.ts:53,59: const prestasiIni = Math.max(0, prestasiSd - prestasiLalu); bobotIni: (prestasiIni / 100) * bobot,

**Tes terkait.** tests/integration/progres-harian-vs-mingguan.test.ts (perlu ditambah kasus kumulatif > volK)

**Usul perbaikan.** Hitung `bobotToday` lewat `itemAchievement({ volK, volLalu: kumulatifSampaiKemarin, volIni: volumeHariIni, bobot }).bobotIni` (kumulatif s/d H-1 sudah tersedia di jalur snapshot: `cumulativeVolumeByLineage(locationId, reportDate)` dikurangi volume hari ini). Kalau user memang menghendaki definisi harian yang berbeda, tuliskan sebagai keputusan (konflik-spesifikasi) dan beri label berbeda di PDF/WA; jangan dua definisi diam-diam. Tambah uji paritas harian vs mingguan untuk kasus volK dikurangi adendum.

**Reproduksi.** GAGAL (merah, sesuai klaim). Berkas sementara tests/integration/_audit-laporan-input-25.test.ts dijalankan dengan DATABASE_URL=...marlin_test APP_ENV=test, lalu DIHAPUS (git status bersih). Fixture: kontrak mulai 2026-08-01 (weekMode default senin_minggu), revisi 1 item volK=100 amount 100 jt / grand 125 jt; laporan disetujui 20 Agt (20 m3, minggu 4) dan 25 Agt (5 m3, minggu 5); lalu adendum revisi 2 volK=20 (di bawah realisasi 25). Keluaran: ringkas bobotToday = 20 | kanonik bobotIni = 0 ringkas bobotHariIni = 20 | realizedPct(asOf) = 80 mingguan volLalu/volIni/volSd = 20 5 25 | prestasiLalu/

### C-7. 🟡 hapusFotoCepatAction: foto kantong tanpa lokasi bisa dihapus siapa pun pemegang photo.quick – tidak ada pemeriksaan pemilik

`src/lib/foto-cepat/actions.ts:296` · sedang · authz · `laporan-input-23`

**Skenario.** Foto yang lokasinya belum terdeteksi hanya terlihat oleh pengunggahnya (`getKantong` queries.ts:89 `{ locationId: null, uploadedById: user.id }`) dan hanya bisa DITETAPKAN oleh pengunggahnya (`tetapkanLokasiAction` :409-418 menyaring `uploadedById: actor.id`). Tetapi `hapusFotoCepatAction` untuk `p.locationId == null` melewati `requireLocationAccess` (tidak ada lokasi) dan tidak memeriksa `uploadedById`; `hapusFotoKantong` (service.ts:135-162) juga tidak. Mandor B yang mengetahui id foto milik mandor A (mis. dari tautan/riwayat audit/tangkapan layar) memanggil aksi ini → foto A berikut arsip aslinya di R2 dihapus permanen. Pelanggaran prinsip CLAUDE.md #3 (backend harus menegakkan, bukan UI). ID UUID memang tidak mudah ditebak, karena itu tingkatnya sedang, bukan kritis.

**Bukti.** foto-cepat/actions.ts:296-305: const p = await db.photo.findUnique({ where: { id: id.data }, select: { id: true, locationId: true, reportId: true, activityId: true } }); if (!p) return { error: "Foto tidak ditemukan." }; if (p.reportId || p.activityId) return { error: "Foto ini sudah dipakai – hapus lewat laporannya." }; if (p.locationId) await requireLocationAccess(actor, p.locationId); await hapusFotoKantong(p.id, actor.id); // uploadedById tidak pernah diperiksa

**Tes terkait.** tests/integration/foto-cepat.test.ts (tidak menguji hapus sama sekali)

**Usul perbaikan.** Terapkan aturan yang sama dengan `tetapkanLokasiAction`: untuk `locationId == null` wajib `uploadedById === actor.id` (super_admin boleh dikecualikan secara eksplisit). Tambah uji integrasi: user lain memanggil hapus pada foto tanpa lokasi milik orang lain → ditolak, baris tetap ada.

**Reproduksi.** Berkas uji sementara tests/integration/_audit-laporan-input-23.test.ts (DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test; sudah DIHAPUS, git status bersih dari jejak saya). Setup: org X dengan mandor A & B (field_supervisor = pemegang photo.quick sah), org Y dengan mandor C; foto kantong locationId=null, reportId/activityId=null, uploadedById=A, ber-originalKey. Mock hanya server-only/next/r2 (r2Delete dicatat) dan requireCapability→user sesi; requireLocationAccess TIDAK di-mock (tidak pernah tersentuh karena locationId null). Hasil: 2 failed | 1 passed. - "mando

### C-8. 🟡 Antrean pengingat grup tidak menyaring dateKey: giliran hari-hari lalu yang tertahan (WAHA belum dikonfigurasi/mati) dikirim belakangan sebagai pengingat 'hari ini' untuk tanggal lampau

`src/lib/harian/penjadwal-grup.ts:119` · sedang · alur · `laporan-input-27`

**Skenario.** Sakelar pengingat grup menyala, tetapi WAHA belum dikonfigurasi selama beberapa hari: `antrekanPengingatGrup` tetap membuat satu baris `menunggu` per paket per hari (UNIQUE hanya (package_id, date_key)), sedangkan `kurasPengingatGrup` keluar lebih awal (`berhenti: "WAHA belum dikonfigurasi."`) tanpa menyentuh barisnya. Begitu WAHA aktif, `findMany({ status: "menunggu", sendAfter: { lte: now } })` mengambil SEMUA baris lama, menghitung `tagihanPerPaket(tanggalLama)` dan mengirim ke grup PPK "*Laporan Harian <tanggal 3 hari lalu> – Paket X* … belum lengkap hari ini" untuk tiap hari yang tertahan (4 pesan per putaran, berjeda 1 menit). Kalimat "hari ini" pada tanggal lampau menyesatkan pembaca di grup pemberi kerja.

**Bukti.** penjadwal-grup.ts:117-124: if (!(await isWahaConfigured())) return { ...hasil, berhenti: "WAHA belum dikonfigurasi." }; const antre = await db.groupReminderJob.findMany({ where: { status: "menunggu", sendAfter: { lte: now } }, // tanpa saringan dateKey orderBy: { sendAfter: "asc" }, take: maks,

**Tes terkait.** tests/integration/pengingat-grup.test.ts (perlu kasus giliran bertanggal lampau)

**Usul perbaikan.** Kedaluwarsakan giliran yang `dateKey`-nya bukan hari ini (Asia/Jakarta) – tandai `dilewati` dengan `lastError: "kedaluwarsa"` – atau saring `dateKey: jakartaDateKey(now)` di query dan bersihkan sisanya di putaran harian. Tambah uji: baris `menunggu` bertanggal kemarin tidak boleh terkirim.

**Reproduksi.** MERAH (temuan terbukti). Berkas uji sementara tests/integration/_audit-laporan-input-27.test.ts (sudah DIHAPUS; `git status --porcelain` kosong) menjalankan skenario penuh lewat fungsi produksi: 3 hari berturut-turut `antrekanPengingatGrup(H-3..H-1)` + `kurasPengingatGrup` dengan `isWahaConfigured=false` (semua berhenti "WAHA belum dikonfigurasi."), lalu WAHA hidup dan `kurasPengingatGrup({ now: 2026-08-29 18.00 WIB })`. Keluaran: giliran tertahan: dateKey 2026-08-26/27/28 semua `menunggu` hasil kuras: { aktif: true, dikerjakan: 3, terkirim: 3, dilewati: 0, gagal: 0 } PESAN TERKIRIM >>> *Lapor

### C-9. 🟢 Tombol "Simpan" tanpa perubahan pada ringkasan chat berstatus final/sent selalu gagal: mesin transisi tidak punya final→final / sent→sent

`src/lib/waha/summary-actions.ts:104` · rendah · alur · `laporan-input-26`

**Skenario.** Panel ringkasan (`chat-grup/panel-ringkasan.tsx:192`, `summary-panel.tsx:191`) menampilkan tombol Simpan (`mode=simpan`) untuk semua status. Pengguna membuka ringkasan `final`, tidak mengubah teks, menekan Simpan → `changed=false`, `target = from = "final"` → `transitionSummary("final","final")` → TRANSITIONS.final = [draft_ai, edited_draft, sent] → error "Status "Final" tidak bisa langsung menjadi "Final"." Sama untuk `sent`. Dibuktikan MERAH: `transitionSummary("final","final").ok === false`, `transitionSummary("sent","sent").ok === false`. Pesan galatnya membingungkan (menyebut transisi ke status yang sama) padahal tidak ada yang perlu diubah.

**Bukti.** summary-actions.ts:104-106: const target = mode === "final" ? "final" : changed ? "edited_draft" : from === "draft_ai" ? "draft_ai" : from; const gate = transitionSummary(from, target as ...); if (!gate.ok) return { error: gate.error }; summary-lifecycle.ts:46-47: final: ["draft_ai", "edited_draft", "sent"], sent: ["draft_ai", "edited_draft"],

**Tes terkait.** tests/unit (summary-lifecycle) – tidak menguji jalur Simpan tanpa perubahan

**Usul perbaikan.** Bila `!changed && mode === "simpan"` kembalikan `{ success: "Tidak ada perubahan." }` tanpa transisi, atau tambahkan self-loop `final→final`/`sent→sent` di TRANSITIONS secara eksplisit, atau nonaktifkan tombol Simpan bila teks tidak berubah. Tambah uji unit kecil di tests/unit untuk `saveSummaryDraftAction` (unchanged + final).

**Reproduksi.** MERAH (gagal, cacat terbukti). Berkas sementara tests/unit/_audit-laporan-input-26.test.ts mereplikasi ekspresi baris 104 summary-actions.ts (mode=simpan, changed=false) lalu memanggil transitionSummary(from, target) yang asli. Keluaran: final → final {"ok":false,"error":"Status \"Final\" tidak bisa langsung menjadi \"Final\"."} sent → sent {"ok":false,"error":"Status \"Terkirim\" tidak bisa langsung menjadi \"Terkirim\"."} Tests 2 failed | 2 passed (kontrol draft_ai/edited_draft unchanged → ok). Berkas uji sudah dihapus; `git status --short` kosong (tidak ada jejak).

---

## D. RAB – parser, lineage, ekspor

### D-1. 🟠 Ekspor → impor ulang menghilangkan item yatim berkode '~n' (dan '-.a' / '~1.a') tanpa peringatan; total tertulis 'JUMLAH (pra-PPN)' juga tidak pernah dicek karena regex TOTAL_BER_PPN cocok 'ppn'

`src/lib/export/rab-xlsx.ts:164` · tinggi · bug · `rab-parse-29`

**Skenario.** hps-parser memberi kode `~${orphanSeq}` pada baris kode-kosong bernilai sesudah item berharga (baris 1074, kasus 'Pengiriman'), dan `${parent?.code ?? "-"}.${code}` → '-.a' bila belum ada item numerik (baris 1081). Kode itu masuk DB dan ditulis apa adanya oleh ekspor (displayCode('~1') = '~1'). Saat berkas ekspor diimpor ulang, classifyRow('~1') = 'other': tidak ada cabang yang menangkapnya (bukan roman/SUBCODE/NUM/DOTNUM/DEEPCODE/LETTER/''), baris dibuang. Pagar 'Σ item vs total ditulis berkas' pun mati untuk berkas terbitan MARLIN sendiri: baris 'JUMLAH (pra-PPN)' lolos TOTAL_POLOS tetapi dibuang oleh TOTAL_BER_PPN (/ppn|.../ cocok pada 'pra-PPN'). Dibuktikan skrip scratch: ekspor 3 node (kategori, item '1' 2 jt, item '~1' 1 jt, total 3 jt) → parse ulang menghasilkan 1 item, total 2.000.000, warnings [] – satu juta hilang sunyi.

**Bukti.** hps-parser.ts:1074 const it = mkItem(`~${++orphanSeq}`, name, row, null); hps-parser.ts:1081 ? `${parent?.code ?? "-"}.${code}` hps-parser.ts:696-701 const TOTAL_POLOS = /^(jumlah|jumlah\s*harga|total|grand\s*total)\b/i; const TOTAL_BER_PPN = /ppn|pajak|termasuk|setelah|dibulatkan/i; if (!TOTAL_POLOS.test(t) || TOTAL_BER_PPN.test(t)) return;

**Tes terkait.** tests/unit/rab-ekspor-impor-ulang.test.ts (tidak ada kasus '~n'); tests/unit/hps-parser.test.ts:33 memastikan baris 'Pengiriman Tiang' TERHITUNG pada impor pertama, tapi tidak menguji bolak-balik.

**Usul perbaikan.** Parser harus mengenali kembali kode yang ia hasilkan sendiri (tambah pola `^~\d+(\.[a-z0-9]+)?$` dan `^-\.[a-z0-9]+$` sebagai item level-1 / anak), atau ekspor menulis kode yang bisa dibaca ulang (mis. penomoran lanjutan). Ubah TOTAL_BER_PPN agar 'pra-PPN' / 'sebelum PPN' TIDAK dianggap ber-PPN (mis. `/(?<!pra-|sebelum )ppn/`), dan tambahkan tes bolak-balik untuk item yatim yang menuntut total & jumlah item identik.

**Reproduksi.** Berkas sementara tests/unit/_audit-rab-parse-29.test.ts (sudah DIHAPUS; git status bersih). Hasil `pnpm vitest run` pada berkas itu: 4 failed | 1 passed. - classifyRow("~1")/("-.a")/("~1.a") → "other" (lulus, mengonfirmasi jalur). - (a) ekspor [kategori I 3jt; item "1" 2jt; item "~1" 1jt], total 3jt → parse ulang: items ['1=2000000'], total 2000000, warnings [] → GAGAL (expected 2 items / 3.000.000). - (b) expect warning "total yang DITULIS berkas" pada berkas ekspor yang sama → GAGAL (false): baris "JUMLAH (pra-PPN)" dibuang TOTAL_BER_PPN. - (c) rantai penuh: berkas sumber [I PEKERJAAN PERSIA

### D-2. 🟠 Beberapa modul masih memotong lineageKey dengan split('#')[0]; item di bawah kategori roman ganda ('VI#3#1') dianggap milik kategori 'VI' pertama

`src/lib/periodic-report.ts:525` · tinggi · data-integritas · `rab-parse-30`

**Skenario.** flatten memberi kategori roman ganda kunci 'VI#3' (dibuktikan skrip: keys ['VI','VI#1','VI#2','VI#3','VI#3#1','VI#3#3']). periodic-report.catOf() mengambil `lineageKey.split('#')[0]` = 'VI' untuk item 'VI#3#1' → realisasi seluruh kategori kedua dijumlahkan ke subtotal kategori PERTAMA di laporan mingguan/bulanan KKP, sementara kolom rencana (usableStored, s.lineageKey='VI#3') tetap di kategori kedua → satu laporan memuat rencana dan realisasi di kategori berbeda. Tempat lain dengan pemotongan yang sama: periodic-report.ts:658 (fallback jadwal), daily-report/queries.ts:577 (kategoriLookup blanko harian), daily-report/actions.ts:427 (bangunan pada cap foto), photo-restamp/service.ts:209. Bandingkan import.ts:532 dan plan/suggest.ts:74 yang sudah benar (prefix terpanjang + '#'). Catatan: di luar berkas wajib area RAB, dilaporkan karena lensa pemeriksaan memintanya secara eksplisit.

**Bukti.** periodic-report.ts:523-527 const catByRoot = new Map(kategoriNodes.map((nd) => [nd.lineageKey, nd])); const catOf = (lineageKey: string): PeriodCategory => { const root = lineageKey.split("#")[0]; let cat = catMap.get(root); import.ts:528-532 (yang benar) .sort((a, b) => b.key.length - a.key.length); catKeys.find((c) => lineageKey === c.key || lineageKey.startsWith(`${c.key}#`))

**Tes terkait.** tests/integration/periodic-report.test.ts (revision/lineage gate) tidak memuat fixture dengan kategori roman ganda.

**Usul perbaikan.** Satu helper kanonik `kategoriDariLineage(lineageKey, kategoriKeys)` (prefix terpanjang + '#', seperti import.ts:532) dan ganti semua `split('#')[0]`; tambah tes dengan dua kategori 'VI' yang menuntut subtotal realisasi masuk kategori kedua.

**Reproduksi.** MERAH (3/3 gagal). Tes integrasi sementara tests/integration/_audit-rab-parse-30.test.ts (sudah DIHAPUS, git status bersih) membuat RAB aktif dengan kunci persis keluaran flattenParsedRab untuk dua kategori roman "VI" (diverifikasi skrip scratchpad: ['kategori:VI:VI', 'item:1:VI#1', 'item:2:VI#2', 'kategori:VI#3:VI#3', 'item:1:VI#3#1', 'item:3:VI#3#3']), 4 item @Rp10 jt (bobot 25% masing-masing), baselineScheduleItem VI=minggu 1–4 saja & VI#3=minggu 5–8 saja, realisasi HANYA VI#3#1 = 50/100 di minggu 2. Hasil getPeriodReport(loc,'mingguan',2): categories: [{ key:'VI', code:'VI', name:'PEKERJAA

### D-3. 🟠 Sub kode ganda mendapat suffix GANDA ('II.1#2#2'); ekspor membocorkan 'II.1#2' dan impor ulang menghilangkan sub itu beserta lineage item-nya

`src/lib/rab/flatten.ts:78` · tinggi · bug · `rab-parse-28`

**Skenario.** Berkas HPS punya dua sub berkode sama dalam satu kategori (nyata di data KKP, lihat komentar flatten.ts baris 8-9), dan sub pertama punya item berkode '2'. hps-parser.pushSub sudah memberi kode 'II.1#2' pada sub kedua; flatten.dedup lalu menemukan key 'II#II.1#2' SUDAH dipakai item '2' sub pertama, sehingga sub kedua diberi kode 'II.1#2#2' / key 'II#II.1#2#2'. Ekspor (rab-xlsx.ts:74 displayCode hanya menghapus SATU suffix `#\d+$`) menulis 'II.1#2' di kolom Kode dokumen resmi. Impor ulang berkas ekspor itu: 'II.1#2' gagal SUBCODE (`/^[IVX]+\.\d+(?:\.\d+)*\.?$/`) → baris sub dibuang tanpa peringatan, item-itemnya bergabung ke sub PERTAMA, lineage berubah dari 'II#II.1#2#2#1' menjadi 'II#II.1#1#2' → realisasi item sub kedua lepas saat adendum. Dibuktikan skrip scratch: flatten → kode 'II.1#2#2'; ekspor → Detail RAB memuat 'II.1#2'; parse ulang → hanya 1 sub, items ['1 Galian','2 Urugan','1 Beton'], warnings kosong.

**Bukti.** flatten.ts:74-83 const dedup = (parentKey, code) => { let eff = code; let key = parentKey ? `${parentKey}#${eff}` : eff; for (let n = 2; usedKeys.has(key); n++) { eff = `${code}#${n}`; rab-xlsx.ts:74 const displayCode = (code: string) => code.replace(/#\d+$/, "").trim();

**Tes terkait.** tests/unit/rab-ekspor-impor-ulang.test.ts:117 hanya menguji suffix tunggal 'VI#2' (hijau, memberi rasa aman palsu); tests/unit/flatten.test.ts tidak punya kasus sub kembar dengan item '2'.

**Usul perbaikan.** Pisahkan suffix dedup dari path: simpan `code` asli di kolom code dan taruh penomoran ganda di kolom/penanda lain (mis. `dupSeq`), atau pakai pemisah path yang tidak mungkin muncul di suffix (mis. '/' untuk path, '#' untuk suffix) dengan migrasi lineageKey. Sementara: displayCode harus menghapus SEMUA suffix (`/(#\d+)+$/`), dan parser harus menerima kode sub ber-suffix atau menolak berkas dengan pesan. Tambahkan tes bolak-balik dengan sub kembar + item '2'.

**Reproduksi.** MERAH (gagal, membuktikan temuan). Berkas uji sementara tests/unit/_audit-rab-parse-28.test.ts dibuat, dijalankan sendiri, lalu DIHAPUS (git status bersih dari berkas saya; `_audit-rab-parse-29.test.ts` yang tersisa milik agen audit lain, tidak disentuh). Uji 1 – skenario buatan (kategori II, sub 'II.1 Pekerjaan Tanah' item 1 Galian + 2 Urugan, sub kembar 'II.1 Pekerjaan Beton' item 1 Beton K250), parse → flatten → buildRabXlsx → parse ulang sheet "Detail RAB" → samakanLineage vs node asli: PARSER sub codes: [ 'II.1', 'II.1#2' ] FLATTEN: sub code=II.1#2#2 key=II#II.1#2#2 parent=II ; item code=

### D-4. 🟡 Impor mode 'aktifkan' pada lokasi ber-RAB aktif tidak menghormati aturan satu draft per lokasi: draft kedua dibuat, berkas sumber tidak diarsipkan

`src/app/(app)/lokasi/[slug]/rab/import/actions.ts:730` · sedang · alur · `rab-parse-34`

**Skenario.** Lokasi punya RAB aktif dan draft adendum #5 (menunggu tanda tangan). User membuka kunci 'Jadikan RAB AKTIF sekarang' dan mengimpor berkas. createRevisionFromNodes membuat draft #6 (tidak memeriksa draft yang ada, berbeda dengan adendum.ts:154 yang melempar AdendumError bila masih ada draft), pastikanBolehAktivasi melempar PersetujuanError → return sukses 'tersimpan sebagai DRAFT' pada baris 751 SEBELUM arsipkanSumber (baris 777) dan sebelum revalidatePath /rab/adendum. Akibat: dua draft sekaligus; page.tsx mengambil draft pertama (revisi tertinggi) untuk persetujuanDraft dan meneruskannya ke SEMUA baris draft (revision-list.tsx:203) sehingga draft #5 memperlihatkan tanda tangan draft #6; impor mode draft berikutnya (findFirst tanpa orderBy) membuang salah satu draft secara acak; berkas sumber adendum tidak terarsip walau jalur draft mengarsipkannya.

**Bukti.** import/actions.ts:746-757 try { await pastikanBolehAktivasi(res.revisionId); } catch (e) { if (!(e instanceof PersetujuanError)) throw e; revalidatePath(`/lokasi/${location.slug}`, "layout"); return { success: `Revisi #${res.revisionNo} tersimpan sebagai DRAFT ...` }; adendum.ts:154-160 if (existingDraft) { throw new AdendumError(`Masih ada draft revisi #... – aktifkan atau buang dulu`); }

**Tes terkait.** Tidak ditemukan tes yang mengimpor mode 'aktifkan' saat draft lain sudah ada (tests/integration/adendum-empat-mata.test.ts tidak dibaca utuh).

**Usul perbaikan.** Sebelum createRevisionFromNodes pada mode aktifkan dengan activeRevision: tolak bila ada draft (pesan sama dengan adendum.ts) atau ganti draft itu secara eksplisit seperti jalur draft; pindahkan arsipkanSumber ke sebelum gerbang empat mata; page.tsx menghitung persetujuan per revisi draft.

**Reproduksi.** GAGAL (merah) – tests/integration/_audit-rab-parse-34.test.ts (sudah dihapus, `git status` bersih). Setup: lokasi dengan RAB aktif #1 (fixture rab-aktif-situbondo.xlsx, 903 item), kontrak ber-startDate, draft #2 dibuat via createAdendumDraft + 1 tanda tangan PD (setujuiRevisi). Lalu importHps mode=aktifkan dua langkah (pratinjau → confirm=1 + previewSha) dengan @/lib/r2 di-mock isR2Configured=true. Keluaran: - `HASIL aktifkan: success: 'Revisi #3 tersimpan sebagai DRAFT (903 item) – belum aktif ...'` - `DRAFT setelah impor aktifkan: [{revisionNo:2, sourceDocumentId:null, note:'draft lama'}, {r

### D-5. 🟡 Teks format Inggris '1,234.56' (koma SEBELUM titik, pasti bukan Indonesia) dibaca 1,23456; dengan dua koma menjadi null – tanpa peringatan

`src/lib/rab/angka-lokal.ts:95` · sedang · bug · `rab-parse-33`

**Skenario.** Sel ber-format Text hasil tempel dari Excel/aplikasi berlokal Inggris: volume '1,234.56', jumlah '1,234,560.00'. Aturan 'ada koma → koma desimal' mengubah '1,234.56' menjadi '1.23456' (1000× lebih kecil) dan '1,234,560.00' menjadi '1.234,56000' → Number() NaN → null, sehingga total_price null dan leafRaw jatuh ke volume×harga yang keduanya sudah salah. Dokumentasi modul mengklaim 'hanya yang PASTI yang ditafsirkan', padahal koma yang diikuti titik tidak mungkin bentuk Indonesia. Dibuktikan skrip: {'1,234.56': 1.23456, '1,234,560.00': null, '12,500': 12.5}. Cek-silang vol×harga=jumlah tidak berbunyi karena semua kolom salah searah atau null.

**Bukti.** angka-lokal.ts:95-99 if (t.includes(",")) { t = t.replace(/\./g, "").replace(",", "."); } else if ((t.match(/\./g) ?? []).length >= 2) { t = t.replace(/\./g, ""); }

**Tes terkait.** tests/unit/angka-lokal.test.ts – tidak ada kasus koma-lalu-titik; tidak ada tes yang menjaga cacatnya, tapi juga tidak ada yang menangkapnya.

**Usul perbaikan.** Bila keduanya ada, pemisah desimal = yang muncul TERAKHIR (koma terakhir → Indonesia; titik terakhir → Inggris). Koma lebih dari satu tanpa titik → pemisah ribuan. Kalau tetap ingin tidak menebak, kembalikan null DAN kumpulkan peringatan 'sel berformat Inggris' – jangan diam-diam 1/1000.

**Reproduksi.** MERAH (2 dari 4 uji gagal) dengan berkas sementara tests/unit/_audit-rab-parse-33.test.ts (sudah dihapus, git status bersih). Unit: `bacaAngkaLokal("1,234.56")` = 1.23456 (bukan 1234.56/null); `bacaAngkaLokal("1,234,560.00")` = null. Jalur penuh `parseHpsWorkbook` (worksheet dengan sel Text: vol "1,234.56", harga "1,000.00", jumlah "1,234,560.00"; baris 2 vol "5.00", harga "1,000,000.00", jumlah "5,000,000.00"; baris JUMLAH "6,234,560.00"): ITEMS: [{"code":"1","volume":1.23456,"unit_price":1,"total_price":null},{"code":"2","volume":5,"unit_price":null,"total_price":null}] TOTAL: 0 WARNINGS: ["

### D-6. 🟡 nilaiEksak() dan walkItem() menghitung nilai induk berbeda pada tiga jenjang; induk bersubtotal di atas anak yang 'dinaikkan' menggandakan nilai dan impor DITOLAK

`src/lib/rab/flatten.ts:97` · sedang · formula-duplikat · `rab-parse-32`

**Skenario.** Item '6' (grup, kolom Jumlah = subtotal 2100) → anak '6.1' Bekesting (vol×harga 1000) → cucu '6.1.a' 500, '6.1.b' 600 (bentuk GEMPOLSEWU baris 138 ditambah baris induk bersubtotal, konvensi yang sumLeaves kenali). nilaiEksak('6') = Σ nilaiEksak(anak) = leafRaw('6.1') = 1000 (cucu yang dinaikkan tidak ikut), sedangkan walkItem menjumlahkan childExact = 1000+500+600 = 2100. dinaikkan('6') lalu membandingkan 2100 vs 1000 → true → '6' dibaca sebagai ITEM 2100 dan '6.1','6.1.a','6.1.b' naik sejajar: grandTotal 4200. sumLeaves (hps-parser) = 2100. bedaAntarLayer menemukan selisih dan importHps menolak dengan pesan 'cacat pembacaan MARLIN' – berkas sah tidak bisa diimpor. Dibuktikan skrip: sumLeaves 2100, flatten 4200n, bedaAntarLayer [{I PEKERJAAN A: 2100 vs 4200}, {SELURUH BERKAS}]. Tanpa induk bersubtotal (varian C2) kedua layer sepakat 2100.

**Bukti.** flatten.ts:95-100 const nilaiEksak = (it) => { if (it.children.length === 0) return leafRaw(it); const anak = it.children.reduce((t, c) => t + nilaiEksak(c), 0); if (anak === 0) return leafRaw(it); return dinaikkan(it) ? leafRaw(it) : anak; flatten.ts:160-165 (walkItem) for (const ca of walkItem(ch, key, sink)) { aux.children.push(ca); childExact += ca.exact; }

**Tes terkait.** tests/unit/flatten.test.ts dan tests/unit/hps-parser.test.ts tidak punya kasus tiga jenjang dengan induk bersubtotal.

**Usul perbaikan.** Samakan definisi: nilaiEksak(c) untuk anak yang dinaikkan harus = leafRaw(c) + Σ nilaiEksak(cucu) (persis sumLeaves cabang 'own + childSum'), atau ekstrak SATU fungsi nilai-eksak pohon yang dipakai sumLeaves dan flatten. Tambah tes tiga jenjang yang menuntut bedaAntarLayer = [].

**Reproduksi.** MERAH. Berkas uji sementara tests/unit/_audit-rab-parse-32.test.ts (sudah DIHAPUS, `git status` bersih) dijalankan sendiri: `pnpm vitest run tests/unit/_audit-rab-parse-32.test.ts` → 1 failed | 2 passed. Varian A (grup '6' total_price=2100 tanpa volume/harga → anak '6.1' 1×1000 → cucu '6.1.a' 1×500, '6.1.b' 1×600): sumLeaves = 2100; grandTotal(flatten) = 4200n; nodes = [kategori:I=4200, item:6@I=2100, item:6.1@I=1000, item:6.1.a@I=500, item:6.1.b@I=600] – grup bersubtotal '6' dibaca sebagai ITEM 2100 dan seluruh turunannya naik sejajar, subtotalnya terhitung dua kali; bedaAntarLayer = [{label:

### D-7. 🟢 Penyesuaian realisasi saat aktivasi menghitung ulang valueDone dengan harga satuan revisi BARU, bertentangan dengan kontrak 'valueDone beku saat lapor'

`src/lib/rab/import.ts:335` · rendah · konflik-spesifikasi · `rab-parse-35`

**Skenario.** Adendum menurunkan volume item sekaligus (walau diperingatkan DECISIONS 213) mengubah harga satuannya. sesuaikanRealisasiKeVolumeBaru menulis ulang baris yang volumenya diturunkan dengan valueDone = round(volumeBaru × harga REVISI BARU) (harga = n.unitPrice dari node revisi yang baru aktif), sementara baris lain item yang sama tetap memakai harga lama yang beku. Satu item punya baris dengan dua basis harga; blanko harian/snapshot paparan menampilkan valueDone campuran. Sumber A (kode): import.ts:327-335; Sumber B (dokumen): CALCULATION_INTEGRITY_PROTOCOL.md tabel baris valueDone – 'beku saat lapor'; Sumber C (tes): tests/integration/adendum-sesuaikan-realisasi.test.ts belum dibaca untuk memastikan harga yang diuji.

**Bukti.** import.ts:327-335 const harga = Number(n.unitPrice ?? 0); for (const [i, r] of rows.entries()) { if (sesudah[i] === sebelum[i]) continue; await tx.dailyReportItem.update({ where: { id: r.id }, data: { volumeDone: sesudah[i]!, valueDone: hitungNilai(sesudah[i]!, harga) }, CALCULATION_INTEGRITY_PROTOCOL.md:63 | `valueDone` | `round(volume × hargaSatuan)` | `money.ts` | per item laporan | **beku saat lapor** |

**Tes terkait.** tests/integration/adendum-sesuaikan-realisasi.test.ts (tidak dibaca utuh; perlu dicek apakah menguji kasus harga berubah).

**Usul perbaikan.** Putuskan bersama user: pakai harga yang tersimpan di baris (valueDone/volumeDone lama) atau harga revisi baru untuk SEMUA baris item itu; tulis keputusannya di arsip DECISIONS dan protokol, lalu tes.

**Reproduksi.** MERAH (direproduksi). Uji integrasi sementara tests/integration/_audit-rab-parse-35.test.ts (sudah dihapus; git status bersih): revisi 1 aktif dengan item A (I#1, vol 45,7) dan B (I#2, vol 10), keduanya harga 100.000; dua laporan `disetujui` (A 20+25,7; B 4+6) dengan valueDone = vol × 100.000. Draft revisi 2: A vol 30 + harga 150.000, B vol 10 + harga 150.000. Setelah activateRevision(): [ {lk:"I#1", vol:13.129, valueDone:1969350, hargaImplisit:150000}, {lk:"I#1", vol:16.871, valueDone:2530650, hargaImplisit:150000}, {lk:"I#2", vol:4, valueDone:400000, hargaImplisit:100000}, {lk:"I#2", vol:6, 

---

## E. Adendum – inti

### E-1. 🟠 Impor 'Jadikan RAB AKTIF' saat draft sudah ada melahirkan DRAFT KEDUA secara deterministik; halaman adendum lalu memilih draft sembarang dan laporan basis draft lama tidak pernah naik

`src/app/(app)/lokasi/[slug]/rab/import/actions.ts:730` · tinggi · alur · `adendum-inti-36`

**Skenario.** Lokasi punya RAB aktif #1 dan draft adendum #2 (dibuat dari editor; mandor sudah melapor item baru lewat basis draft_adendum terhadap node #2). User membuka tab Impor, menekan 'Buka kunci Jadikan RAB AKTIF sekarang' (import-form.tsx:186-197), mengunggah berkas adendum, konfirmasi. `importHps` mode aktifkan langsung `createRevisionFromNodes` (baris 730) → revisi #3 berstatus draft, lalu `pastikanBolehAktivasi` melempar PersetujuanError dan aksi mengembalikan SUKSES 'tersimpan sebagai DRAFT' (746-757). Tidak ada pemeriksaan draft yang sudah ada (berbeda dengan mode draft baris 702 dan `createAdendumDraft` yang menolak). Kini dua draft hidup: adendum/page.tsx:41-60 `findMany` tanpa orderBy lalu `find(status==='draft')` → yang tampil/ditandatangani/diaktifkan bergantung urutan Postgres; `getLeafNodeOptions` (daily-report/queries.ts:66) `findFirst` draft juga sembarang. Bila #3 yang diaktifkan, `activateRevision` hanya menaikkan basis untuk `rabNode.revisionId = #3` (import.ts:201-204), sehingga laporan draft_adendum terhadap node #2 TIDAK pernah menjadi resmi walau lineage-nya ada di #3, dan #2 tidak bisa dibuang (FK RESTRICT, sudah tercatat di OPEN_ISSUES).

**Bukti.** import/actions.ts:730-757: const res = await createRevisionFromNodes(location.id, nodes, { source, note, userId: user.id }); try { await pastikanBolehAktivasi(res.revisionId); } catch (e) { if (!(e instanceof PersetujuanError)) throw e; return { success: `Revisi #${res.revisionNo} tersimpan sebagai DRAFT ...` }; } adendum/page.tsx:59-60: const draft = revisions.find((r) => r.status === "draft") ?? null;

**Tes terkait.** tests/integration/adendum-empat-mata.test.ts (jalur impor aktifkan tidak diuji); tidak ada tes yang menegakkan 'satu draft per lokasi' pada jalur impor

**Usul perbaikan.** Di mode aktifkan, bila lokasi sudah punya draft: tolak dengan pesan yang menyebut nomor draftnya (atau minta user membuang/mengganti draft lewat mode draft), sebelum `createRevisionFromNodes`. Tambahkan partial unique index (locationId) WHERE status='draft' di DB supaya invariannya bukan sekadar aturan satu layar. Pada `activateRevision`, naikkan basis juga untuk baris draft_adendum yang lineageKey-nya ada di revisi yang diaktifkan (bukan hanya yang rabNode-nya milik revisi itu), atau tolak aktivasi bila ada draft lain yang punya laporan.

**Reproduksi.** Berkas uji SEMENTARA `tests/integration/_audit-adendum-inti-36.test.ts` (sudah DIHAPUS; `git status` bersih dari berkas saya – `_audit-adendum-inti-38.test.ts` yang tersisa milik pemeriksa lain, tidak disentuh). PostgreSQL 16 lokal dinyalakan (`pg_ctlcluster 16 main start`), DB `marlin_test` sudah 97 migrasi, `prisma migrate deploy` → "No pending migrations". Dijalankan: `DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-adendum-inti-36.test.ts`. Skenario: kontrak ber-startDate (→ isAdendum) · RAB aktif #1 (3 item, dibuat v

### E-2. 🟠 Aktivasi menulis ulang laporan harian (termasuk final) menjadi 0 untuk item yang dinolkan di draft, tanpa satu pun kalimat di halaman adendum/konfirmasi yang mengatakannya

`src/lib/rab/import.ts:319` · tinggi · konflik-spesifikasi · `adendum-inti-39`

**Skenario.** Item 'Galian Tanah' aktif 45,7 m³ sudah dilaporkan penuh (3 laporan disetujui/final). User mengisi template adendum dengan VOLUME ADENDUM = 0 (tanpa pemetaan manual ke baris pengganti) – pratinjau hanya bilang 'volumenya dijadikan 0 … TETAP tercantum di RAB dengan nilai nol' dan menandai 'di bawah realisasi'. Draft ditandatangani dua orang, tombol Aktifkan: dialog konfirmasi (draft-controls.tsx:130-132) hanya menyebut 'realisasi tersambung otomatis via lineage'. `sesuaikanRealisasiKeVolumeBaru` memanggil `sesuaikanProporsional([20,15.7,10], 0)` → `[0,0,0]` (dibuktikan skrip scratchpad/nol.ts), lalu menulis volumeDone=0 dan valueDone=0 ke tiga baris laporan, termasuk yang FINAL. Foto dan verifikasi Wakil PPK atas baris itu tetap ada, angkanya kini nol. DECISIONS 203 mewajibkan penyesuaian angka user 'dikatakan di UI'; keputusan user 2026-09-03 (komentar import.ts:253) lahir dalam konteks PEMETAAN MANUAL yang dikonfirmasi, sedangkan kode menerapkannya ke SETIAP item termasuk yang dinolkan tanpa pemetaan. Tes adendum-sesuaikan-realisasi hanya menguji 45,7→32,149 dan menyebut 'tidak ada hari yang dinihilkan' – kasus target 0 tidak diuji.

**Bukti.** import.ts:321-324: if (!rows || n.volume == null) continue; const volumeBaru = Number(n.volume); const sebelum = rows.map((r) => Number(r.volumeDone)); const sesudah = sesuaikanProporsional(sebelum, volumeBaru); sesuaikan-realisasi.ts:52-56: if (target < 0) return null; if (total <= target) return null; // target 0 lolos → semua baris 0 draft-controls.tsx:132: "Revisi aktif lama menjadi arsip (jejak tetap ada), kurva-S di-regenerate, dan realisasi tersambung otomatis via lineage."

**Tes terkait.** tests/integration/adendum-sesuaikan-realisasi.test.ts (tidak ada kasus volume baru 0 / tanpa pemetaan); tests/unit tidak ada untuk sesuaikanProporsional target 0

**Usul perbaikan.** Konflik antara DECISIONS 203, kode, dan konteks keputusan 2026-09-03 – tanyakan user. Minimal: (1) halaman adendum + dialog konfirmasi WAJIB mencantumkan daftar item yang realisasinya akan diturunkan (dari→ke, jumlah baris laporan, termasuk final) sebelum Aktifkan; (2) untuk volume baru 0 dengan realisasi > 0 dan tanpa pemetaan, TOLAK aktivasi atau minta pemetaan/penghapusan eksplisit, jangan nolkan diam-diam; (3) tambah tes target 0.

**Reproduksi.** MERAH (integrasi, berkas sementara tests/integration/_audit-adendum-inti-39.test.ts, sudah dihapus). Fixture persis kasus temuan: RAB aktif item I#1 volume 45,7; tiga DailyReportItem basis aktif 20 / 15,7 / 10 (status disetujui, disetujui, FINAL); draft revisi #2 item I#1 volume 0 tanpa pemetaan. Setelah `activateRevision(draftRevId, userId)`: SESUDAH AKTIVASI: [{"tgl":"2026-08-10","status":"disetujui","vol":0,"nilai":"0"},{"tgl":"2026-08-12","status":"disetujui","vol":0,"nilai":"0"},{"tgl":"2026-08-15","status":"final","vol":0,"nilai":"0"}] AUDIT rab.adendum_sesuaikan_realisasi: {"volumeBaru"

### E-3. 🟡 Peringatan 'Δ RAB draft ≠ CCO' mengadu Δ SATU lokasi dengan `valueDelta` CCO yang melekat pada KONTRAK (bisa memayungi banyak lokasi) — selalu berbunyi pada paket multi-lokasi

`src/app/(app)/lokasi/[slug]/rab/adendum/page.tsx:321` · sedang · konflik-spesifikasi · `adendum-inti-42`

**Skenario.** Paket dua lokasi A dan B, satu kontrak, satu CCO-01 dengan valueDelta = ΔA+ΔB (incl-PPN). Draft A dikaitkan ke CCO-01 (createAdendumDraft hanya memeriksa CCO milik paket). page.tsx:322-323 membandingkan `withPpn(diff.delta)` (Δ lokasi A saja) dengan `draft.amendment.valueDelta` (seluruh kontrak) → selalu berbeda → banner 'Samakan dulu (edit draft atau betulkan CCO)' yang tidak bisa dipenuhi. Di sisi lain `cco/route.ts` menerbitkan dokumen CCO PER LOKASI (`CCO-${draft.revisionNo-1}_${slug}.xlsx`) sementara `ContractAmendment` unik per (contractId, ccoNumber) dan batas-adendum.ts menegaskan plafon milik kontrak. Model CCO per-kontrak vs dokumen CCO per-lokasi belum diputuskan konsisten.

**Bukti.** page.tsx:321-324: if (diff && draft.amendment) { const deltaInclPpn = withPpn(diff.delta, ppnPercent); if (deltaInclPpn !== draft.amendment.valueDelta) { peringatan.push(`Δ RAB draft termasuk PPN ... sedangkan CCO ${draft.amendment.ccoNumber} tercatat ...`); schema.prisma:1189: @@unique([contractId, ccoNumber])

**Tes terkait.** Tidak ada tes untuk peringatan CCO

**Usul perbaikan.** Tanyakan user: satu CCO = satu kontrak (Σ semua lokasi) atau per lokasi? Bila per kontrak, bandingkan Σ Δ seluruh revisi yang ber-amendmentId sama (aktif + draft ini) dengan valueDelta, dan sebut rinciannya per lokasi. Bila per lokasi, tambahkan locationId ke ContractAmendment.

**Reproduksi.** MERAH – direproduksi lewat tests/integration/_audit-adendum-inti-42.test.ts (DB test asli, sudah DIHAPUS; git status bersih). Skenario yang dibangun: 1 paket, 1 kontrak (packageId @unique), DUA lokasi A & B masing-masing RAB aktif Rp 1 miliar. Satu CCO-01 dicatat lewat ContractAmendment dengan valueDelta = withPpn(ΔA + ΔB) = withPpn(50jt + 30jt, 11%) = 88.800.000 – yaitu angka yang BENAR menurut formulir "Perubahan nilai (Rp)" di halaman kontrak (kontrak-forms.tsx:510), yang memang menanyakan perubahan nilai KONTRAK. Draft adendum lokasi A dikaitkan ke CCO-01 lewat createAdendumDraft(lokasi.a,

### E-4. 🟡 Layar editor, template, dan CCO memakai realisasi cakupan 'aktif' sedangkan pagar server memakai 'semua' — realisasi basis draft tidak terlihat, tombol hapus & batas bawah volume di grid berbohong

`src/app/(app)/lokasi/[slug]/rab/adendum/page.tsx:123` · sedang · bug · `adendum-inti-43`

**Skenario.** Item baru 'Bollard' hanya ada di draft; mandor sudah melapor 12 unit lewat basis draft_adendum (laporan dikirim). Halaman adendum: `realizedMap = cumulativeVolumeByLineage(location.id)` (default 'aktif') → `realized` 0, tidak ada badge 'realisasi 12', `canDelete` true (ikon hapus aktif), `cellEditorParams.min = 0`. User mengetik volume 5 → server (`updateDraftItemVolume`, cakupan 'semua') menolak 'tidak boleh di bawah realisasi (12)' – angka yang tidak pernah tampil di layar. Klik hapus → ditolak server. Berkas CCO (cco/route.ts:150) dan template (template/route.ts:76) juga membawa `realisasiByLineage` cakupan aktif, sehingga pengaman DECISIONS 242 di dalam Excel tidak tahu pekerjaan yang dilaporkan atas draft.

**Bukti.** page.tsx:123: cumulativeVolumeByLineage(location.id), page.tsx:186-191: realized: realizedMap.get(n.lineageKey) ?? 0, ... canDelete: (subtreeRealized.get(n.id) ?? 0) <= 1e-6, adendum.ts:269-270: const realized = (await cumulativeVolumeByLineage(rev.locationId, undefined, "semua")).get(node.lineageKey) ?? 0;

**Tes terkait.** tests/integration/adendum-editor.test.ts hanya menguji pagar server, bukan angka yang dikirim ke layar

**Usul perbaikan.** Di page.tsx pakai cakupan 'semua' (sama dengan pagar), dan di CCO/template pertimbangkan hal yang sama atau sebutkan di berkas bahwa realisasi basis draft tidak termasuk.

**Reproduksi.** MERAH terbukti lewat uji integrasi sementara `tests/integration/_audit-adendum-inti-43.test.ts` (sudah DIHAPUS; `git status --short` bersih, tidak ada berkas repo yang diubah). Skenario fixture: lokasi dengan revisi #1 aktif (item I#1 "Pancang" vol 100) dan revisi #2 draft berisi I#1 + item draft-only I#9 "Bollard" (vol 50, Rp 1 jt/unit). Laporan harian 2026-09-01 mencatat 12 unit Bollard lewat `upsertItem` atas node DRAFT (`item.basis === "draft_adendum"` terverifikasi), lalu status laporan dinaikkan ke `dikirim` (masuk COUNTED_REPORT_STATUSES). Perintah: DATABASE_URL=postgresql://marlin:marl

### E-5. 🟡 Berkas adendum yang tertahan gerbang empat mata di jalur 'aktifkan' TIDAK diarsipkan ke R2/Document, padahal revisinya tersimpan dan kelak diaktifkan

`src/app/(app)/lokasi/[slug]/rab/import/actions.ts:746` · sedang · data-integritas · `adendum-inti-40`

**Skenario.** Lokasi punya RAB aktif; user membuka kunci 'Jadikan RAB AKTIF sekarang', mengunggah adendum resmi, konfirmasi. `createRevisionFromNodes` menyimpan revisi #N draft (730), `pastikanBolehAktivasi` melempar PersetujuanError, aksi `return { success: 'tersimpan sebagai DRAFT' }` pada 751 – SEBELUM `arsipkanSumber` di 777. Mode draft (709) mengarsipkan berkasnya; jalur ini tidak. Setelah PD+SM menandatangani dan draft diaktifkan, RAB kontrak yang berlaku tidak punya `sourceDocumentId`, berkas sumbernya tidak ada di Dokumen, sha256 tidak tercatat – persis pada revisi yang mengubah nilai kontrak.

**Bukti.** import/actions.ts:746-757: try { await pastikanBolehAktivasi(res.revisionId); } catch (e) { if (!(e instanceof PersetujuanError)) throw e; revalidatePath(`/lokasi/${location.slug}`, "layout"); return { success: `Revisi #${res.revisionNo} tersimpan sebagai DRAFT ...` }; } await activateRevision(res.revisionId, user.id); ... await arsipkanSumber({ ... }); // baris 777, tidak tercapai

**Tes terkait.** Tidak ada tes untuk pengarsipan di jalur impor (R2 dinonaktifkan di uji)

**Usul perbaikan.** Panggil `arsipkanSumber` segera setelah `createRevisionFromNodes` (sebelum gerbang), untuk kedua mode; atau pindahkan pengarsipan ke dalam `createRevisionFromNodes` sebagai langkah best-effort yang selalu dijalankan.

**Reproduksi.** GAGAL (merah, sesuai temuan). Uji integrasi sementara tests/integration/_audit-adendum-inti-40.test.ts (R2 di-mock isR2Configured→true, DB marlin_test, importHps dipanggil apa adanya via FormData dua langkah pratinjau→confirm, kontrak dengan startDate terisi, fixture rab-aktif-situbondo.xlsx). Hasil: `Tests 1 failed | 2 passed (3)`. Kontrol #1 (HPS awal, mode aktifkan) → success /aktif/, sourceDocumentId terisi, document count=1 ✓. Kasus #2 (mode aktifkan dengan RAB aktif) → success "tersimpan sebagai DRAFT", lalu log: `[inti-40] draft #2 sourceDocumentId = null | document count = 1 | r2 keys 

### E-6. 🟡 `kenaikanLokasiLain` menyerap selisih pembulatan PPN dan selisih dasar kontrak-vs-RAB, lalu halaman menuduh 'lokasi lain' memakai plafon pada paket satu lokasi

`src/lib/rab/batas-adendum.ts:174` · sedang · bug · `adendum-inti-37`

**Skenario.** Paket SATU lokasi, kontrak = withPpn(RAB) persis, PPN 11%. Draft menaikkan RAB Rp 4. `nilaiAkhir = withPpn(rab+4)`, `kenaikanDraft = withPpn(4)`, `kenaikanLokasiLain = kenaikanKumulatif − kenaikanDraft` = withPpn(rab+4) − withPpn(rab) − withPpn(4) = +1 (pembulatan half-up dua kali). Skrip terhadap modul asli (scratchpad/batas.ts): 181 dari 600 kombinasi (rab 100.000.000..100.000.099 × 6 delta) menghasilkan `kenaikanLokasiLain ≠ 0` pada paket satu lokasi. Kasus lebih besar: kontrak tercatat Rp 40.000 di bawah withPpn(RAB) (masih dalam toleransi 0,1% `contractMismatch`) → `kenaikanLokasiLain = 40.000`. page.tsx:284-295 lalu menampilkan 'Lokasi lain di paket ini sudah memakai +Rp 40.000 dari plafon adendum' padahal tidak ada lokasi lain.

**Bukti.** batas-adendum.ts:162-175: const nilaiAkhir = withPpn(akhirPraPpn, m.ppnPercent); const kenaikanKumulatif = nilaiAkhir - nilaiAwal; ... kenaikanLokasiLain: kenaikanKumulatif - kenaikanDraft, page.tsx:284: } else if (batasAdendum.dasar === "kontrak" && batasAdendum.kenaikanLokasiLain !== 0n) {

**Tes terkait.** tests/unit/adendum-batas-10persen.test.ts – semua kasus memakai ppnPercent 0 dan kontrak = Σ RAB persis, jadi selisih ini tidak pernah terlihat

**Usul perbaikan.** Hitung `kenaikanLokasiLain` langsung dari data lokasi lain: Σ withPpn(totalPraPpn lokasi≠draft) − (nilaiKontrak − withPpn(aktifLokasiIni)) atau, lebih jujur, pisahkan 'selisih dasar kontrak vs Σ RAB awal' sebagai angka sendiri dan hanya tampilkan banner 'lokasi lain' bila rabAktifPaket memuat lokasi lain DAN kenaikannya berasal dari sana. Tambah tes unit satu lokasi dengan PPN 11% yang menuntut kenaikanLokasiLain = 0.

**Reproduksi.** Berkas uji sementara tests/unit/_audit-adendum-inti-37.test.ts dibuat, dijalankan sendiri, lalu DIHAPUS (git status bersih). Hasil: MERAH dua-duanya. (1) Paket satu lokasi, kontrak = withPpn(rab,11) persis, rab 100.000.000..100.000.099 × delta 1..6: `expected 189 to be 0` – 189 dari 600 kombinasi menghasilkan kenaikanLokasiLain ≠ 0 (temuan menyebut 181; selisih kecil, substansi sama: selisih pembulatan half-up ppnAmount dua kali, nilainya ±1). (2) Paket satu lokasi, rab 5.000.000.000, kontrak = withPpn(rab,11) − 40.000 (contractMismatch → false, dalam toleransi 0,1%), draft +1.000.000: `expect

### E-7. 🟡 `cabutPersetujuan` tidak memeriksa status revisi: tanda tangan atas adendum yang SUDAH AKTIF bisa dihapus lewat approveRevisionAction cabut=1

`src/lib/rab/persetujuan.ts:96` · sedang · data-integritas · `adendum-inti-38`

**Skenario.** PD dan SM menyetujui draft #2; super admin mengaktifkannya (rev #2 kini aktif, RAB kontrak berganti). PD (atau SM) mengirim POST ke `approveRevisionAction` dengan FormData revisionId=#2 dan cabut=1 (tombolnya memang hilang dari UI karena halaman adendum hanya merender draft, tetapi aksi servernya menerima revisi apa pun). `cabutPersetujuan` hanya `deleteMany({revisionId, userId})` tanpa cek `status`, `setujuiRevisi` sebaliknya menolak non-draft (baris 79-81). Baris `rab_revision_approvals` – satu-satunya bukti terstruktur 'siapa menyetujui angka yang mana' (`totalValue` disnapshot di sana) – hilang untuk RAB yang sedang berlaku; yang tersisa hanya audit log.

**Bukti.** persetujuan.ts:96-100: export async function cabutPersetujuan(revisionId: string, user: { id: string; role: UserRole }) { const hapus = await db.rabRevisionApproval.deleteMany({ where: { revisionId, userId: user.id } }); if (hapus.count === 0) throw new PersetujuanError("Anda belum menyetujui revisi ini."); rab/actions.ts:810: if (cabut) { await cabutPersetujuan(parsed.data, user); }

**Tes terkait.** tests/integration/adendum-empat-mata.test.ts blok 'cabut persetujuan' hanya menguji draft

**Usul perbaikan.** Di `cabutPersetujuan`, baca revisi dan tolak bila `status !== 'draft'` (pesan: 'Revisi sudah aktif – persetujuannya tidak bisa dicabut'). Pertimbangkan menandai baris sebagai dicabut (kolom revokedAt) alih-alih menghapus, supaya jejak tetap terstruktur.

**Reproduksi.** Berkas uji integrasi sementara tests/integration/_audit-adendum-inti-38.test.ts (SUDAH DIHAPUS; `git status --short` kosong). Skenario: revisi #1 `digantikan`, revisi #2 `draft` disetujui PD + SM lewat `setujuiRevisi`, lalu status #2 diubah ke `aktif`; PD memanggil `approveRevisionAction(undefined, FormData{revisionId=#2, cabut=1})` dengan mock sesi seperti adendum-empat-mata.test.ts. Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-adendum-inti-38.test.ts. Hasil: GAGAL (merah, sesuai harapan reproduksi). Keluara

### E-8. 🟡 Gerbang empat mata memakai 'ada RAB aktif' sebagai definisi adendum, bertentangan dengan DECISIONS 118 (sebelum SPMK = koreksi HPS awal, bukan adendum) yang dipakai jalur impor

`src/lib/rab/persetujuan.ts:116` · sedang · konflik-spesifikasi · `adendum-inti-41`

**Skenario.** Paket belum SPMK (`Contract.startDate` null) tetapi HPS awal sudah diimpor dan aktif (#1). Ternyata kolom harga salah; user mengimpor ulang HPS yang benar dengan 'Jadikan RAB AKTIF'. Sesuai DECISIONS 118, import/actions.ts:358-359 menghitung `isAdendum = false` dan `source = 'hps_awal'`. Namun `pastikanBolehAktivasi` (persetujuan.ts:116-121) menghitung `count(status aktif) > 0` → menuntut PD + AM/PM/SM dengan pesan 'Aktivasi adendum butuh persetujuan DUA orang…' untuk revisi yang oleh sistem sendiri diberi label 'HPS awal'. Halaman adendum juga menampilkan draft ber-source hps_awal sebagai 'Draft adendum'. Tiga sumber berbeda: DECISIONS 118 (koreksi HPS bukan adendum), DECISIONS 234/persetujuan.ts ('belum ada RAB aktif = HPS awal'), dan tes adendum-empat-mata 'HPS awal bukan adendum' yang hanya menguji kasus tanpa revisi aktif.

**Bukti.** persetujuan.ts:116-121: const adaAktif = await db.rabRevision.count({ where: { locationId: rev.locationId, status: "aktif" } }); // Belum ada RAB aktif = ini HPS awal, bukan adendum. if (adaAktif === 0) return; import/actions.ts:358-359: const contractStarted = location.package?.contract?.startDate != null; const isAdendum = activeRevision !== null && contractStarted;

**Tes terkait.** tests/integration/adendum-empat-mata.test.ts 'HPS awal bukan adendum' (hanya kasus adaAktif=0)

**Usul perbaikan.** Tanyakan user apakah koreksi HPS pra-SPMK juga wajib empat mata. Bila tidak: gerbang memakai definisi yang sama dengan DECISIONS 118 (ada aktif DAN kontrak sudah SPMK, atau `rev.source === 'adendum'`), dan pesannya menyebut 'koreksi HPS' bila memang bukan adendum. Bila ya: perbarui DECISIONS 118 dan label UI supaya tidak menyebut 'adendum' untuk revisi hps_awal.

**Reproduksi.** MERAH – terbukti, bukan dugaan. Uji sementara `tests/integration/_audit-adendum-inti-41.test.ts` (sudah DIHAPUS; Postgres lokal saya hidupkan dengan `pg_ctlcluster 16 main start`, DB `marlin_test`). Skenario: Contract dibuat dengan `startDate: null` (belum SPMK), lokasi punya RabRevision #1 `status=aktif, source=hps_awal`, lalu draft #2 `source=hps_awal`. Perintah: `DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-adendum-inti-41.test.ts --reporter=verbose --disable-console-intercept` Keluaran: ``` PESAN GERBANG: Aktivasi 

---

## F. Adendum – templat & CCO KKP

### F-1. 🟠 Template adendum terbitan MARLIN hanya dikenali pada mode 'draft'; pada mode 'Jadikan RAB AKTIF' ia dibaca sebagai HPS biasa dan nilainya terbaca DUA KALI LIPAT tanpa satu peringatan pun

`src/app/(app)/lokasi/[slug]/rab/import/actions.ts:228` · tinggi · alur · `adendum-template-47`

**Skenario.** Form impor menawarkan mode 'aktifkan' untuk 'adendum yang SUDAH resmi' (import-form.tsx:147). User yang adendumnya sudah ditandatangani dan berkasnya berupa template MARLIN memilih mode itu. Karena deteksi template dipagari `if (mode === "draft")`, berkas jatuh ke `parseHpsBuffer`. Dibuktikan skrip pada fixture template-adendum-situbondo-asli.xlsx: jalur HPS menghasilkan priceColumn 'HPS (kolom E/F)', 899 item, grandTotal 7.447.090.474 – padahal parser template membaca 3.723.269.227 (sisi aktif 3.723.269.226); `warnings` = [] (baris 'NILAI KONTRAK BERJALAN' tidak cocok regex TOTAL_POLOS sehingga cek-silang total tidak menyala). Pratinjau yang tersusun sesudah samakanLineage: '21 item baru · 25 hilang · 878 tetap · Rp 3,72 M -> Rp 7,45 M'. Pemeriksaan template basi (baris 337) pun tidak berjalan karena `templateAdendum` null. Gerbang empat mata memang menahan aktivasinya, tetapi revisi draft bernilai 2x tetap ditulis dan pratinjaunya menyesatkan.

**Bukti.** actions.ts:207-209, 228 // Hanya dicoba pada mode draft: template adendum memang cuma dipakai di // sana, dan membuka workbook dua kali untuk berkas HPS 4-5 MB itu mahal. const mode: ImportMode = formData.get("mode") === "draft" ? "draft" : "aktifkan"; ... if (mode === "draft") { const mungkinTemplate = (await namaSheetXlsx(buffer)).some((x) => x.nama === ADENDUM_TEMPLATE_SHEET && !x.tersembunyi);

**Tes terkait.** tests/integration/impor-kkp-tambah-kurang.test.ts dan tests/unit/rab-add-01-template-bolak-balik.test.ts hanya menguji mode draft / parser langsung; tidak ada tes template pada mode aktifkan

**Usul perbaikan.** Alasan biaya sudah tidak berlaku: `namaSheetXlsx` murah dan sudah dipakai sebagai gerbang. Jalankan deteksi template di kedua mode; bila template terdeteksi pada mode 'aktifkan', pakai parser template (plus cek revisi basi) atau tolak dengan pesan 'template adendum hanya bisa diimpor ke draft'. Tes merah-dulu: importHps mode=aktifkan dengan template -> priceColumnLabel harus 'TEMPLATE ADENDUM' dan total sama dengan parser template.

**Reproduksi.** MERAH. Berkas uji sementara tests/unit/_audit-adendum-template-47.test.ts (sudah DIHAPUS; `git status` bersih kecuali berkas _audit-...-48 milik sesi lain) dijalankan dengan `pnpm vitest run tests/unit/_audit-adendum-template-47.test.ts --reporter=verbose --silent=false` atas fixture terbitan MARLIN sendiri `tests/fixtures/template-adendum-situbondo-asli.xlsx`: isAdendumTemplate: true TEMPLATE total: 3723269226 item: 903 HPS priceColumn: {"source":"hps","label":"HPS (kolom E/F)"} HPS total: 7447090474 item: 899 HPS warnings: [] bedaAntarLayer: [] Jadi berkas yang `isAdendumTemplate()` akui seb

### F-2. 🟠 Baris induk yang dibuang (HAPUS / volume negatif / baris judul dihapus user) meninggalkan anak yatim: uangnya lenyap dari total kategori tanpa peringatan, lalu commit meledak SESUDAH draft lama dihapus

`src/lib/rab/adendum-template-parse.ts:242` · tinggi · data-integritas · `adendum-template-45`

**Skenario.** Tiga jalan masuk yang sama-sama nyata: (a) user menulis HAPUS pada item yang punya baris anak (item-di-bawah-item memang ada di DB, DECISIONS 563 dan tes RAB-ADD-02); (b) user mengetik volume negatif pada item berinduk; (c) pada template format baru (kolom M), user menghapus baris judul sub/grup yang dianggap hiasan tetapi membiarkan item-itemnya. Di ketiga kasus parser membuang baris induk (`continue`) tetapi anak-anaknya tetap masuk `nodes` dengan `parentLineageKey` menunjuk kunci yang sudah tidak ada. `hitung()` (baris 382-388) hanya berjalan dari akar, jadi rupiah anak tidak pernah sampai ke kategori. Dibuktikan skrip: HAPUS pada I#1 yang punya anak I#1#2 -> Σkategori 0, Σitem 1.000.000; hapus baris sub I#I.1 -> pratinjau '0 item hilang · 3 tetap' tetapi totalBaru 1.000.000 dari totalAktif 3.000.000 (Rp 2 juta hilang tanpa satu kalimat). Lalu saat 'Simpan ke draft adendum': actions.ts:702 `discardDraft(draft.id)` dijalankan DULU, baru `createRevisionFromNodes`; di import.ts:116-123 batch tanpa induk -> revisi baru dihapus dan `throw new Error('Struktur RAB tidak konsisten (orphan node)')`. Draft adendum yang sudah ada sebelumnya sudah terlanjur dibuang.

**Bukti.** adendum-template-parse.ts:237-243 if (/\bHAPUS\b/.test(keterangan)) { dihapus.push({ lineageKey, code: kode, name: nama }); continue; } (anak tetap didorong di baris 282-293 dengan parentLineageKey: induk) actions.ts:702-703 if (draft) await discardDraft(draft.id, user.id); const resDraft = await createRevisionFromNodes(location.id, nodes, {

**Tes terkait.** tests/unit/adendum-template.test.ts (kasus HAPUS hanya pada item daun); tests/unit/rab-add-02-induk-template.test.ts (membuktikan item-di-bawah-item ada, tapi tidak menguji HAPUS/negatif pada induk); tidak ada tes yang memeriksa invarian 'setiap parentLineageKey ada di nodes'

**Usul perbaikan.** Di akhir `parseAdendumTemplate`, periksa invarian: setiap `parentLineageKey` non-null harus ada di `nodes`. Kalau ada yatim, lempar `AdendumTemplateError` yang menyebut baris & induknya (mis. 'Baris 12 "Pembesian balok" induknya "Bekesting balok" di-HAPUS/berubah jadi negatif/dihapus – HAPUS induknya sekalian atau kembalikan barisnya'). Di actions.ts, pindahkan `discardDraft` ke SESUDAH `createRevisionFromNodes` berhasil (atau bungkus keduanya dalam satu transaksi) supaya draft lama tidak hilang ketika penyimpanan gagal. Tes merah-dulu: HAPUS pada item berinduk -> harus throw; hapus baris sub di template kolom-M -> harus throw.

**Reproduksi.** MERAH, ketiga skenario. Berkas sementara `tests/unit/_audit-adendum-template-45.test.ts` dan `tests/integration/_audit-adendum-template-45.test.ts` sudah DIHAPUS; `git status --porcelain` bersih (berkas `_audit-adendum-template-47.test.ts` milik agen lain, tidak saya sentuh). Baris uji DB di marlin_test (org `org-aumu2faxcz`) sudah dibersihkan. A. UNIT – `pnpm vitest run tests/unit/_audit-adendum-template-45.test.ts` Template dibangun lewat `buildAdendumTemplateXlsx` yang asli (jadi kolom M induk terisi seperti berkas terbitan MARLIN). Test Files 1 failed (1) · Tests 3 failed (3) (a) HAPUS pad

### F-3. 🟠 Dua item baru berkode sama di bawah induk yang sama menghasilkan lineageKey kembar: pratinjau diam, commit gagal di constraint unik SESUDAH draft lama dihapus dan meninggalkan revisi setengah jadi

`src/lib/rab/adendum-template-parse.ts:335` · tinggi · data-integritas · `adendum-template-46`

**Skenario.** User menyisipkan dua baris item baru di kategori yang sama dan memberi kode yang sama (lazim: '-' sebagai bullet, atau keduanya '1'). Parser membentuk `key = `${induk}#+${kode}`` tanpa pengecekan kembar (pengaman `barisPerLineage` di baris 187 hanya berlaku untuk baris BER-lineageKey). Dibuktikan skrip: dua baris berkode '-' -> nodes 'I#+- Urugan' dan 'I#+- Pasangan', `new Set(keys).size === keys.length` false; `itemBaru` di pratinjau memuat keduanya tanpa peringatan. Saat commit: `createRevisionFromNodes` (import.ts:100 membuat baris revisi dengan totalValue penuh; batch akar tersimpan; batch berikutnya `createManyAndReturn` melanggar `@@unique([revisionId, lineageKey])` schema.prisma:1440) -> Prisma P2002 dilempar, tidak ada pembersihan, sehingga tersisa revisi draft berisi node akar saja dengan totalValue seolah lengkap; draft adendum sebelumnya sudah dibuang lebih dulu (actions.ts:702). Pesan yang sampai ke user adalah pesan mentah Prisma.

**Bukti.** adendum-template-parse.ts:331-335 const urut = (barisBaruPerInduk.get(induk) ?? 0) + 1; barisBaruPerInduk.set(induk, urut); // lineageKey item baru dibuat dari kodenya; ditandai "+" supaya tidak // pernah bentrok dengan lineage kontrak lama yang sudah punya realisasi. const key = `${induk}#+${kode || `baru${urut}`}`;

**Tes terkait.** tests/unit/adendum-template-item-baru.test.ts (hanya satu item baru); tests/unit/adendum-template.test.ts

**Usul perbaikan.** Jadikan kunci item baru unik per induk (mis. `${induk}#+${kode}` lalu sufiks `#2`, `#3` bila sudah dipakai, seperti `dedup` di flatten.ts) ATAU tolak dengan menyebut kedua nomor barisnya. Tambahkan asersi di akhir parser bahwa semua lineageKey unik. Di sisi commit, bungkus pembuatan revisi dalam transaksi supaya kegagalan di tengah tidak meninggalkan revisi setengah jadi.

**Reproduksi.** MERAH. Berkas sementara `tests/integration/_audit-adendum-template-46.test.ts` (sudah DIHAPUS; `git status` bersih dari jejak saya – yang tersisa `_audit-adendum-template-45.test.ts` milik agen lain). Jalan: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-adendum-template-46.test.ts Isi uji: revisi aktif sungguhan (kategori "I" + item "I#1") -> `buildAdendumTemplateXlsx` -> sisipkan DUA baris baru di kategori I, keduanya Kode "-" (Urugan pasir 10 m3, Pasangan batu 4 m3, harga 200.000) -> `parseAdendumTemplate` -> `banding

### F-4. 🟡 Harga satuan item lama diubah tanpa mengubah volume: unitPrice baru disimpan tetapi amount tetap Jumlah Kontrak lama – tiga dokumen menyebut tiga angka berbeda

`src/lib/rab/adendum-template-parse.ts:276` · sedang · data-integritas · `adendum-template-48`

**Skenario.** Kolom E (Harga Satuan) sengaja terbuka (KOLOM_TERBUKA) karena baris sisipan mewarisi kuncinya. User mengubah E pada item kontrak (10 m3 × 100.000 -> 150.000) tanpa menyentuh G. Parser: `tetap` true (vol == volKontrak) -> `amount = jumlahKontrak` = 1.000.000, tetapi `unitPrice: harga` = 150.000 (baris 288). Dibuktikan skrip: node {unitPrice 150000, volume 10, amount 1000000}; pratinjau memperingatkan 'harga berubah, dampak Rp 500.000' namun totalBaru == totalAktif (1.000.000) – peringatan dan angka total saling membantah. Di Excel yang dipegang user, kolom H = ROUND(G*E) menunjukkan 1.500.000. Kalau draft ini dijadikan dokumen CCO, cco-xlsx.ts:446-452 menulis `jumlahBaru: ${hargaBaru}*${vol}` = 1.500.000 (lihat pola yang dikunci tests/unit/cco-harga-beda-sisi.test.ts). Jadi: berkas user 1,5 jt, DB 1,0 jt, dokumen CCO 1,5 jt – angka resmi yang tersimpan tidak konsisten dengan harga satuannya sendiri, dan itu bukan 'apa adanya' (DECISIONS 203/212) melainkan setengah-setengah.

**Bukti.** adendum-template-parse.ts:276-280, 288 const tetap = volKontrak != null && Math.abs(vol - volKontrak) < EPS; const amount = tetap && jumlahKontrak != null ? BigInt(Math.round(jumlahKontrak)) : BigInt(Math.round(vol * (harga ?? 0))); ... unitPrice: harga,

**Tes terkait.** tests/unit/adendum-template.test.ts 'memakai Jumlah Kontrak apa adanya' (hanya kasus harga tidak berubah); tidak ada tes harga berubah pada template

**Usul perbaikan.** Syarat `tetap` harus mencakup harga: pakai Jumlah Kontrak apa adanya HANYA bila volume DAN harga satuan sama dengan kontrak; bila harga berubah, hitung `vol × harga` (sama dengan rumus H di berkas) sehingga DB = berkas, dan biarkan peringatan `hargaBerubah` yang sudah ada tetap menyala. Alternatif yang lebih ketat sesuai DECISIONS 213: tolak perubahan harga item lama di template dengan menyebut barisnya. Tes merah-dulu: ubah E saja -> amount harus 1.500.000 (atau throw).

**Reproduksi.** GAGAL (merah) – temuan tereproduksi persis. Berkas sementara tests/unit/_audit-adendum-template-48.test.ts (sudah DIHAPUS; `git status --porcelain` kosong). Isi: terbitkan template lewat buildAdendumTemplateXlsx untuk satu item kontrak (V#2 "Beton K-250", volume 10 m3, unitPrice 100.000, amount 1.000.000n), lalu ubah HANYA sel kolom E (Harga Satuan) menjadi 150.000 – kolom G (Volume Adendum) tidak disentuh – lalu parseAdendumTemplate + bandingkanTerhadapAktif. Keluaran `pnpm vitest run tests/unit/_audit-adendum-template-48.test.ts`: PARSE → { unitPrice: 150000, volume: 10, amount: 1000000n } k

### F-5. 🟡 Template format lama (tanpa kolom M – semua fixture di repo begini) yang kehilangan indentasi terbaca sebagai pohon rata: 194 'kategori', total Rp 0, pratinjau '903 tetap · 0 hilang', dan commit BERHASIL

`src/lib/rab/adendum-template-parse.ts:162` · sedang · data-integritas · `adendum-template-50`

**Skenario.** Kelima fixture template di tests/fixtures ber-kolom M/N kosong (format sebelum DECISIONS 563), jadi induknya sepenuhnya dari indentasi kolom B. Berkas beredar lewat WhatsApp dan diedit di aplikasi yang tidak menyimpan indentasi (indentasi bukan fitur universal spreadsheet ponsel/web). Dibuktikan skrip dengan menolkan indent pada template-adendum-situbondo-asli.xlsx: `kedalaman` 0 untuk semua baris -> tumpukan selalu kosong -> setiap baris judul jadi `kategori` (baris 214-216), setiap item `parentLineageKey` null; hasil: {kategori 194, item 903}, 1097 akar, Σkategori 0, Σitem 3.723.269.226; pratinjau: '0 baru · 0 hilang · 903 tetap · Rp 3.723.269.226 -> Rp 0'. Semua node akar berarti tidak ada yatim, jadi `createRevisionFromNodes` sukses menulis revisi bernilai 0 dengan pohon rata. Tidak ada pagar 'kolom induk kosong DAN tak satu pun indentasi padahal ada baris judul'.

**Bukti.** adendum-template-parse.ts:162-167 const indentSel = ws.getCell(r, C_URAIAN).alignment?.indent; const kedalaman = typeof indentSel === "number" ? indentSel : 0; while (tumpukan.length > 0 && tumpukan[tumpukan.length - 1]!.depth >= kedalaman) tumpukan.pop(); const indukBaris = (): string | null => indukTertulis || (tumpukan.length > 0 ? tumpukan[tumpukan.length - 1]!.key : null);

**Tes terkait.** tests/unit/rab-add-02-induk-template.test.ts (indentasi utuh); tidak ada tes indentasi hilang

**Usul perbaikan.** Tambahkan pagar: bila kolom M kosong di seluruh tabel DAN tidak ada satu pun sel B ber-indent > 0 padahal ada baris judul ber-lineageKey (yang mengandung '#'), tolak dengan pesan 'struktur induk tidak terbaca – unduh template baru'. Pagar kedua yang murah dan umum: tolak hasil parse yang Σkategori ≠ Σitem (invarian yang sudah dipakai tes RAB-ADD-01) – ini juga menangkap temuan #1.

**Reproduksi.** MERAH (terbukti). Berkas uji sementara tests/unit/_audit-adendum-template-50.test.ts dibuat, dijalankan sendirian dengan `pnpm vitest run tests/unit/_audit-adendum-template-50.test.ts --silent=false --reporter=verbose`, lalu DIHAPUS (git status bersih dari berkas saya; yang tersisa `_audit-adendum-template-49.test.ts` milik pemeriksa lain, tidak saya sentuh). Keluaran: template-adendum-situbondo-asli.xlsx: template=true baris=1098 kolomM=0 kolomN=0 indent>0=1081 template-adendum-situbondo.xlsx: template=true baris=1098 kolomM=0 kolomN=0 indent>0=1081 3c42cc97-template-adendum-…-rev1_1.xlsx: te

### F-6. 🟡 Item baru ditempelkan ke baris JUDUL terakhir, bukan ke induk baris tempat ia disisipkan: sisipan sesudah item langsung sub yang datang setelah sebuah grup masuk ke grup itu

`src/lib/rab/adendum-template-parse.ts:320` · sedang · bug · `adendum-template-51`

**Skenario.** Struktur lazim KKP: sub I.1 -> grup '2 Pekerjaan Pondasi' (anak a, b) -> item langsung '3 Sloof' di bawah sub. User menyisipkan baris kosong tepat setelah '3 Sloof' (indent sama dengan Sloof) dan mengisi '4 Kolom praktis'. `indukBerjalan` hanya diperbarui pada baris non-item (baris 228), jadi nilainya masih grup 'I#I.1#2'. Dibuktikan skrip: item baru mendapat parentLineageKey 'I#I.1#2' dan lineageKey 'I#I.1#2#+4', padahal secara dokumen ia saudara Sloof di bawah I#I.1. Pratinjau hanya menyebut 'kategori: PERSIAPAN', jadi salah induk ini tidak terlihat; ia tersimpan ke pohon RAB dan terbawa ke blanko KKP, ekspor RAB, dan dokumen CCO. Padahal parser sudah memegang jawaban yang benar lewat `tumpukan`/`indukBaris()` (induk = baris terakhir yang indent-nya lebih kecil), tetapi tidak memakainya untuk baris tanpa lineageKey.

**Bukti.** adendum-template-parse.ts:320, 228 const induk = indukBerjalan ?? kategoriBerjalan?.lineageKey ?? null; ... indukBerjalan = lineageKey; // hanya di cabang !isItem

**Tes terkait.** tests/unit/adendum-template.test.ts 'item baru menempel ke kategori' (hanya kasus kategori tanpa sub/grup)

**Usul perbaikan.** Untuk baris tanpa lineageKey, tentukan induk dari `tumpukan` dengan kedalaman baris sisipan: induk = elemen tumpukan terakhir yang depth < kedalaman baris (baris sisipan mewarisi format/indent tetangganya). Bila indent tidak ada (0) jatuh ke `indukBerjalan` seperti sekarang. Sebutkan induk penuh (jalur) di daftar `itemBaru` pratinjau, bukan hanya nama kategori. Tes merah-dulu persis skenario di atas.

**Reproduksi.** MERAH (gagal seperti yang diklaim). Berkas sementara `tests/unit/_audit-adendum-template-51.test.ts` dibuat, dijalankan, lalu DIHAPUS (`git status --porcelain` kosong sesudahnya). Susunan: kategori `I` PERSIAPAN -> sub `I#I.1` Struktur -> grup `I#I.1#2` Pekerjaan Pondasi (anak `#a`, `#b`) -> item langsung sub `I#I.1#3` Sloof. Template diterbitkan `buildAdendumTemplateXlsx`, lalu satu baris kosong disisipkan tepat di bawah "Sloof" dengan indent yang sama (meniru Insert di Excel yang mewarisi format tetangga), diisi kode "4" / "Kolom praktis" / m3 / 500.000 / vol 3. $ pnpm vitest run tests/unit/

### F-7. 🟡 Tes Tambakagung mengunci bacaan yang Rp 91 juta (4,0%) di atas total yang ditulis berkas itu sendiri, dengan pembanding buatan dan diagnosis 'baris tersembunyi' yang keliru

`tests/unit/cco-tata-letak-kkp.test.ts:102` · sedang · tes · `adendum-template-49`

**Skenario.** Tes membandingkan Σ item parser dengan `kolomBerkas = 2_367_925_554n` (toleransi 1%) dan komentarnya menolak total berkas (2.289.265.441) dengan alasan 'SUM yang cakupannya berbeda … 708 baris tersembunyi'. Diperiksa langsung pada fixture: sel total berkas r1351 = Y1348+Y1344+…+Y71 = 2.289.265.441,06 dan Σ baris TERLIHAT di dalam seluruh rentang SUM itu = 2.289.265.441,06 persis (baris hidden BUKAN penyebabnya). Parser membaca 2.380.369.748; selisih +91.104.307 seluruhnya berasal dari 49 baris TERLIHAT r962–r1021 (VIII.1 'Pekerjaan Struktural') yang berada di luar semua SUM subtotal, dan kolom Y-nya berisi ANGKA KETIK yang sama dengan harga satuan, bukan rumus W×L seperti baris yang dihitung berkas (r962: vol 20, harga 174.862,27, Y 174.862,27; bandingkan r1022: Y = {W1022*L1022}). hps-parser memang menyalakan peringatan 4% tetapi teksnya mendiagnosis 'Ada baris yang terhitung dua kali' – juga salah. Tes ini (dan tests/integration/impor-kkp-tambah-kurang.test.ts yang menuntut totalBaru == totalAktif dari bacaan yang sama) membuat bacaan yang tidak sama dengan berkas terlihat 'hijau' – persis blind spot 'tes yang justru menjaga cacatnya' (CARA_KERJA_AGEN §5). Angka pembanding 2.367.925.554 tidak sama dengan total berkas maupun bacaan parser dan tidak dijelaskan asalnya.

**Bukti.** cco-tata-letak-kkp.test.ts:92-107 * Pembandingnya adalah kolom JUMLAH berkas itu sendiri pada baris yang * MARLIN baca (baris terlihat): Rp 2.367.925.554. Bukan angka di sel * totalnya (Rp 2.289.265.441) – sel itu hasil SUM yang cakupannya berbeda, * dan berkas ini memang memuat 708 baris tersembunyi yang sengaja tidak * dihitung ... const kolomBerkas = 2_367_925_554n; ...toBeLessThanOrEqual(Number(kolomBerkas) * 0.01);

**Tes terkait.** tests/unit/cco-tata-letak-kkp.test.ts:86-108; tests/integration/impor-kkp-tambah-kurang.test.ts:162-168

**Usul perbaikan.** Ganti pembanding dengan total yang DITULIS berkas (2.289.265.441) dan tuliskan selisih 91.104.307 sebagai fakta yang harus dijelaskan, bukan ditoleransi. Untuk parsernya: pada berkas CCO, baris yang gagal cek-silang `vol × harga ≈ jumlah` DAN tidak tercakup rumus subtotal berkas layak dilaporkan per baris (nomor baris + selnya), bukan hanya persentase, dan teks 'terhitung dua kali' diganti diagnosis yang benar. Minimal: perbaiki komentar tes supaya tidak menyesatkan pembaca berikutnya tentang peran baris hidden.

**Reproduksi.** MERAH, dan tiap klaim angkanya cocok sampai rupiah. (1) Total yang DITULIS berkas (skrip scratchpad, exceljs atas fixture asli): M1351 = {M1348+M1344+...+M71} -> 2289265441.0010448 Y1351 = {Y1348+Y1344+...+Y71} -> 2289265441.062912 Z1351 -> 100.00000000270248 (bobot berkasnya sendiri tuntas 100% atas 20 subtotal itu) (2) Peran baris tersembunyi = NOL, bukan penyebab selisih. Ekspansi ke-20 subtotal: covered rows: 508 | numeric visible: 418 | hidden: 0 sum all covered: 2289265441.06 | visible: 2289265441.06 | hidden: 0.00 Sheet memang punya 725 baris hidden (576 bernilai, 331.914.887) – tapi TI

---

## G. Baseline, kurva-S, rencana

### G-1. 🔴 Jadwal impor 'apa adanya' dengan satu kategori tanpa jadwal (yang DECISIONS 203 izinkan) dianggap tidak usable → editor, KKP, dan rincian jadwal jatuh ke jadwal otomatis, sementara kurva resmi tetap dari impor

`src/lib/baseline.ts:309` · kritis · data-integritas · `baseline-kurva-55`

**Skenario.** User impor Time Schedule Excel mode apaadanya; satu kategori kecil (mis. 'Lain-lain') tidak ada barisnya. susunJadwalApaAdanya (jadwal-verbatim.ts:82-84, 97-98) menyimpan baris itu sebagai weekly semua 0 dan banner menyebut '1 pekerjaan tanpa jadwal di Excel dibiarkan kosong' (dijaga tests/unit/jadwal-apa-adanya.test.ts 'dibiarkan kosong'). BaselinePoint = kurva impor. Tetapi deriveCategorySchedule mensyaratkan SETIAP kategori `w.some(v>0)`, dan periodic-report.ts:636-637 memakai syarat yang sama: → editor jadwal membuka origin 'otomatis' (schedule-editor.tsx:115), rincian-jadwal-data.ts:40 memakai segmen otomatis, dan tabel kategori blanko KKP (kurvaSchedule) dihitung ulang dari scheduleFromItems (auto) padahal baris 'Kumulatif Rencana' memakai planCumOfficial = titik impor. Satu dokumen KKP, dua jadwal rencana yang tidak sepakat (kambuhnya audit B3 yang disebut kkp-sheet.ts:105-111).

**Bukti.** baseline.ts:306-310 `const usable = base.categories.every((c) => { const w = byKey.get(c.lineageKey); return w && w.length === totalWeeks && w.some((v) => v > 0); });`; periodic-report.ts:636-637 `const usableStored = storedSched.length > 0 && storedSched.every((s) => s.weekly.length === totalWeeks && s.weekly.some((v) => v > 0));`; jadwal-verbatim.ts:98 `mentah.push(sum > 0 ? { kat, weekly, sum } : { kat, weekly: kosong(), sum: 0 });`

**Tes terkait.** tests/unit/jadwal-apa-adanya.test.ts menguji hasil impor saja; tidak ada tes yang mengecek bahwa jadwal impor dengan tanpaJadwal>0 masih dibaca kembali oleh deriveCategorySchedule/getPeriodReport sebagai 'tersimpan'.

**Usul perbaikan.** Ubah syarat usable menjadi: semua baris berpanjang totalWeeks DAN minimal SATU baris bernilai >0 (atau simpan penanda eksplisit asal jadwal pada Baseline). Tes MERAH-DULU: impor verbatim dengan satu kategori kosong → deriveCategorySchedule.origin harus 'tersimpan' dan Σ kurvaSchedule KKP == planCumOfficial.

**Reproduksi.** GAGAL (merah) – temuan terkonfirmasi. Uji sementara tests/integration/_audit-baseline-kurva-55.test.ts (sudah DIHAPUS; `git status --porcelain` bersih) menembus jalur nyata: RAB 4 kategori (Persiapan 5%, Struktur 69,5%, Finishing 25%, Lain-lain 0,5%), kontrak 28 hari = 4 minggu, Excel jadwal user hanya memuat 3 baris pertama dengan total tepat 100 (10 / 60 / 30). importJadwalAction (mode default apaadanya) DITERIMA: IMPOR: "...3 pekerjaan memakai angka Excel apa adanya. 1 pekerjaan tanpa jadwal di Excel dibiarkan kosong. ..." KURVA RESMI (BaselinePoint): [10, 40, 80, 100] SCHEDULE ITEMS: Persi

### G-2. 🔴 Dua semantik tanggal akhir kontrak: penulis endDate = SPMK + durasi (DECISIONS 054) vs pembaca/tes = SPMK + durasi − 1 (DECISIONS 092) — grid minggu baseline dapat kolom hantu & konversi mode membuang jadwal impor

`src/lib/package/actions.ts:1104` · kritis · konflik-spesifikasi · `baseline-kurva-54`

**Skenario.** KODE penulis: package/actions.ts:1104 & :1473 endDate = start + durationDays. KODE pembaca: periodic-report.ts:396 (mode assume) end = start + (durationDays − 1); progress-calc.weekEndFractions:361 menghitung totalDays = (end−start)+1 (inklusif). DOKUMEN: DECISIONS 054 'endDate = SPMK + durationDays'; DECISIONS 092 'akhir = mulai + durasi − 1'. TES: sampul-harian-mode-minggu.test.ts:44-45 durationDays 60 → endDate start+59. Akibat terverifikasi skrip: (1) senin_minggu, 116 hari dari Kamis 5/3/2026 → hari ke-116 = Minggu 28/6, endDate tersimpan Senin 29/6 → totalWeeksFor = 18 kolom, M18 = '29/6' (sehari SESUDAH kontrak); baseline, blanko KKP, dan target planPct tuntas 100% di M18, bukan M17. (2) tujuh_hari dengan durasi kelipatan 7 (119/140/210 hari): totalWeeksFor = ceil(dur/7) = 17 tetapi weekEndFractions/totalWeeksBetween = 18 → konversiBaselineModeMinggu 'dilewati' (baseline.ts:169) → package/actions.ts:1254 & mode-minggu-default.ts:86 memanggil regenerateBaseline yang MEMBUANG jadwal impor verbatim/manual – persis yang dilarang DECISIONS 427d.

**Bukti.** package/actions.ts:1104 `const endDate = startDate ? new Date(startDate.getTime() + d.durationDays * DAY_MS) : null;`; periodic-report.ts:396 `endDate = new Date(startDate.getTime() + (contract.durationDays - 1) * DAY);`; progress-calc.ts:361 `const totalDays = Math.max(1, Math.round((end.getTime() - start.getTime()) / DAY_MS) + 1);`; skrip: dur=119 tujuh_hari fr(tulis)=18 vs ceil=17; dur=116 senin_minggu kolom tulis=18 vs −1=17, kolom terakhir {start:'2026-06-29',end:'2026-06-29'}.

**Tes terkait.** tests/unit/periode-minggu.test.ts memakai end = SPMK + (hari−1) (asumsi −1) sehingga tidak pernah menyentuh endDate yang benar-benar ditulis aksi SPMK; tidak ada tes yang membaca endDate hasil startExecution/editContract.

**Usul perbaikan.** Putuskan semantik (hari ke-1 = SPMK ⇒ endDate = SPMK + durasi − 1, konsisten dengan getPeriodBounds, weekEndFractions, dan tes), satukan di satu helper, backfill Contract.endDate yang ada, tulis keputusan baru '## (baru)' dan tes yang membaca endDate dari aksi SPMK. Sampai diputuskan: STOP – jangan 'membetulkan' salah satu sisi.

**Reproduksi.** MERAH di dua tingkat. Kedua berkas uji sementara sudah DIHAPUS (`git status --porcelain` bersih). A. tests/unit/_audit-baseline-kurva-54.test.ts (pnpm vitest run <berkas>) – 2 failed / 2: stdout uji-1 (senin_minggu, SPMK Kamis 2026-03-05, durasi 116): { endTulis: '2026-06-29', endBaca: '2026-06-28', nTulis: 18, nBaca: 17, kolomTerakhir: { start: '2026-06-29', end: '2026-06-29' } } AssertionError: expected 18 to be 17 stdout uji-2 (tujuh_hari; ceil(dur/7) yang dipakai totalWeeksFor vs weekEndFractions yang dipakai konversi): { '119': { ceil: 17, fracTulis: 18, fracBaca: 17 }, '120': { ceil: 18,

### G-3. 🟠 Rencana mingguan menghitung rentang minggu dengan aritmetika 7 hari, mengabaikan Contract.weekMode (default senin_minggu)

`src/app/(app)/lokasi/[slug]/rab/actions.ts:621` · tinggi · alur · `baseline-kurva-53`

**Skenario.** Kontrak weekMode=senin_minggu (default skema, schema.prisma:1156), SPMK Kamis 2026-03-05. Lapisan hitung (progress-calc.weekDateRange / getPeriodBounds.currentWeek) menyatakan minggu 2 = Senin 9/3–Minggu 15/3. Tetapi (a) addWeeklyPlanItem & applyWeeklySuggestions menyimpan WeeklyPlan.weekStart/weekEnd = SPMK+(n−1)×7 = 12/3–18/3; (b) getRencanaMingguan (rencana-mingguan.ts:115-116) mencetak periode formulir 12/3–18/3 sambil memakai nomor minggu mode-aware dari getPeriodBounds; (c) PPC minggu lalu (rencana-mingguan.ts:231-234) mengukur realisasi pada rentang 7-hari yang salah; (d) blanko harian tanggal Senin 9/3 (minggu 2 menurut mode) mencari rencana lewat weekStart<=tgl<=weekEnd (daily-report/queries.ts:978) dan menemukan rencana MINGGU 1; jumlah hari & hari ke-n (queries.ts:994-996) ikut salah; (e) tab Rencana rab/page.tsx:173-174 menjumlah realisasi minggu pada rentang yang salah. Diverifikasi skrip: minggu 2 rencana(7hari)=2026-03-12..18 vs kalender=2026-03-09..15; weekOfDate(9/3)=2 tetapi rencana 7-hari yang memuat 9/3 = minggu 1.

**Bukti.** actions.ts:621-622 `const weekStart = new Date(startDate.getTime() + (d.weekNumber - 1) * 7 * DAY_MS); const weekEnd = new Date(weekStart.getTime() + 6 * DAY_MS);` (sama di :721-722); rencana-mingguan.ts:115-116 `const periodeStart = new Date(startDate.getTime() + (minggu - 1) * 7 * DAY); const periodeEnd = new Date(periodeStart.getTime() + 6 * DAY);` – sedangkan PROJECT.md §Periode minggu: "Helper murninya HANYA di progress-calc.ts (weekOfDate, weekDateRange, totalWeeksBetween)".

**Tes terkait.** tests/integration/rencana-mingguan.test.ts memakai SPMK Senin 2026-06-01 sehingga 7-hari == kalender – tes buta terhadap cacat ini. Tidak ada tes rencana mingguan dengan SPMK non-Senin.

**Usul perbaikan.** Ganti ketiga penulis rentang (actions.ts:621/721, rencana-mingguan.ts:115, rab/page.tsx:173) dengan weekDateRange(startDate, n, weekMode, endDate) dari progress-calc; tulis tes integrasi rencana mingguan + blanko harian dengan SPMK Kamis mode senin_minggu (harus MERAH dulu); backfill WeeklyPlan.weekStart/weekEnd yang sudah tersimpan karena dipakai PPC, blanko harian, dan ai-hub/source.ts.

**Reproduksi.** MERAH (3 dari 4 tes gagal). Berkas sementara `tests/integration/_audit-baseline-kurva-53.test.ts` dibuat, dijalankan, lalu DIHAPUS (verifikasi `git status --porcelain` setelahnya: tidak ada jejak berkas saya; satu-satunya entri tersisa `?? tests/unit/_audit-baseline-kurva-54.test.ts` adalah milik pemeriksa lain, tidak saya sentuh). Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-baseline-kurva-53.test.ts Fixture: kontrak SPMK Kamis 2026-03-05, endDate 2026-04-29, `weekMode` DIBIARKAN default skema (= senin_mingg

### G-4. 🟠 Kurva-S blanko KKP: minggu berjalan dihitung tanpa weekMode → pada Senin–Rabu minggu kalender berjalan, realisasi & deviasi kolom minggu itu kosong

`src/lib/periodic-report.ts:682` · tinggi · bug · `baseline-kurva-57`

**Skenario.** Kontrak senin_minggu, SPMK Kamis 5/3/2026. Senin 9/3 user mencetak laporan mingguan ke-2 (getPeriodBounds.currentWeek = 2, mode-aware, periodic-report.ts:412 – jadi minggu 2 boleh diminta). Di getPeriodReport, currentWeek dihitung currentWeekNumber(startDate, seriesLen, today) TANPA mode → default tujuh_hari → floor(4/7)+1 = 1. cutoffWeek = min(1, weekIndex=2) = 1 → actualSeries[1] = null → kurva-S KKP dan baris 'Kumulatif Realisasi/Deviasi' untuk minggu 2 kosong, sementara tabel item laporan minggu 2 terisi. Terjadi pada Senin–Rabu setiap minggu (3 dari 7 hari) untuk SPMK Kamis.

**Bukti.** periodic-report.ts:682 `const currentWeek = currentWeekNumber(startDate, seriesLen, today);` (tanpa argumen ke-4) vs :412 `currentWeekNumber(startDate, totalWeeks, now, weekMode)`; :709 `const cutoffWeek = Math.min(currentWeek, Math.max(1, weekIndex));`; :724 `actualTotals.map((v, i) => (i + 1 <= cutoffWeek ? v : null))`.

**Tes terkait.** tests/integration/sampul-harian-mode-minggu.test.ts / periodic-report.test.ts tidak menguji getPeriodReport pada hari Senin–Rabu dengan SPMK non-Senin.

**Usul perbaikan.** Teruskan weekMode ke currentWeekNumber di :682 (sama seperti :412) dan tambah tes mode senin_minggu yang membekukan 'hari ini' = Senin setelah SPMK Kamis.

**Reproduksi.** MERAH – reproduksi berhasil lewat uji integrasi sementara `tests/integration/_audit-baseline-kurva-57.test.ts` (sudah DIHAPUS setelah dijalankan; `git status` bersih dari berkas saya). Fixture: kontrak `weekMode: "senin_minggu"`, SPMK Kamis 2026-03-05, akhir 2026-04-05 (5 kolom minggu), RAB 1 kategori + 2 item, baseline linear 5 titik; laporan harian final Jumat 2026-03-06 (minggu 1) dan Senin 2026-03-09 (minggu 2 kalender). Jam dibekukan `vi.useFakeTimers({ toFake: ["Date"] })` + `vi.setSystemTime(new Date("2026-03-09T02:00:00.000Z"))` = Senin 09:00 WIB. Perintah: DATABASE_URL=postgresql://ma

### G-5. 🟠 Identitas kategori di blanko KKP dipotong dari lineageKey dengan split('#')[0] — kategori romawi ganda ('VI#2') dilebur ke 'VI'

`src/lib/periodic-report.ts:525` · tinggi · data-integritas · `baseline-kurva-58`

**Skenario.** HPS dengan dua kategori berkode romawi sama (flatten.ts:8 menyebutnya 'nyata di data HPS: kategori roman ganda'); dedup memberi kunci 'VI' dan 'VI#2'; item di bawahnya 'VI#2#1'. Di getPeriodReport, catOf('VI#2#1') → root 'VI' → item kategori kedua masuk baris kategori PERTAMA, subtotal 'VI' membengkak, kategori 'VI#2' tidak punya baris; kolom 'Bobot Rencana' item itu (:756-761) memakai jadwal kategori 'VI' bukan 'VI#2'. Jalur fallback kurvaSchedule (:658) memotong dengan cara sama. Kontras: regenerateBaseline (rab/import.ts:531-532) dan suggest.ts:73-74 memakai pencocokan prefiks terpanjang dengan batas '#' yang benar – jadi baseline/jadwal memakai identitas 'VI#2' sementara tabel KKP tidak mengenalnya.

**Bukti.** periodic-report.ts:524-525 `const catOf = (lineageKey: string): PeriodCategory => { const root = lineageKey.split("#")[0];`; :658 `const root = nd.lineageKey.split("#")[0];` vs rab/import.ts:532 `catKeys.find((c) => lineageKey === c.key || lineageKey.startsWith(`${c.key}#`))` (diurutkan panjang kunci menurun).

**Tes terkait.** Tidak ada tes getPeriodReport dengan kategori berkode ganda.

**Usul perbaikan.** Pakai satu helper kanonik 'kategoriDariLineage(lineageKey, catKeys)' (prefiks terpanjang + batas '#') untuk periodic-report :525 dan :658; tes fixture dua kategori 'VI' → 'VI'/'VI#2' dan pastikan subtotal serta bobot rencana per kategori benar.

**Reproduksi.** MERAH, terbukti lewat integrasi nyata (bukan simulasi helper). Berkas sementara `tests/integration/_audit-baseline-kurva-58.test.ts` (sudah DIHAPUS; `git status --porcelain` bersih). Isi: satu lokasi + kontrak 4 minggu, RAB revisi aktif dengan DUA kategori berkode romawi sama – `lineageKey` "VI" (PEKERJAAN REVETMENT, Rp600jt) dan "VI#2" (PEKERJAAN DINDING PENAHAN TANAH, Rp400jt) – item "VI#1" dan "VI#2#1", baseline 4 titik + matriks `baselineScheduleItem` per kategori (VI: [30,30,0,0]; VI#2: [0,0,20,20]), satu laporan harian final minggu-1 pada item "VI#2#1" (50 dari 100 m3). Perintah: DATABAS

### G-6. 🟡 Impor jadwal mode 'Sesuaikan bobot ke RAB' menolkan sel negatif diam-diam, padahal parser menyerahkan penolakan ke penyimpan

`src/lib/baseline.ts:552` · sedang · bug · `baseline-kurva-59`

**Skenario.** Excel berisi sel −5 pada satu minggu (salah rumus). Mode apaadanya menolak dengan menyebut baris & minggu (jadwal-verbatim.ts:89-93; tes 'nilai negatif ditolak'). Bila user mencentang 'Sesuaikan bobot ke RAB', hitungJadwalBaru mode rab menghitung posSum dari nilai positif saja dan memetakan v<=0 → 0: impor SUKSES, bentuk jadwal berubah tanpa satu kata pun di banner/pratinjau (pratinjauJadwalAction tidak punya pemeriksaan negatif). Diverifikasi skrip: raw [10,−5,20,25] → weekly [9.09,0,18.18,22.73] Σ=50 tanpa galat. Ini persis blind spot CARA_KERJA_AGEN §5 'angka pengguna dibetulkan diam-diam'.

**Bukti.** baseline.ts:547 `const posSum = raw && raw.length === totalWeeks ? raw.reduce((s, v) => s + (v > 0 ? v : 0), 0) : 0;` :552 `weekly = raw.map((v) => (v > 0 ? Math.round(v * k * 1e6) / 1e6 : 0));`; jadwal-import.ts:94-97 'Nilai dikembalikan APA ADANYA – termasuk negatif … sekarang penyimpanlah yang menolaknya dengan menyebut baris & minggunya'.

**Tes terkait.** tests/unit/jadwal-apa-adanya.test.ts hanya menguji mode apaadanya; tidak ada tes negatif untuk mode rab.

**Usul perbaikan.** Di hitungJadwalBaru mode rab, tolak nilai negatif/non-finite dengan pesan yang sama seperti susunJadwalApaAdanya (satu validator bersama), dan tampilkan di pratinjau.

**Reproduksi.** MERAH, terbukti lewat uji integrasi sementara `tests/integration/_audit-baseline-kurva-59.test.ts` (menyalin harness `tests/integration/impor-jadwal-excel.test.ts`: RAB 5/70/25, kontrak 28 hari → 4 minggu), dijalankan dengan `DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-baseline-kurva-59.test.ts`. Berkas itu SUDAH DIHAPUS (`git status --porcelain` bersih dari berkas saya; `_audit-baseline-kurva-60.test.ts` milik agen lain, tidak saya sentuh). Masukan Excel (identik dengan berkas yang DITOLAK pada mode default): I Peker

### G-7. 🟡 Panel saran rencana: sebelum SPMK (currentWeek = 0) rencana dibaca 100% dan deviasi −100% — rumus tandingan planPctAtWeek

`src/lib/plan/suggest.ts:115` · sedang · formula-duplikat · `baseline-kurva-56`

**Skenario.** SPMK dicatat bertanggal masa depan (DECISIONS 202, package/actions.ts:1485 mengembalikan 'terjadwal'), baseline ada. User membuka tab Rencana → 'Saran otomatis' minggu 1. currentWeekNumber mengembalikan 0 (progress.ts:70); suggest.ts membaca points[0 − 1] → undefined → fallback ke titik TERAKHIR = 100 → planPct 100, deviationPct = −100, behind = true; applyWeeklySuggestions menulis audit {behind:true, deviationPct:-100} dan pesan sukses '(mengejar deviasi -100%)'. Diverifikasi skrip: `planPct sebelum SPMK = 100 (seharusnya 0)`. Pola yang sama di scurve-chart.tsx:63 (legenda 'Rencana (100.0%)' sebelum SPMK) karena getScurveSeries meneruskan currentWeek 0 walau tipe ScurveSeries mendokumentasikan 'clamp 1..totalWeeks' (baseline.ts:750).

**Bukti.** suggest.ts:115 `const planPct = Number(points[currentWeek - 1]?.plannedPct ?? points[points.length - 1]?.plannedPct ?? 0);` vs kanonik progress.ts:75-82 planPctAtWeek: `if (weekNumber <= 0) return 0;` – DECISIONS 202 justru dibuat untuk kasus ini. scurve-chart.tsx:63 `const lastPlan = planPct[currentWeek - 1] ?? planPct[planPct.length - 1] ?? 0;`

**Tes terkait.** tests/unit/plan-suggest.test.ts hanya menguji computeSuggestions (inti murni), tidak suggestWeeklyPlan; tidak ada tes DECISIONS 202 untuk panel saran maupun legenda kurva.

**Usul perbaikan.** suggest.ts: `planPct = planPctAtWeek(points.map(p=>Number(p.plannedPct)), currentWeek)`; scurve-chart: pakai planPctAtWeek atau tampilkan 'belum mulai' bila currentWeek 0; tes integrasi suggestWeeklyPlan dengan SPMK masa depan → planPct 0, behind false.

**Reproduksi.** MERAH – direproduksi lewat integrasi (berkas sementara tests/integration/_audit-baseline-kurva-56.test.ts, DATABASE_URL=...marlin_test APP_ENV=test, sudah DIHAPUS setelah dijalankan). Skenario data: org/vendor/paket + Contract{startDate = hari ini + 14 hari, durationDays = 56} (SPMK masa depan), 1 lokasi, RabRevision aktif (2 kategori + 2 item), Baseline aktif linear 8 minggu (plannedPct 12,5 … 100), TANPA laporan harian. Keluaran (stdout vitest): currentWeekNumber = 0 planPctAtWeek = 0 suggest = { planPct: 100, actualPct: 0, deviationPct: -100, behind: true, saran: 1 } scurve currentWeek = 0 

### G-8. 🟢 Edit manual kurva menulis ulang contractDays = minggu×7 pada mode senin_minggu (M1 pendek) — riwayat baseline menyebut durasi yang tidak ada di kontrak

`src/lib/baseline.ts:63` · rendah · bug · `baseline-kurva-60`

**Skenario.** Kontrak 119 hari, SPMK Kamis, senin_minggu → totalWeeksFor = 18 kolom (M1 pendek). User menyesuaikan kurva manual (saveManualBaselineAction) tanpa mengubah jumlah minggu. refWeeks = ceil(119/7) = 17 ≠ 18 → contractDays baseline baru = 18×7 = 126. Kartu Riwayat baseline menampilkan '126 hari (18 mgg)' (baseline-history.tsx:202) untuk kontrak 119 hari; regenerateBaseline tidak pernah lagi 'unchanged' karena membandingkan active.contractDays === contractDays (rab/import.ts:608) sehingga 'Hitung ulang' selalu menumpuk versi.

**Bukti.** baseline.ts:62-63 `const refWeeks = Math.ceil(ref.contractDays / 7); const contractDays = refWeeks === points.length ? ref.contractDays : points.length * 7;` – asumsi minggu = ceil(hari/7) hanya benar untuk tujuh_hari (rab/import.ts:387-390).

**Tes terkait.** Tidak ada tes updateBaselinePoints pada mode senin_minggu.

**Usul perbaikan.** Bandingkan points.length dengan totalWeeksFor(locationId).totalWeeks; bila sama pertahankan contractDays kontrak, bila berbeda tolak/jelaskan, jangan mengarang durasi.

**Reproduksi.** MERAH (gagal seperti yang diperkirakan temuan). Berkas sementara tests/integration/_audit-baseline-kurva-60.test.ts dibuat, dijalankan, lalu DIHAPUS (`git status --porcelain` kosong sesudahnya). Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-baseline-kurva-60.test.ts Fixture: kontrak durationDays=119, startDate 2026-01-08 (Kamis, dipastikan via getUTCDay()===4), endDate start+118, weekMode="senin_minggu"; 1 kategori + 1 item RAB; baseline dibuat lewat regenerateBaseline yang sesungguhnya (bukan disuntik tangan)

---

## H. Otorisasi & audit

### H-1. 🟠 Perubahan lingkup lokasi (cabut/tambah) bisa diajukan DAN disetujui oleh PM/AM untuk lokasi yang bukan penugasannya, bahkan lintas organisasi – tanpa requireLocationAccess dan tanpa filter orgId

`src/lib/package/lingkup-lokasi.ts:216` · tinggi · authz · `authz-sapu-61`

**Skenario.** Project Manager B ditugaskan hanya ke lokasi X (paket A). Ia mengirim FormData ke `ajukanLingkupLokasiAction` dengan `locationId` = lokasi Y paket C (bisa organisasi lain) dan `amendmentId` = CCO kontrak paket C (kedua UUID terbaca dari halaman/PDF mana pun yang pernah ia buka, atau dari dokumen adendum yang beredar). `ajukanPerubahanLingkup` hanya memanggil `requireCapability("contract.manage")` (dipegang PM & AM yang BUKAN cross-location) lalu `db.location.findUnique({ where: { id } })` tanpa `package.orgId` dan tanpa `requireLocationAccess` → draft `cabut` tercipta. Lalu `setujuiPerubahanLingkup(changeId)` juga hanya `contract.manage` + `bolehMenyetujui(role)` tanpa cek akses lokasi/organisasi → PM B mengisi kursi "penugasan" untuk lokasi yang tidak pernah ditugaskan kepadanya. Begitu PD ikut menyetujui, `status: "aktif"` → `lingkupLokasi()` mengeluarkan lokasi Y dari SEMUA agregat paket (nilai kontrak, progres, kurva-S, laporan KKP) sejak effectiveDate. Bandingkan `approveRevisionAction` (rab/actions.ts:804-813) yang memanggil `requireLocationAccess` sebelum `setujuiRevisi` – di sini pagar itu tidak ada sama sekali. `batalkanPerubahanLingkup` (321-333) dan `arsipkanLokasiDicabut` (365-370, `db.location.findMany({ where: { packageId } })`) punya lubang org yang sama.

**Bukti.** ajukanPerubahanLingkup (216-232): const user = await requireCapability("contract.manage"); const [lokasi, adendum] = await Promise.all([ db.location.findUnique({ where: { id: input.locationId }, select: { id: true, packageId: true, name: true } }), db.contractAmendment.findUnique({ where: { id: input.amendmentId }, ... }), ]); // tidak ada requireLocationAccess / orgId di mana pun setujuiPerubahanLingkup (268-288): requireCapability("contract.manage") → bolehMenyetujui(user.role) → locationScopeChange.findUnique({ where: { id: changeId } }) – tanpa cek lokasi/org. Pembanding: rab/actions.ts:804-809 `await requireLocationAccess(user, rev.locationId);` sebelum `setujuiRevisi`.

**Tes terkait.** tests/integration/lingkup-lokasi-adendum.test.ts – mock `requireLocationAccess: async () => {}` (baris 32) tetapi service tidak pernah memanggilnya, jadi tes tidak bisa merah; tidak ada tes yang memakai PM ter-scope dengan lokasi di luar penugasan.

**Usul perbaikan.** Di `ajukanPerubahanLingkup`: cari lokasi dengan `where: { id, package: { orgId: user.orgId } }` lalu `await requireLocationAccess(user, lokasi.id)`; di `setujuiPerubahanLingkup`/`batalkanPerubahanLingkup`: muat `row.locationId` lalu `requireLocationAccess` (org + penugasan) sebelum upsert suara; di `arsipkan/bukaArsipLokasiDicabut`: `db.location.findMany({ where: { packageId, package: { orgId: user.orgId } } })`. Tulis tes integrasi: PM hanya ditugaskan lokasi X mencoba ajukan+setujui cabut lokasi Y → harus ForbiddenError (merah dulu).

**Reproduksi.** MERAH TERBUKTI. Berkas sementara `tests/integration/_audit-authz-sapu-61.test.ts` dibuat, dijalankan, lalu DIHAPUS (`git status --porcelain` kosong setelahnya). Penyiapan sengaja dibuat jujur: HANYA `requireUser` yang dipalsukan (tidak ada cookie di tes); `requireCapability` di-mock memakai `can()` yang ASLI dari `@/lib/authz`, dan `requireLocationAccess`/`hasLocationAccess` dibiarkan ASLI. Data: org1 (paket A + paket B) dan org2 (paket C); PM1 = project_manager org1 dengan LocationAssignment HANYA ke lokasi X1 (paket A); PD1 = program_director org1. Perintah: DATABASE_URL=postgresql://marlin:

### H-2. 🟡 perbaikiFotoHeicAction dan purgeOriginalsAction memindai tabel foto tanpa batas organisasi untuk peran lintas-lokasi – arsip asli foto organisasi lain bisa dihapus/ditulis ulang

`src/lib/photo-restamp/actions.ts:539` · sedang · authz · `authz-sapu-65`

**Skenario.** (a) `purgeOriginalsAction` (photo.archive_purge = SA/PD): `scope = accessibleLocationIds(actor)` → null; `whereArsip(f, null)` (service.ts:289-291) hanya menambah `locationId in` bila `scope !== null`, dan filter `packageId`/`locationId` dari FormData tidak diverifikasi org → `db.photo.findMany` mengembalikan arsip asli SEMUA organisasi → `r2Delete(originalKey)` + `originalPurgedAt` ditulis untuk foto org lain (tak bisa dipulihkan, DECISIONS 197/198). (b) `perbaikiFotoHeicAction` (photo.restamp): `db.photo.findMany({ where: { r2Key: { endsWith: ".heic" } } })` tanpa org/lokasi → foto org lain dirender ulang, kunci R2 lama dihapus, `photoStampRevision` ditulis atas nama actor org A. Bandingkan `restampPhotoAction`/`purgeOneOriginalAction` di file yang sama yang memanggil `requireLocationAccess` (81, 344). Dorman selama single-org.

**Bukti.** service.ts:289-291 export function whereArsip(f, scope) { const and = [{ originalKey: { not: null } }]; if (scope !== null) and.push({ locationId: { in: scope } }); // null → tanpa org actions.ts:539-543 const kandidat = await db.photo.findMany({ where: { r2Key: { endsWith: ".heic" } }, select: { id: true }, orderBy: { createdAt: "asc" }, take: HEIC_PER_JALAN });

**Tes terkait.** tests/unit/photos-heic.test.ts (pipeline gambar, bukan scope)

**Usul perbaikan.** `whereArsip`: bila `scope === null` tambahkan `{ location: { package: { orgId } } }` (terima `orgId` sebagai parameter) dan validasi `f.packageId`/`f.locationId` milik org; HEIC: tambahkan `location: { package: { orgId: actor.orgId } }` pada kedua findMany (foto tanpa lokasi: batasi `uploadedBy.orgId`).

**Reproduksi.** MERAH (3 dari 4 uji gagal). Berkas sementara tests/integration/_audit-authz-sapu-65.test.ts (dijalankan dengan DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test), SUDAH DIHAPUS – `git status --porcelain` bersih. Skenario: dua organisasi nyata di satu DB (Org A & Org B), masing-masing punya paket + lokasi + kegiatan + foto ber-`originalKey`; Org B punya satu foto tambahan berkunci `.heic`. Sesi = super_admin ORG A, `accessibleLocationIds` → null (sesuai isCrossLocation). Keluaran: stdout [purge paket B duluan] {"ok":"2 arsip asli dihapus (0.0 MB). Cap foto-foto itu

### H-3. 🟡 catatSuratAction & petakanSuratAction menerjemahkan `accessibleLocationIds() === null` sebagai TANPA filter sama sekali – lokasi organisasi lain lolos untuk super_admin/program_director (pola B11 yang scope.ts larang)

`src/lib/surat/actions.ts:82` · sedang · authz · `authz-sapu-62`

**Skenario.** Program Director org A memanggil `petakanSuratAction` dengan `locationId` = lokasi org B. `izin = await accessibleLocationIds(user)` → `null` (cross-location). Baris 394-397: `db.location.findFirst({ where: { id: d.locationId, ...(izin ? { id: { in: izin } } : {}) } })` → `{ id }` saja, tanpa `package.orgId` → lokasi org B ditemukan → `db.issue.create`/`db.finding.create` menulis kendala/temuan ke lokasi org B dengan `raisedById` user org A. Hal yang sama di `catatSuratAction` baris 82-86 (surat org A ditautkan ke lokasi org B). `lib/auth/scope.ts` (baris 3-16) secara eksplisit menyatakan `null` = "tanpa batas DI DALAM organisasi" dan pemakai WAJIB lewat `locationScopeWhere`; `packageId` di file yang sama sudah benar memakai `packageScopeWhere` (baris 77-79) dan `bacaBerkasSuratAction` (569-573) benar memakai `{ orgId }` untuk cabang null – hanya dua tempat ini yang menyimpang. Dorman selama satu organisasi, tapi persis kelas kebocoran DECISIONS 150/B11.

**Bukti.** const izin = await accessibleLocationIds(user); ... if (d.locationId) { const lok = await db.location.findFirst({ where: { id: d.locationId, ...(izin ? { id: { in: izin } } : {}) }, select: { id: true }, }); if (!lok) return { error: "Lokasi tidak ditemukan dalam scope Anda." }; } (petakanSuratAction 394-397 identik)

**Tes terkait.** tidak ada tes surat yang menguji scope lokasi lintas organisasi (tests/unit/paket-lokasi-scope.test.ts hanya menguji helper scope.ts)

**Usul perbaikan.** Ganti `...(izin ? { id: { in: izin } } : {})` dengan `locationScopeWhere(user, izin)` dari `@/lib/auth/scope` (atau `requireLocationAccess(user, d.locationId)` yang sudah memeriksa org + penugasan). Tes integrasi: PD org A memetakan surat ke lokasi org B → "tidak ditemukan".

**Reproduksi.** Uji integrasi sementara tests/integration/_audit-authz-sapu-62.test.ts (dijalankan lalu DIHAPUS; `git status` bersih dari berkas saya). Skenario: dua organisasi lengkap di satu DB test; aktor = program_director org A (accessibleLocationIds → null); satu Letter milik org A; sasaran = locationId milik org B. Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-authz-sapu-62.test.ts Hasil: 3 tes, 1 lulus (kontrol), 2 GAGAL – yakni MERAH, persis membuktikan kebocoran: stdout > petakanSuratAction MENOLAK lokasi organisasi

### H-4. 🟢 changeLocationStatus (dan startPelaksanaan di package/actions.ts) mencatat audit STATUS dengan `audit()` best-effort di luar transaksi – melanggar aturan AUDIT-01 yang ditulis di audit.ts sendiri

`src/app/(app)/lokasi/[slug]/actions.ts:69` · rendah · data-integritas · `authz-sapu-68`

**Skenario.** `changeLocationStatus` menulis `location.update` + `locationStatusHistory.create` dalam `$transaction` (48-68), lalu `await audit(...)` di luar transaksi (69-73). `audit()` menelan galat (audit.ts:52-58) dan dokumentasinya tegas: "JANGAN dipakai untuk mutasi UANG atau STATUS: gunakan auditIn" (audit.ts:41-43). Bila `auditLog.create` gagal (koneksi putus/kuota), status lokasi sudah berubah tanpa jejak audit – persis keadaan yang AUDIT-01 larang. Pola yang sama di `startPelaksanaan` (package/actions.ts:1532) untuk transisi paket → pelaksanaan + status lokasi → berjalan, dan `advanceStage`/`revertStage` (277, 333). Modul lain di repo (finance `mutasiBerjejak`, findings `transition`, documents-manage) sudah memakai `auditIn` di dalam tx. Histori status tetap tertulis, jadi jejak tidak hilang total – karena itu rendah.

**Bukti.** await db.$transaction(async (tx) => { await tx.location.update({ ..., data: { status: d.toStatus, ... } }); await tx.locationStatusHistory.create({ ... }); }); await audit(user.id, "location.status_change", "location", location.id, { from: location.status, to: d.toStatus, note: d.note ?? null }); audit.ts:41-43: "JANGAN dipakai untuk mutasi UANG atau STATUS: untuk itu gunakan `auditIn` di dalam transaksi yang sama (AUDIT-01)"

**Tes terkait.** tidak ada tes yang memaksa auditLog gagal lalu memeriksa status batal

**Usul perbaikan.** Baca `ip = await requestIp()` sebelum tx, lalu panggil `auditIn(tx, ...)` di dalam `$transaction` untuk changeLocationStatus, advanceStage, revertStage, startPelaksanaan (pola finance/actions.ts `mutasiBerjejak`).

**Reproduksi.** MERAH, terbukti. Berkas sementara tests/integration/_audit-authz-sapu-68.test.ts (sudah DIHAPUS; `git status --porcelain` bersih) memanggil `changeLocationStatus` sungguhan terhadap PostgreSQL test (DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test), dengan `@/lib/auth/session` dan `next/cache` di-mock, lalu `vi.spyOn(db.auditLog, "create").mockRejectedValue(new Error("simulasi: koneksi audit putus"))` – persis skenario "koneksi putus/kuota". Keluaran: status awal: prospek hasil aksi: {"success":"Status lokasi → Berjalan."} status akhir: berjalan · isActive=true ·

### H-5. 🟢 ensureMilestones adalah server action publik ("use server") yang memutasi milestone + menghapus salinan legacy hanya dengan requireUser() – tanpa capability, dan konsolidasi status milestone tanpa audit

`src/lib/milestones/actions.ts:42` · rendah · authz · `authz-sapu-66`

**Skenario.** wakil_ppk / exec_viewer / field_supervisor (tanpa `compliance.manage`) memanggil `ensureMilestones(packageId)` langsung (endpoint action-nya terekspos ke klien karena diexport dari berkas "use server"). Baris 43-45 hanya `requireUser()` + cek org paket; tanpa `locationId` tidak ada `requireLocationAccess`. Efek: `adminMilestone.createMany` + `consolidateLegacyPaketMilestones` (161-180) yang meng-update status/verifiedById induk ke "paling maju" dan `deleteMany` salinan lokasi – perubahan status milestone tanpa capability, dan konsolidasi ini tidak di-audit per milestone (hanya angka `consolidated` di audit `milestone.ensure`). Melanggar prinsip #3 CLAUDE.md (setiap mutasi server action wajib requireCapability + audit).

**Bukti.** export async function ensureMilestones(packageId: string, locationId?: string): Promise<void> { const user = await requireUser(); const pkg = await db.package.findUnique({ where: { id: packageId }, select: { orgId: true } }); if (!pkg || pkg.orgId !== user.orgId) throw new ForbiddenError("Paket tidak ditemukan"); ... const consolidated = await consolidateLegacyPaketMilestones(packageId); // update status + deleteMany

**Tes terkait.** tidak ada

**Usul perbaikan.** Pindahkan `ensureMilestones` ke modul non-"use server" (mis. `milestones/ensure.ts`) yang dipanggil server component/aksi ber-capability, atau gerbang dengan `document.view` untuk materialisasi dan `compliance.manage` untuk konsolidasi; catat auditIn per milestone yang statusnya diubah/dihapus.

**Reproduksi.** MERAH (dalam arti "cacat terbukti ada"): berkas sementara tests/integration/_audit-authz-sapu-66.test.ts (sudah DIHAPUS; `git status --porcelain` bersih) dijalankan dengan DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test → 3 lulus, semuanya menegaskan perilaku yang dituduhkan. Sesi dipalsukan sebagai role `field_supervisor` TANPA penugasan lokasi (requireLocationAccess selalu melempar). Disiapkan satu baris WARISAN pra-078: milestone scope-paket (`templateKey` kontrak) dengan locationId terisi, status "selesai", verifiedById terisi. Kasus A – `await expect(ensure

### H-6. 🟢 createUser: `locationIds` dari FormData langsung dijadikan LocationAssignment tanpa cek organisasi untuk peran `user.manage` – regresi AUTH-03 yang sudah ditutup di setAssignments

`src/lib/users/actions.ts:123` · rendah · authz · `authz-sapu-63`

**Skenario.** super_admin/program_director org A membuat pengguna baru dan menyelipkan `locationIds` = id lokasi org B. Baris 123-129 hanya menyaring bila `!can(actor.role, "user.manage")`; untuk pemegang user.manage `accessibleLocationIds` mengembalikan `null` sehingga tidak ada saringan apa pun, lalu baris 152-157 `db.locationAssignment.createMany({ data: locationIds.map(...) })` menulis penugasan ke lokasi org B. Akun baru (org A) kini lolos `hasLocationAccess`? – tidak untuk halaman (cek `package.orgId` gagal), tetapi baris penugasan lintas-org tetap tertulis dan dibaca `accessibleLocationIds()` (session.ts:171-177, tanpa filter org) sehingga daftar/`locationRelScopeWhere({id in ...})` di banyak query menampilkan data lokasi org B. `setAssignments` (358-365) sudah memvalidasi `package.orgId` dengan komentar AUTH-03; `createUser` dibiarkan tanpa validasi yang sama.

**Bukti.** let locationIds = d.locationIds ?? []; if (!can(actor.role, "user.manage")) { const own = await accessibleLocationIds(actor); // null = akses semua if (own !== null) { ... locationIds = locationIds.filter((id) => allowed.has(id)); } } ... await db.locationAssignment.createMany({ data: locationIds.map((locationId) => ({ userId: user.id, locationId })), skipDuplicates: true }); Pembanding setAssignments 358-365: `db.location.count({ where: { id: { in: locationIds }, package: { orgId: actor.orgId } } })` + tolak bila tidak sama.

**Tes terkait.** tests/integration/authz-proteksi-akun.test.ts, tests/integration/akar-super-admin.test.ts (createUser diuji hanya untuk akar/peran, bukan untuk lokasi lintas-org)

**Usul perbaikan.** Sebelum createMany, validasi SEMUA locationIds seperti di setAssignments: `count where { id in, package: { orgId: actor.orgId } } === locationIds.length`, untuk semua peran (pemegang user.manage pun). Tes: SA org A membuat user dengan lokasi org B → error, tidak ada baris penugasan.

**Reproduksi.** MERAH – terbukti, lalu berkas ujinya dihapus (git status bersih). Berkas sementara `tests/integration/_audit-authz-sapu-63.test.ts` (dua org lengkap di satu DB, sesi di-mock seperti `tests/integration/akar-super-admin.test.ts`, tapi `accessibleLocationIds` DITIRU SETIA dari implementasi asli `src/lib/auth/session.ts:168-177` – bukan `async () => null` seperti di berkas akar, supaya tidak menciptakan lubangnya sendiri). Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-authz-sapu-63.test.ts Keluaran: ✓ setAssignmen

### H-7. 🟢 setPackageWaGroupAction & testWahaCaptureAction memuat paket tanpa filter organisasi; setPackageDriveFolderAction memakai capability `wa.configure` untuk folder Drive

`src/lib/waha/actions.ts:184` · rendah · authz · `authz-sapu-67`

**Skenario.** (a) `setPackageWaGroupAction`: `db.package.findUnique({ where: { id: d.packageId } })` tanpa `orgId` → super_admin org A bisa menautkan/melepas grup WA paket org B (laporan otomatis org B berpindah grup). `setPackageDriveFolderAction` (gdrive/actions.ts:184-188) untuk hal serupa sudah benar memakai `orgId: user.orgId` – dua aksi sebelah-menyebelah, satu berpagar satu tidak. (b) `testWahaCaptureAction`: `db.package.findFirst({ where: { waGroupId: { not: null } } })` mengambil paket organisasi mana pun dan menyimpan pesan uji ke sana. (c) `setPackageDriveFolderAction` digerbang `wa.configure` yang komentar authz.ts:89 definisikan sebagai "atur grup WhatsApp per paket + tes koneksi WAHA" – matriks izin tidak lagi menggambarkan apa yang sebenarnya dibuka kapabilitas itu. Dorman (super_admin saja) tetapi bertentangan dengan doktrin AUTH-01 session.ts:137-145 ("penjagaan tidak boleh bergantung pada keadaan data").

**Bukti.** waha/actions.ts:183-185 const user = await requireCapability("wa.configure"); const pkg = await db.package.findUnique({ where: { id: d.packageId }, select: { id: true } }); gdrive/actions.ts:184-186 (pembanding) const pkg = await db.package.findFirst({ where: { id: packageId, orgId: user.orgId }, select: { id: true } });

**Tes terkait.** tidak ada

**Usul perbaikan.** Tambahkan `orgId: user.orgId` pada kedua lookup paket di waha/actions.ts; beri `setPackageDriveFolderAction` kapabilitas sendiri (mis. `gdrive.configure`) atau perbarui komentar/matriks `wa.configure` agar jujur.

**Reproduksi.** MERAH, dua-duanya, lewat tests/integration/_audit-authz-sapu-67.test.ts (sementara; sudah dihapus, git status bersih). Fixture: org A (super_admin) + org B (dua paket). Mock hanya @/lib/auth/session (requireCapability→user org A), @/lib/waha/client (getGroupInfo dilumpuhkan supaya tak menyentuh jaringan), next/cache, react cache. Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-authz-sapu-67.test.ts Keluaran: × setPackageWaGroupAction MENOLAK paket organisasi lain AssertionError: expected '120363000000007702@g.us

---

## I. Konsumen angka (PDF, Excel, WA, AI, komponen)

### I-1. 🟡 Legenda kurva-S sebelum SPMK menampilkan “Rencana (100.0%)” – minggu 0 jatuh ke titik terakhir baseline

`src/components/knmp/scurve-chart.tsx:63` · sedang · bug · `konsumen-angka-70`

**Skenario.** Lokasi sudah punya baseline tetapi SPMK belum tiba → `currentWeekNumber` mengembalikan 0 (DECISIONS 202) dan `baseline.ts:792,856` meneruskan `currentWeek: 0` dengan `actualPct` semua null. Di ScurveChart, `planPct[currentWeek - 1]` = `planPct[-1]` = undefined → `??` jatuh ke `planPct[planPct.length - 1]` = 100. Legenda di halaman Progress/lokasi/paket berbunyi “Rencana (100.0%)” untuk proyek yang belum boleh mulai, bertolak belakang dengan `planPctAtWeek` (0 untuk minggu 0) yang dipakai KPI di halaman yang sama. Dibuktikan MERAH: render `ScurveChart` dengan `currentWeek: 0` → regex `Rencana \(([\d.]+)%\)` menghasilkan "100.0". Selain itu `toFixed(1)` mencetak titik desimal ("12.4%") di UI berbahasa Indonesia, berbeda dari seluruh blanko yang memakai id-ID.

**Bukti.** const lastPlan = planPct[currentWeek - 1] ?? planPct[planPct.length - 1] ?? 0; // :63 … Rencana ({lastPlan.toFixed(1)}%) // :149 // lib/progress.ts:79: if (weekNumber <= 0) return 0; ← aturan resmi minggu 0

**Tes terkait.** Tidak ada tes untuk scurve-chart.tsx (grep tests/: kosong); tests/unit/ews-rules.test.ts:48 sudah memodelkan weekNumber 0 di modul lain

**Usul perbaikan.** Pakai `planPctAtWeek(planPct, currentWeek)` dari progress.ts (sudah menangani minggu 0) dan formatter id-ID; tambah tes unit render `currentWeek: 0` → legenda 0,0%.

**Reproduksi.** MERAH, terbukti. Berkas sementara tests/unit/_audit-konsumen-angka-70.test.ts (renderToStaticMarkup + React.createElement, tanpa JSX), fixture: totalWeeks 4, planPct [10,35,70,100], actualPct semua null, currentWeek 0. Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test SESSION_SECRET=... pnpm vitest run tests/unit/_audit-konsumen-angka-70.test.ts Keluaran: stdout | ... > legenda harus 0, bukan 100 LEGENDA minggu 0 = 100.0 × legenda harus 0, bukan 100 AssertionError: expected '100.0' to be '0.0' Test Files 1 failed (1) | Tests 1 failed | 2 passed (3) Dua t

### I-2. 🟡 Laporan eksekutif AI mencampur dua level status tanpa mengaku: gerbang “data belum masuk” memakai laporan FINAL saja, sedangkan realisasi yang ditampilkan memakai level dilaporkan (dikirim+disetujui+final)

`src/lib/ai-hub/render.ts:181` · sedang · konflik-spesifikasi · `konsumen-angka-72`

**Skenario.** Portofolio 30 hari, semua laporan sudah `disetujui` (belum difinalkan – lazim, finalisasi dilakukan berkala). `getLocationsProgress` (COUNTED) memberi realisasi 40% dan deviasi +2 pp; tetapi `finalReports = rep.final = 0` sehingga `dataBelumMemadai` → status dipaksa `data_kurang`, `dataWarning` menyatakan “baru 0 dari 30 laporan harian yang final. Angka deviasi di bawah mencerminkan data yang belum masuk, bukan pekerjaan yang berhenti” (padahal deviasinya justru SUDAH memuat 30 laporan itu), `alasanPrioritas` menulis “Kondisi fisik belum dapat dinilai sampai laporan harian masuk”, dan `barisPrioritas` menyembunyikan realisasi 40% (“belum ada laporan final (0/30) · rencana 38.0%”). Tiga kanal (HTML cetak, WA, Excel) memuat pernyataan yang salah tentang data yang sebenarnya ada. Protokol §“Status progress harus dibedakan” menuntut level disebut dan tidak dicampur; di sini angka resmi level dilaporkan dinilai oleh gerbang level frozen.

**Bukti.** finalReports: rep.final ?? 0, sentReports: (rep.dikirim ?? 0) + (rep.disetujui ?? 0), // source.ts:397-398 return t.reportsFinal / t.reportsExpected < AMBANG_DATA_KOSONG; // render.ts:104 ? `Kinerja fisik belum bisa dinilai: baru ${totals.reportsFinal} dari ${totals.reportsExpected} laporan harian yang final. Angka deviasi di bawah mencerminkan data yang belum masuk, bukan pekerjaan yang berhenti.` // render.ts:181 const tanpaLaporan = w.finalReports === 0; → `belum ada laporan final (0/${w.expectedReports}) · rencana …` // render.ts:243-245

**Tes terkait.** tests/unit/ai-laporan-isi.test.ts:342 hanya menguji `finalReports: 0, actualPct: 0`; kasus finalReports 0 dengan actualPct > 0 (laporan dikirim/disetujui) tidak diuji

**Usul perbaikan.** Putuskan (tanya user) level mana yang menjadi dasar gerbang: bila dilaporkan, hitung `reportsCounted` (dikirim+disetujui+final) sebagai penyebut kelengkapan; bila final, JANGAN mengklaim “data belum masuk” – tulis “belum difinalkan” dan tetap tampilkan realisasi level dilaporkan dengan labelnya. Tambah tes untuk finalReports 0 + actualPct 40.

**Reproduksi.** Berkas uji sementara tests/unit/_audit-konsumen-angka-72.test.ts dibuat, dijalankan, lalu DIHAPUS (git status --porcelain kosong, tidak ada jejak). Skenario: 1 lokasi, expectedReports 30, finalReports 0, sentReports 30 (semua 'disetujui'), planPct 38, actualPct 40, deviationPp +2; AI menulis overallStatus 'normal'. Keluaran (pnpm vitest run <berkas> --reporter=verbose --silent=false, 2 passed): STATUS: data_kurang | Data belum lengkap WARNING: Kinerja fisik belum bisa dinilai: baru 0 dari 30 laporan harian yang final. Angka deviasi di bawah mencerminkan data yang belum masuk, bukan pekerjaan y

### I-3. 🟡 Laporan yang “diharapkan” dihitung sampai asOf tanpa dibatasi akhir kontrak – proyek yang sudah PHO dinyatakan data_kurang / readiness rendah oleh AI

`src/lib/ai-hub/source.ts:375` · sedang · konflik-spesifikasi · `konsumen-angka-71`

**Skenario.** Paket dengan SPMK 1 Maret dan endDate 31 Mei, seluruh laporan final, lokasi berstatus pho. Run Portfolio Pulse/Report Studio periode Juli (asOf = hari ini, 60 hari setelah endDate): `expected = floor((asOf − max(start,SPMK))/hari)+1` = 31 hari Juli, `finalReports` Juli = 0. Akibat berantai: `computeReadiness` menandai “Laporan harian difinalkan sesuai periode” buruk (bobot 25); `dataBelumMemadai` (0/31 < 25%) MEMAKSA status laporan eksekutif menjadi `data_kurang` dengan kalimat “baru 0 dari 31 laporan harian yang final … data yang belum masuk” di HTML/WA/Excel untuk proyek yang sudah selesai. PROJECT.md §6 (DECISIONS 340): hari di luar masa kontrak TIDAK dihitung sebagai belum lapor; `source.ts` hanya memilih `contract.startDate` (:177), `endDate` tidak pernah dibaca.

**Bukti.** package: { select: { name: true, contract: { select: { startDate: true } } } }, // :177 const from = Math.max(start.getTime(), startDate.getTime()); if (endMs >= from) expected = Math.floor((endMs - from) / DAY_MS) + 1; // :374-375 // render.ts:102-105: if (t.reportsExpected <= 0) return true; return t.reportsFinal / t.reportsExpected < AMBANG_DATA_KOSONG; // PROJECT.md §6: "hari di luar masa kontrak & hari yang belum tiba TIDAK dihitung sebagai 'belum lapor' – DECISIONS 340"

**Tes terkait.** tests/unit/ai-hub.test.ts & ai-laporan-isi.test.ts memasok expectedReports sebagai fixture tetap; tidak ada tes yang menurunkan expected dari tanggal kontrak

**Usul perbaikan.** Batasi `endMs` dengan `min(asOf, contract.endDate)` (dan hormati status lokasi selesai/pho/fho) memakai helper yang sama dengan kalender Pelaksanaan Harian (DECISIONS 340), atau nyatakan di limitations bahwa periode di luar masa kontrak tidak dihitung. Tambah tes: periode setelah endDate → expected 0 → status tidak dipaksa data_kurang.

**Reproduksi.** MERAH, direproduksi lewat tests/integration/_audit-konsumen-angka-71.test.ts (sementara, sudah DIHAPUS; git status bersih kecuali berkas agen lain _audit-konsumen-angka-72.test.ts). Setup: org + paket(stage pelaksanaan) + vendor, Contract startDate 2026-03-01 / endDate 2026-05-31 / durationDays 92, Location status='pho', TANPA laporan sama sekali. Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run <berkas>. Keluaran (hari uji 2026-09-15): expectedReports = 31 finalReports = 0 readiness = 20 poor <-- skor 20 ini akibat setup minimal saya ta

### I-4. 🟢 Formulir rencana mingguan: subtotal per kategori dan kolom “Kekurangan” dihitung di komponen/PDF/Excel, bukan di rencana-format/rencana-mingguan – dan subtotalnya hanya ada di layar

`src/components/knmp/kkp-weekly-plan.tsx:313` · rendah · formula-duplikat · `konsumen-angka-76`

**Skenario.** `CategoryBlock` menjumlahkan `bobotTarget` dan `nilaiTarget` per kategori (:313-314) dan menampilkannya sebagai baris kelompok; PDF (rencana-kkp.ts:260) dan Excel (rencana-xlsx.ts:205) menulis baris kelompok TANPA subtotal, jadi dokumen yang “sama persis” (klaim di kepala rencana-xlsx.ts) menampilkan angka yang tidak ada di dua kanal lain. Kolom Kekurangan = `max(0, target − realisasi)` ditulis tiga kali (kkp-weekly-plan.tsx:187, rencana-kkp.ts:211, rencana-xlsx.ts:165) alih-alih satu field di `tidakTuntas`; kalau definisi “realisasi” PPC berubah (kumulatif vs dalam-pekan, riwayat 2026-08-28), tiga tempat harus diubah bersama.

**Bukti.** const subtotalBobot = rows.reduce((s, r) => s + r.bobotTarget, 0); // kkp-weekly-plan.tsx:313 const subtotalNilai = rows.reduce((s, r) => s + r.nilaiTarget, 0); // :314 <Td align="right">{vol(Math.max(0, t.target - t.realisasi), t.unit)}</Td> // :187 { text: satuan(Math.max(0, t.target - t.realisasi), t.unit), align: "right" }, // rencana-kkp.ts:211 round2(Math.max(0, t.target - t.realisasi)), // rencana-xlsx.ts:165

**Tes terkait.** tests/unit rencana-* tidak memeriksa paritas subtotal kategori antar kanal (tidak ditemukan)

**Usul perbaikan.** Tambahkan `kekurangan` pada `tidakTuntas` dan subtotal per kategori (atau putuskan subtotal tidak ditampilkan di mana pun) di lib/plan/rencana-mingguan.ts; ketiga konsumen membaca field itu.

**Reproduksi.** MERAH (2 dari 3 uji gagal). Berkas sementara tests/unit/_audit-konsumen-angka-76.test.ts (sudah DIHAPUS; `git status --porcelain` bersih) memakai satu fixture RencanaMingguan dengan dua kategori (PEKERJAAN TANAH = 24jt + 6jt, PEKERJAAN PASANGAN = 6jt) dan satu item tidakTuntas, lalu merender KkpWeeklyPlan (renderToStaticMarkup) dan membangun buildRencanaMingguanXlsx dari fixture yang SAMA. ✓ "layar A4 memuat subtotal kategori" – HTML mengandung "30.000.000" dan "3,00%" × "Excel memuat subtotal kategori yang sama" AssertionError: expected [ 'RENCANA KERJA MINGGUAN', …(383) ] to include '3000000

### I-5. 🟢 Cakupan nilai RAB & cakupan harga RAPL dihitung ulang di komponen cetak dan di Excel padahal rapl-calc sudah mengembalikannya (`keandalan.cakupanNilai/cakupanHarga`)

`src/components/knmp/rapl-lembar.tsx:48` · rendah · formula-duplikat · `konsumen-angka-75`

**Skenario.** Lembar cetak RAPL menerima `harga.perbandingan` (dipakai di :51 untuk margin) yang sudah memuat `keandalan.cakupanNilai` dan `cakupanHarga` dari `bandingkanDenganNilaiProyek` (rapl-calc.ts:532-535), tetapi :48-50 menghitung `pctNilai`/`pctHarga` sendiri; rapl-xlsx.ts:146 melakukan hal yang sama untuk sel “Cakupan”. Ambang `< 99.95` di :94 pun disalin dari rapl-calc.ts:545. Hari ini hasilnya identik; begitu definisi cakupan di calc layer berubah (mis. penyebut dikurangi baris tanpa volume), lembar A4 dan Excel diam-diam menampilkan cakupan lama – padahal justru cakupanlah yang dijadikan alasan lembar ini ada. Sel Excel :243 juga memakai `toFixed(1)` (titik desimal) di teks berbahasa Indonesia.

**Bukti.** const pctNilai = rapl.nilaiRab > 0n ? (Number(rapl.dipakai.nilai) / Number(rapl.nilaiRab)) * 100 : 0; // rapl-lembar.tsx:48-49 const pctHarga = harga.baris.length > 0 ? (harga.berharga / harga.baris.length) * 100 : 0; // :50 const p = harga.perbandingan; // :51 (sudah punya keandalan.cakupan*) const pct = rapl.nilaiRab > 0n ? (Number(rapl.dipakai.nilai) / Number(rapl.nilaiRab)) * 100 : 0; // rapl-xlsx.ts:146 // rapl-calc.ts:532-535 rumus yang sama, kanonik

**Tes terkait.** tests/unit tidak memeriksa paritas cakupan lembar/Excel vs rapl-calc (tidak ditemukan)

**Usul perbaikan.** Pakai `harga.perbandingan.keandalan.cakupanNilai/cakupanHarga` (atau ekspos fungsi cakupan murni dari rapl-calc) di kedua konsumen; ganti `toFixed` dengan formatter id-ID.

**Reproduksi.** Berkas sementara /home/user/marlin-gibaku/tests/unit/_audit-konsumen-angka-75.test.ts (React.createElement + renderToStaticMarkup) – sudah DIHAPUS; git status bersih dari berkas itu. Perintah: pnpm vitest run tests/unit/_audit-konsumen-angka-75.test.ts --reporter=verbose → "Test Files 1 passed (1) · Tests 2 passed (2)". Uji A (keadaan produksi hari ini): rapl.dipakai.nilai=726 jt, nilaiRab=1 M, 3 dari 10 sumber daya berharga. bandingkanDenganNilaiProyek menghasilkan cakupanNilai=72,6 dan cakupanHarga=30; lembar mencetak "72,6%" dan "30,0%". LULUS ⇒ tidak ada angka salah hari ini (ini yang mena

### I-6. 🟢 Angka di laporan AI (HTML cetak, WA, Excel, prompt, sitasi, risiko) diformat `toFixed(1)` bertitik desimal, berbeda dari konvensi id-ID seluruh blanko dan balasan WA tanya-jawab

`src/lib/ai-hub/render.ts:348` · rendah · formula-duplikat · `konsumen-angka-77`

**Skenario.** Report Studio menghasilkan “realisasi 12.4% vs rencana 30.2% (−17.8 pp)” di WhatsApp dan di HTML A4, sementara balasan WA niat progress (tanya-format.ts:28-30) menulis “12,40%”, dan blanko KKP memakai Intl id-ID. Pembaca yang sama menerima dua konvensi desimal dari sistem yang sama; kkp-period-report.tsx:22-30 sudah mencatat bahwa “12.40” dibaca dua belas ribu empat ratus oleh pembaca Indonesia dan melarang `toFixed` di dokumen. Bukan formula angka, tetapi presentasi angka resmi (protokol: persentase dibulatkan di presentation layer secara konsisten).

**Bukti.** <td class="num">${w.planPct.toFixed(1)}%</td><td class="num">${w.actualPct.toFixed(1)}%</td> // render.ts:348 return `${v >= 0 ? "+" : "−"}${Math.abs(v).toFixed(1)}`; // render.ts:91 value: `rencana ${facts.planPct.toFixed(1)}% · realisasi ${facts.actualPct.toFixed(1)}% …`, // source.ts:438 `Rencana ${f.planPct.toFixed(1)}% vs realisasi ${f.actualPct.toFixed(1)}% …` // risk.ts:51 export const pct = (n: number) => `${n.toFixed(2).replace(".", ",")}%`; // tanya-format.ts:28 (konvensi WA)

**Tes terkait.** tests/unit/ai-laporan-isi.test.ts & ai-laporan-excel.test.ts menegaskan paritas antar kanal AI, tidak memeriksa konvensi desimal terhadap kanal WA/blanko

**Usul perbaikan.** Satu formatter persen id-ID (seperti `formatPct` di lib/format.ts) untuk render.ts, excel.ts (angka numerik + numFmt), source.ts sitasi, risk.ts, prompt.ts; sel Excel tetap numerik dengan numFmt.

**Reproduksi.** MERAH. Berkas sementara tests/unit/_audit-konsumen-angka-77.test.ts (fixture PulseRow planPct=30.2, actualPct=12.4, deviationPp=-17.8; sudah DIHAPUS, `git status --porcelain` bersih) menjalankan renderAiReportHtml / renderAiReportWhatsApp / buildAiReportWorkbook lalu menolak semua desimal bertitik yang bukan pemisah ribuan id-ID (regex sama semangatnya dgn penjaga FMT-01): `pnpm vitest run tests/unit/_audit-konsumen-angka-77.test.ts` → Tests 3 failed | 3 passed - HTML cetak A4: AssertionError: expected [ '17.8', '12.4', '30.2', '30.2', '12.4', '17.8' ] to deeply equal [] (satu '15.00' tambahan

---

## J. Keuangan & kesiapan termin

### J-1. 🟠 Parser jumlah rupiah membuang koma desimal diam-diam: '1.500.000,00' menjadi Rp150.000.000

`src/lib/finance/actions.ts:87` · tinggi · data-integritas · `keuangan-79`

**Skenario.** Site Manager menyalin nominal dari invoice vendor dalam format Indonesia '1.500.000,00' ke form realisasi/komitmen/invoice/termin. amountSchema (actions.ts:83-90) menghapus SEMUA titik/koma/spasi → '150000000' lolos regex ^\d+$ → BigInt 150.000.000 (100× nilai sebenarnya) tersimpan sebagai transaksi diajukan tanpa peringatan. Dibuktikan dengan node: "1.500.000,00".replace(/[.,\s]/g,"") → "150000000", regex lolos. Bertentangan dengan DECISIONS 203 (angka pengguna tidak boleh 'dibetulkan' diam-diam). Input hanya inputMode=numeric (desktop bebas mengetik apa saja).

**Bukti.** actions.ts:83-90 const amountSchema = z .string("Jumlah wajib diisi") .trim() .min(1, "Jumlah wajib diisi") .transform((s) => s.replace(/[.,\s]/g, "")) .pipe(z.string().regex(/^\d+$/, "Jumlah harus angka rupiah bulat")) .transform((s) => BigInt(s))

**Tes terkait.** Belum ada uji untuk amountSchema

**Usul perbaikan.** Tolak masukan yang mengandung pemisah desimal (koma diikuti 1–2 digit di akhir, atau campuran titik+koma) dengan pesan yang menyebut nilainya, atau parse dengan aturan id-ID (titik = ribuan, koma = desimal) dan tolak bila ada pecahan. Tambah uji unit amountSchema untuk '1.500.000,00' dan '15.000,50'.

**Reproduksi.** GAGAL (merah). Berkas sementara tests/integration/_audit-keuangan-79.test.ts (DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test) memanggil server action asli `createCommitment` dengan FormData nyata ke Postgres nyata; berkas SUDAH DIHAPUS, `git status --porcelain` bersih. Hasil: Test Files 1 failed (1) | Tests 2 failed | 1 passed (3) [A] amount="1.500.000,00" → hasil aksi: { success: 'Komitmen PO-A-... diajukan.' } tersimpan: 150000000 AssertionError: expected 150000000n to be 1500000n [B] amount="15.000,50" → hasil aksi: { success: 'Komitmen PO-B-... diajukan.' }

### J-2. 🟡 Label 'sisa' pencairan termin dihitung di komponen dan mengabaikan retensi – angka yang ditawarkan UI ditolak server

`src/app/(app)/lokasi/[slug]/keuangan/lokasi-keuangan-client.tsx:765` · sedang · formula-duplikat · `keuangan-81`

**Skenario.** Termin 2 nilai Rp100 jt, retensi ditahan Rp5 jt, belum cair. UI DisbursementForm menulis 'Pencairan (sisa Rp100.000.000)' karena remaining = amount − disbursedTotal (client:765). User mengisi 100 jt → applyDisbursementTx (apply.ts:87-92) menghitung payable = amount − retentionHeld = 95 jt dan menolak: 'Melebihi sisa termin 2 (setelah retensi Rp5.000.000): sisa Rp95.000.000'. Dua definisi 'sisa' untuk objek yang sama, salah satunya di komponen React (dilarang menghitung uang di luar calc layer). Setelah cair 95 jt status 'cair' tapi UI tetap menampilkan sisa Rp5 jt dan tombol Pencairan masih muncul? Tidak – status cair menutup tombol, tetapi selama cair_sebagian label salah.

**Bukti.** lokasi-keuangan-client.tsx:765 const remaining = BigInt(billing.amount) - BigInt(billing.disbursedTotal); apply.ts:87-88 const payable = billing.amount - billing.retentionHeld; const remaining = payable - received;

**Tes terkait.** Tidak ada

**Usul perbaikan.** Kirim sisa dari server (queries.listOwnerBillings menambahkan payableRemaining = amount − retentionHeld − disbursedTotal, atau fungsi murni di calc.ts yang dipakai apply.ts dan halaman) dan hapus perhitungan di komponen.

**Reproduksi.** MERAH DIBUKTIKAN. Berkas sementara tests/integration/_audit-keuangan-81.test.ts dijalankan lalu DIHAPUS (git status --short kosong setelahnya). Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test \ pnpm vitest run tests/integration/_audit-keuangan-81.test.ts --reporter=verbose --disable-console-intercept Isi uji: buat Contract (retentionPercent 5) + OwnerBilling terminNo 2, amount 100.000.000, retentionHeld 5.000.000, status "disetujui". Baris UI diambil lewat jalur produksi sungguhan `listOwnerBillings()` (sama seperti page.tsx), lalu "sisa" dihitung deng

### J-3. 🟡 setBudgetLine tanpa row-lock: dua penyimpanan paralel menghasilkan dua baris 'disetujui' – budgetTotal (Σ) berbeda dari 'nilai berlaku' (terbaru)

`src/lib/finance/actions.ts:231` · sedang · data-integritas · `keuangan-82`

**Skenario.** Dua approver (atau satu kali klik ganda) menyimpan budget kategori 'material' hampir bersamaan: 200 jt dan 300 jt. Kedua transaksi menjalankan updateMany(status disetujui → batal) lalu create baris disetujui. Pada READ COMMITTED, updateMany T2 tidak melihat baris yang baru dibuat T1 (belum commit / di luar snapshot pernyataan), sehingga dua baris disetujui tersisa. calc.ts:31-35 menjumlahkan SEMUA baris disetujui → budgetTotal = 500 jt, availableBudget ikut menggelembung; sementara latestBudgetByCategory (queries.ts:27-41) hanya mengambil baris terbaru → tabel 'Budget per kategori' menampilkan 300 jt dan baris Total tabel (client:241, dihitung di komponen) = 300 jt ≠ KPI Budget 500 jt di halaman yang sama. Pola lock FOR UPDATE yang sudah dipakai pembayaran/pencairan/expense (B6) tidak diterapkan di sini. Probe integrasi untuk membuktikan balapan ini sudah ditulis tetapi belum selesai berjalan saat laporan ditutup.

**Bukti.** actions.ts:231-245 await tx.budgetLine.updateMany({ where: { locationId, category, status: "disetujui" }, data: { status: "batal" } }); return tx.budgetLine.create({ data: { ..., status: "disetujui", ... } }); calc.ts:31-35 db.budgetLine.groupBy({ ... status: "disetujui" }, _sum: { amount: true } }) queries.ts:36-39 if (!result.has(r.category)) { result.set(...) } // hanya terbaru

**Tes terkait.** Tidak ada; finance-race.test.ts hanya mencakup payment/disbursement

**Usul perbaikan.** Kunci baris (SELECT … FOR UPDATE pada budget_lines lokasi+kategori, atau pg_advisory_xact_lock(hash(locationId, category))) sebelum updateMany, ATAU tambah unique partial index (location_id, category) WHERE status='disetujui'. Samakan definisi 'nilai berlaku' di calc.ts dan queries.ts; hapus Σ di komponen (client:241).

**Reproduksi.** GAGAL (merah) – balapan terbukti nyata di Postgres. Probe sementara tests/integration/_audit-keuangan-82.test.ts (sudah DIHAPUS; `git status` bersih kecuali _audit-keuangan-81.test.ts milik agen lain yang tidak saya sentuh) menyalin PERSIS badan transaksi actions.ts:231-245 dan menjalankan dua transaksi bersamaan lewat db.$transaction. Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-keuangan-82.test.ts Keluaran: [B] baris disetujui = 200000000, 300000000 | budgetTotal(calc) = 500000000 | nilai berlaku(queries) =

### J-4. 🟡 Antrean approval /keuangan tidak difilter organisasi untuk peran lintas-lokasi (kebocoran lintas tenant, kelas B11/DECISIONS 150)

`src/lib/finance/queries.ts:113` · sedang · authz · `keuangan-78`

**Skenario.** super_admin org A membuka /keuangan. page.tsx:34 memanggil pendingApprovals(locIds) dengan locIds = accessibleLocationIds(user) = null (peran lintas-lokasi). Di queries.ts:113 null diterjemahkan menjadi locFilter = {} → query commitment/expense/invoice/ownerBilling status 'diajukan' berjalan TANPA filter package.orgId. Komitmen 'diajukan' milik org B (nomor, deskripsi, vendor, nama lokasi, nominal) tampil di antrean org A. Aksi setujui akan ditolak requireLocationAccess (cek org), tetapi daftarnya sudah bocor. Bandingkan daftar lokasi di halaman yang sama (page.tsx:26) yang memakai locationScopeWhere dan selalu memfilter orgId.

**Bukti.** queries.ts:112-117 const locFilter = locationIds === null ? {} : { locationId: { in: locationIds } }; ... db.commitment.findMany({ where: { status: "diajukan", ...locFilter }, ... queries.ts:131-137 (ownerBilling) juga `locationIds === null ? {}`. scope.ts:5-12 (komentar): "null diterjemahkan tiap halaman sebagai {} = TANPA filter apa pun, termasuk tanpa filter organisasi" – kelas cacat yang sama. DB uji saat ini berisi 14 organizations, jadi dorman single-org tidak lagi berlaku di lingkungan uji.

**Tes terkait.** Tidak ada uji untuk pendingApprovals; tests/integration/finance-race.test.ts hanya menguji apply.ts

**Usul perbaikan.** pendingApprovals menerima user dan memakai locationRelScopeWhere(user, locIds) untuk commitment/expense/invoice; untuk ownerBilling pakai contract.package.orgId = user.orgId bila locIds null. Tambah uji integrasi: komitmen diajukan di org B tidak muncul untuk pembaca org A.

**Reproduksi.** MERAH – terbukti. Uji sementara tests/integration/_audit-keuangan-78.test.ts (sudah DIHAPUS; git status bersih dari berkas saya) membuat dua organisasi di satu DB uji, satu komitmen status "diajukan" di lokasi org B, lalu memanggil pendingApprovals(await accessibleLocationIds(superAdminOrgA)). Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-keuangan-78.test.ts Keluaran: stdout | NOMOR TERLIHAT: ["RAHASIA-B-ak78mu2hpvpw"] x super_admin org A tidak boleh melihat komitmen diajukan milik org B AssertionError: expect

### J-5. 🟡 Kesiapan termin menghitung termin DRAFT sebagai 'terpakai' – ambang termin melompat sebelum termin diajukan

`src/lib/kesiapan/builder.ts:111` · sedang · bug · `keuangan-80`

**Skenario.** Kontrak baru, progress terverifikasi 30%. Finance membuat termin 1 sebagai draft (createOwnerBilling → status 'draft', belum diajukan). builder.ts:111-114 menghitung terpakai = billings dengan status != 'ditolak' → draft ikut = 1 → ambangTerminBerikutnya(1) = termin ke-2, 50%. Kartu Kesiapan Termin menampilkan 'Progress terverifikasi ≥ 50% (termin ke-2)' status gagal → verdict 'Belum siap', padahal termin ke-1 (25%) justru siap diajukan. Dengan 4 draft, kartu menulis 'Seluruh 4 termin sudah diajukan/cair' meski nol yang diajukan. Bertentangan dengan rules.ts:52 ('jumlah termin yang SUDAH diajukan/cair') dan calc.ts:133-137 yang memperlakukan draft sebagai belum menagih apa pun.

**Bukti.** builder.ts:111-114 const terpakai = billings.filter( (b) => b.contractId === pkg.contract?.id && b.status !== "ditolak", ).length; const berikut = ambangTerminBerikutnya(terpakai); rules.ts:52: /** Ambang termin berikutnya berdasar jumlah termin yang SUDAH diajukan/cair. */ calc.ts:133: if (b.status !== "draft") { cur.billed += b.amount; ...

**Tes terkait.** tests/unit/kesiapan-rules.test.ts hanya menguji fungsi murni; builder tidak diuji

**Usul perbaikan.** Saring b.status !== 'ditolak' && b.status !== 'draft' (atau eksplisit in ['diajukan','disetujui','cair_sebagian','cair']). Tambah uji builder dengan termin draft: label harus 'termin ke-1' 25%.

**Reproduksi.** MERAH – reproduksi dengan berkas sementara `tests/integration/_audit-keuangan-80.test.ts` (sudah DIHAPUS; `git status --porcelain` kosong sesudahnya). Perintah: DATABASE_URL=postgresql://marlin:marlin@localhost:5432/marlin_test APP_ENV=test pnpm vitest run tests/integration/_audit-keuangan-80.test.ts Persiapan: Org → Package(stage=pelaksanaan) → Location(berjalan) → Vendor → Contract(111 jt) → RabRevision aktif 100 jt (kategori "I" + item "I#1") → DailyReport status "disetujui" dengan valueDone 30 jt (progress terverifikasi 30%). Pemanggilan `kesiapanPortofolio(sa)` sebagai super_admin. Hasil:

### J-6. 🟢 alokasiBelumTertagih: tagihan seluruh kontrak dikurangkan dari terpasang lokasi-dalam-lingkup saja – laten selama DECISIONS 411, dan dijaga oleh tes yang menegaskannya

`src/lib/finance/calc.ts:206` · rendah · tes · `keuangan-83`

**Skenario.** Kontrak K1 dua lokasi L1 (terpasang 60) dan L2 (40), PPN 0, sudah ditagih 50. Pembaca ter-scope hanya L1 (mis. project_manager bila finance.view dipulihkan sesuai langkah di authz.ts:137-141). page.tsx:60 memotong locationIds ke lingkup → terpasangKontrak = 60; unbilledWork(60, 50) = 10 → 'Belum tertagih' L1 = Rp10. Alokasi yang benar: unbilled kontrak = 100 − 50 = 50, porsi L1 = 30. Angkanya understated 3×. Saat ini tidak terlihat karena hanya super_admin (lintas-lokasi, lingkup = seluruh org) yang punya finance.view (DECISIONS 411). Tes finance-calc.test.ts:155-165 justru menegaskan perilaku 'penyebut ikut menyusut' dengan billed=0 (di mana angkanya kebetulan sama), sehingga cacatnya terkunci.

**Bukti.** page.tsx:60 locationIds: c.package.locations.filter((l) => inScope.has(l.id)).map((l) => l.id), calc.ts:206-210 const terpasangKontrak = c.locationIds.reduce((s, id) => s + (terpasangPerLokasi.get(id) ?? 0n), 0n); const unbilled = unbilledWork(terpasangKontrak, b?.billed ?? 0n, c.ppnPercent); finance-calc.test.ts:157: // Penyebutnya pun ikut menyusut, jadi L1 menanggung sisa kontraknya sendiri

**Tes terkait.** tests/unit/finance-calc.test.ts:155-165 (menjaga perilaku salah)

**Usul perbaikan.** Hitung unbilled dan porsi atas SELURUH lokasi kontrak (terpasang semua lokasi kontrak, bukan hanya yang dalam lingkup), lalu saring hasil per lokasi ke lingkup pembaca. Ubah tes 'lokasi di luar lingkup' memakai billed > 0 supaya merah dulu.

**Reproduksi.** MERAH (unit, berkas sementara tests/unit/_audit-keuangan-83.test.ts – sudah dihapus, `git status --porcelain` bersih). Perintah: pnpm vitest run tests/unit/_audit-keuangan-83.test.ts Keluaran: stdout > "porsi L1 ter-scope = 10" × pembaca ter-scope hanya L1: porsi L1 SEHARUSNYA tetap 30 AssertionError: expected 10n to be 30n Test Files 1 failed (1) | Tests 1 failed | 2 passed (3) Tiga kasus yang dijalankan: 1. Baseline lintas-lingkup (locationIds ["L1","L2"], terpasang 60/40, billed 50, ppn 0) → porsi L1 = 30n. LULUS. 2. Lingkup dipotong pemanggil (locationIds ["L1"], data terpasang sama, bille

---

## Terbantah (9) — diperiksa, TIDAK jadi temuan

Didaftar supaya tidak diangkat lagi sebagai "bug baru" di audit berikutnya.

- **`src/lib/photo-status.ts:83`** – Daftar status "terverifikasi" laporan ditulis ulang di photo-status.ts alih-alih memakai VERIFIED_REPORT_STATUSES dari lifecycle.ts
  <br>Sebab dibantah: Penelusuran kode: fakta temuan akurat – photo-status.ts:83 `STATUS_LAPORAN_TERVERIFIKASI: DailyReportStatus[] = ["disetujui","final"]` dan lifecycle.ts:103 `VERIFIED_REPORT_STATUSES = ["disetujui","final"] as const` adalah dua literal tanpa ikatan; pemakainya photos-gallery.ts:79 (WHERE_TERVERIFIKAS
- **`src/lib/photos.ts:321`** – Sinkron dobel dari antrean luring: foto tanpa lokasi terdeteksi tidak punya dedup → dua baris + dua objek R2 saat respons hilang lalu dicoba ulang
  <br>Sebab dibantah: Penelusuran kode: skenarionya BENAR secara mekanis. `savePhotoForItem` (src/lib/photos.ts:321-327) hanya memeriksa `locationId_sha256` bila `input.locationId` terisi; unique DB `photos_location_id_sha256_key` (prisma/migrations/20260728020000_data_integrity_checks/migration.sql:42) tidak mengikat un
- **`src/lib/rab/cocok-lineage.ts:115`** – Gerbang 'beda kategori' pemetaan manual memakai akar split('#')[0]; pemetaan lintas kategori roman ganda lolos
  <br>Sebab dibantah: Fakta mekanisnya BENAR, tapi skenario kerugiannya TIDAK terjadi dan perilakunya konsisten dengan keputusan yang tercatat. 1. Penelusuran kode. `akar()` (cocok-lineage.ts:115) memang memotong 'VI#3#3' menjadi 'VI', dan `flattenParsedRab` (flatten.ts:74-83, 197) memang menghasilkan kategori kedua 'VI
- **`src/lib/rab/sesuaikan-realisasi.ts:65`** – Pembagian sisa terbesar bisa menihilkan baris laporan kecil (volumeDone 0), bertentangan dengan klaim 'tidak ada hari yang dinihilkan' dan dengan atur
  <br>Sebab dibantah: Aritmetikanya BENAR dan memang bisa direproduksi, tetapi dua premis yang dipakai menjadikannya "bug" keduanya tidak berdiri, dan sisanya adalah artefak pembulatan pada kuantum yang justru DIPILIH secara sadar. 1) PENELUSURAN KODE. Satu-satunya pemanggil adalah `sesuaikanRealisasiKeVolumeBaru` di sr
- **`src/lib/rab/adendum-template-parse.ts:261`** – Volume negatif: komentar kode & petunjuk di berkas berkata 'DITOLAK', DECISIONS 203 menolak pada impor jadwal, tetapi jalur adendum hanya memperingatk
  <br>Sebab dibantah: TERBANTAH pada intinya: temuan ini dibingkai sebagai "konflik-spesifikasi" antara kode, dokumen, dan tes – padahal keputusan yang MENGATUR jalur ini justru menetapkan persis perilaku yang sekarang, dan temuan tidak menyebutnya. 1. Spesifikasi yang berlaku = DECISIONS 502 (indeks docs/DECISIONS.md:1
- **`src/lib/ai-hub/actions.ts:405`** – transitionArtifactAction / editArtifactAction / distributeArtifactAction memuat aiArtifact tanpa cek organisasi – untuk peran lintas-lokasi `scopeCove
  <br>Sebab dibantah: Temuan ini AKURAT secara kode dan saya berhasil membuatnya MERAH, tetapi ia adalah pengangkatan-ulang temuan yang sudah DITUTUP user secara eksplisit, jadi saya nilai terbantah pada tingkat spesifikasi. 1. Keputusan yang membantah: DECISIONS 165 · 2026-07-28 · "Model deployment single-tenant + snap
- **`src/lib/export/rincian-jadwal-xlsx.ts:217`** – Time Schedule rinci: bobot & JUMLAH HARGA ditulis ulang sebagai rumus Excel dengan penyebut dan dasar yang berbeda dari angka resmi (cache TOTAL dipat
  <br>Sebab dibantah: TERBANTAH pada mekanisme yang didakwakan, sekaligus tertutup oleh keputusan yang eksplisit. 1) PENELUSURAN-KODE – premisnya tidak bisa terjadi lewat titik masuk pengguna. Skenario "kategori A Rp1.000 tetapi anak-anaknya hanya Rp600" mengandaikan Σ item ≠ Σ kategori. Satu-satunya pemuat ekspor ini,
- **`src/lib/pdf/harian-ringkas.ts:768`** – Ringkasan harian (PDF ke grup WA) mencetak “Nilai” dari Σ valueDone (harga beku saat lapor) di samping “Bobot” dari revisi aktif – dua basis dalam sat
  <br>Sebab dibantah: DIVERGENSI ANGKANYA NYATA DAN TEREPRODUKSI, TAPI TESIS "bertentangan dengan protokol" TIDAK BERTAHAN. Tiga lensa: 1) PENELUSURAN-KODE. Skenarionya benar secara mekanis: `src/lib/daily-report/ringkas.ts:291` `const nilaiHariIni = itemAktif.reduce((s, i) => s + i.valueDone, 0n)` vs `ringkas.ts:339-34
- **`src/components/knmp/kkp-period-report.tsx:114`** – Total bobot laporan periodik dijumlahkan sendiri di 4 konsumen (komponen, PDF, dua sheet Excel) – bukan dari calculation layer, dan bisa berbeda dari 
  <br>Sebab dibantah: TERBANTAH – fakta kodenya benar, tapi skenario kerusakannya tidak terjadi, sebabnya salah didiagnosis, dan perilakunya sudah jadi KEPUTUSAN eksplisit (DECISIONS 156 + 157). 1) PENELUSURAN-KODE. Fakta yang disebut temuan memang benar: `PeriodReport.totals` (src/lib/periodic-report.ts:167) hanya memu

---

## Belum diverifikasi (7)

Batas sesi memutus pembantah untuk tujuh temuan terakhir. Statusnya **mentah** –
belum dibantah, belum direproduksi, jadi belum boleh disebut temuan:

**Keuangan**
- `src/lib/finance/actions.ts:356` – status transaksi keuangan diubah langsung
  lewat `updateMany`, bukan lewat mesin transisi `lifecycle.ts` + histori
  (Prinsip WAJIB #5).
- `src/lib/kesiapan/builder.ts:290` – syarat close-out "Penyelesaian pembayaran
  owner" memakai konstanta 4 terpisah dari `AMBANG_TERMIN_PCT`, dan menghitung
  termin draft/ditolak sebagai penyebut.

**Kualitas tes**
- `src/lib/rab/import.ts:281` – duplikat B-3 (sudah terkonfirmasi lewat jalur lain).
- `tests/integration/adendum-sesuaikan-realisasi.test.ts:147` – tes "laporan FINAL
  ikut disesuaikan" memeriksa baris DB, bukan dokumen final yang jadi alasan
  DECISIONS 507 (blind spot "tes hijau di lapisan yang salah").
- `docs/decisions/501-600.md:304` – DECISIONS 507 mengubah isi laporan final,
  sementara PROJECT.md menyatakan `finalSnapshot` immutable.
- `src/app/(app)/lokasi/[slug]/rab/actions.ts:129` – duplikat B-9.
- `tests/unit/rab-add-01-template-bolak-balik.test.ts:58` – fixture sisi aktif
  menurunkan induk lewat potong-string `lineageKey`; 8 dari 1.097 node salah
  induk, bertentangan dengan rab-add-02.

---

## Cakupan yang TIDAK terjangkau audit ini

Jujur disebut supaya tidak terbaca sebagai "sudah bersih":

- Renderer PDF (`harian-kkp.ts`, `mingguan-kkp.ts`) dan Excel (`daily-xlsx.ts`)
  hanya diperiksa sebagai konsumen `KkpDailyData`, bukan isi rendernya.
- `ringkas-wa.ts`, `kkp-lampiran-susun.ts`, `kalender-harian` dari sisi layar.
- Jalur R2 pada hapus-foto dan `putarFotoAction`.
- `src/lib/paparan/`, `src/lib/gdrive/`, `src/lib/arsip-asli/`,
  `src/lib/milestones/`, cron routes – hanya tersentuh lewat lensa otorisasi.
- Ronde kedua (kritikus kelengkapan → pemeriksa lanjutan) ditiadakan karena
  batas sesi; area di atas adalah kandidat pertamanya.
