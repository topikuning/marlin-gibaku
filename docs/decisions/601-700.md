# Keputusan 601–700

Indeks judul + tanggal ada di [`docs/DECISIONS.md`](../DECISIONS.md).

---

## 601 · Kolom Jumlah di unduhan RAB jadi RUMUS (membalik 212), selisihnya dikatakan (2026-09-21)

**Keputusan**: sel `Jumlah` tiap ITEM di sheet "Detail RAB" ditulis sebagai
`=ROUND(C{baris}*E{baris},0)`, bukan angka mati. Bila hasil rekalkulasi berbeda
dari nilai tersimpan, sheet "Resume" memuat satu catatan yang menyebut
selisihnya **beserta pemilahan sebabnya**.

**Membalik DECISIONS 212**, atas perintah user:

> *"download RAB, kenapa kolom jumlah kamu hardcode? padahal kan jelas kolom
> jumlah harusnya perkalian harga satuan dan volume. ini contohnya!"*

Saya mengangkat 212 sebagai keberatan (harga satuan dibulatkan 2 desimal dari
AHSP yang lebih panjang, jadi `ROUND(vol × harga)` tidak selalu mendarat di
Jumlah dokumen). Jawaban user:

> *"kenapa ribet sekali sih, kenapa bahas ahsp. sedangkan ini adalah cuma
> download RAB aktif darimu. aku gak tau gimana caranya, yang pasti kan konyol
> kalau misal sedang cek2 lalu jumlahnya ternyata hardcode"*

Itu benar dan 212 keliru pada satu hal mendasar: berkas ini dipakai orang untuk
**memeriksa**, dan hal pertama yang diperiksa adalah apakah Jumlah memang volume
× harga satuan. Kolom berisi angka mati tidak bisa diperiksa sama sekali — ia
hanya menyuruh percaya. 212 mengorbankan justru kegunaan berkasnya demi
kecocokan angka sampai rupiah terakhir.

### Yang diukur sebelum memutuskan

Pada RAB aktif 16 lokasi, selisih `ROUND(vol × harga)` vs `amount` dipilah:

| Sebab | Baris per lokasi | Rupiah |
|---|---|---|
| `amount` 0 padahal vol & harga terisi | 3–19 | besar – purworejo **Rp 22,95 juta** |
| Pembulatan (≤ Rp 4) | 10–176 | receh |
| Selisih > Rp 4 | 38–158 | **Rp 2.292 – 9.795** per lokasi |

Jadi keberatan 212 nyata tapi kecil: di luar baris ber-Jumlah 0, seluruh
pergeseran hanya Rp 2–10 ribu pada kontrak miliaran. Dan baris ber-Jumlah 0
justru membuktikan sebaliknya — di situ angka mati mencetak **Rp 0** untuk
pekerjaan yang volume dan harga satuannya jelas terisi; rumus lebih benar
daripada angka tersimpan. (Datanya sendiri dicatat sebagai DATA-JUMLAH-NOL di
`OPEN_ISSUES.md`.)

### Catatan selisih DIPILAH, bukan digeneralisasi

Percobaan pertama saya menulis satu kalimat penyebab: *"harga satuan dicatat 2
desimal"*. Pada purworejo itu **salah**: Rp 22,9 juta dari Rp 22,96 juta berasal
dari 19 baris ber-Jumlah 0, bukan dari pembulatan. Catatan yang menyebut sebab
yang keliru lebih buruk daripada tidak ada catatan — ia mengarahkan orang
memeriksa tempat yang salah. Sekarang keduanya dihitung terpisah dan disebut
terpisah, lengkap dengan cacah barisnya.

### Yang TIDAK ikut berubah

- **Baris induk** tetap menjumlah sel anak (`F7+F8+…`), bukan mengulang
  perkalian. Subtotal tersimpan memang persis Σ anak.
- **Item tanpa volume/harga satuan** tetap angka mati: tidak ada yang bisa
  dikalikan, dan `ROUND(0*0,0)` cuma mengarang perkalian tanpa dasar di dokumen.
  Pada data nyata 348 baris begini, semuanya bernilai 0.
- **Template adendum** (`adendum-template-xlsx.ts`) kolom `Jumlah Kontrak`
  SENGAJA tetap angka mati. Di sana kolom itu bukan hitungan untuk diperiksa
  melainkan REKAMAN kontrak berjalan, dan kolom `Selisih` = Jumlah Adendum −
  Jumlah Kontrak. Membuatnya rumus akan memunculkan selisih adendum palsu
  beberapa rupiah pada baris yang volumenya sama sekali tidak berubah.
- `result` tiap sel rumus tetap angka tersimpan DB, jadi sebelum Excel
  merekalkulasi berkas dan layar menyebut angka yang sama.

### Cara membuktikannya

RAB aktif `purworejo` (2.197 simpul) diekspor ulang: 2.132 sel Jumlah berumus,
66 angka mati (yang memang tanpa volume/harga). Baris 7 berbunyi
`=ROUND(C7*E7,0)` dengan cache 73.791.470 untuk volume 50 × Rp 1.475.829,39.
Catatan Resume-nya berbunyi: *"…menjadi Rp 22.961.502 lebih tinggi daripada
nilai tersimpan Rp 11.979.698.983. Penyebabnya: 19 baris yang Jumlah-nya tercatat
0 di berkas sumber padahal volume dan harga satuannya terisi (Rp 22.954.856) –
ini yang perlu diperiksa; 288 baris selisih pembulatan (Rp 6.646)…"*. Pada berkas
yang bulat, catatan itu tidak muncul sama sekali.

**Berkas**: `src/lib/export/rab-xlsx.ts`.
**Uji**: `tests/unit/rab-unduhan-jumlah-berumus.test.ts` (merah dulu, 7 uji);
`tests/unit/rab-xlsx.test.ts` dua uji dibalik mengikuti keputusan baru.

---

## 602 · Riwayat input per item pekerjaan – "diinput kapan saja", satu ketukan dari angkanya (2026-09-21)

**Keputusan**: layar baru `/lokasi/[slug]/rab/riwayat` — cari satu item
pekerjaan, lihat SELURUH tanggal item itu dilaporkan beserta **kapan barisnya
benar-benar diketik**, pelapornya, dan status laporannya. Angka realisasi di
daftar peringatan pratinjau impor adendum MENAUTKAN langsung ke sana.

### Sebabnya

Laporan user, sambil memotret peringatan *"7 item volumenya turun DI BAWAH yang
sudah dikerjakan … sudah dikerjakan 51.6"*:

> *"untuk kasus seperti ini, bagaimana user tau kapan pekerjaan itu diinput?
> akan konyol kalau harus cek hari per hari. kamu seharusnya ada fitur cari item
> pekerjaan diinputnya kapan saja"*

Betul, dan tidak ada jalannya sama sekali sebelum ini. Angka "51,6" adalah hasil
`groupBy` lintas seluruh laporan harian; satu-satunya cara melacaknya adalah
membuka laporan hari per hari sampai ketemu. Pada kontrak 150 hari itu bukan
pemeriksaan.

### DUA tanggal, dan bedanya yang justru dicari

- `tanggal` — TANGGAL KERJA (`reportDate`, kolom `@db.Date`).
- `diinputPada` — kapan barisnya diketik (`DailyReportItem.createdAt`).

Menyamakan keduanya menyembunyikan kasus yang paling perlu dilihat: baris
bertanggal minggu lalu yang baru diketik hari ini, tepat sebelum adendum
diajukan. User menanyakan "diinput kapan" dengan kata-katanya sendiri, jadi yang
dijawab harus tanggal INPUT. Layar menandai selisih ≥ 2 hari dengan kalimatnya
sendiri (*"4 hari sesudah tanggal kerjanya"*) — pada data uji semua baris
terbukti diketik 2–5 hari setelah tanggal kerjanya, dan itu langsung terlihat.

### Pilihan yang diambil, dan alasannya

- **Digerakkan URL** (`?q=` dan `?item=`), bukan state lokal: hasilnya bisa
  ditempel di chat, DAN daftar peringatan pratinjau impor bisa menunjuk langsung
  ke item yang dipersoalkan tanpa menyuruh orang mengetik ulang kodenya.
- **Saringan status sama persis dengan `cumulativeVolumeByLineage`.** Kalau
  berbeda, layar ini akan menampilkan jejak yang jumlahnya TIDAK sama dengan
  angka yang membuat orang datang ke sini — riwayat yang tidak menjumlah ke
  angka yang dipersoalkan menambah kebingungan, bukan menghapusnya.
- **Baris `draft_adendum` ikut tampil** dengan penandanya (totalnya tetap dari
  basis `aktif` saja): "kenapa angkanya begitu" kerap terjawab justru oleh baris
  yang tidak masuk hitungan resmi.
- **Nama pelapor dari `DailyReportItem.reportedById` dulu**, baru pengirim/
  pembuat laporannya. Satu laporan harian bisa diisi beberapa orang, dan memakai
  pengirim laporan untuk semua barisnya akan menyebut nama yang salah.
- **Item yang sudah DIHAPUS lewat adendum tetap punya riwayat**, memakai nama
  yang tercatat di laporan hariannya. Itu justru kasus yang paling perlu
  dilihat: pekerjaan yang sudah dikerjakan lalu hilang dari kontrak.
- Riwayat KOSONG ≠ `null`: kosong = item ada tapi belum dikerjakan, `null` =
  itemnya memang tidak ada. Menyamakannya membuat layar bilang "tidak ditemukan"
  untuk pekerjaan yang jelas ada di kontrak.

### Cara membuktikannya

Dibuka di browser pada lokasi seed Kedung Mutih: daftar menampilkan 3 item
berealisasi urut laporan terakhir; membuka *Buat Bedeng Pekerja Dan Gudang*
menampilkan tiga barisnya (18/16/15 Sep 2026, masing-masing 1 m², Rp 1.475.829),
kolom DIINPUT berbunyi 19 Sep 2026 untuk ketiganya dengan penanda "2/4/5 hari
sesudah tanggal kerjanya", pelapor "Paijo Sutrisno", dan status Dikirim/
Disetujui/Final.

**Berkas**: `src/lib/rab/riwayat-item.ts` (baru),
`src/app/(app)/lokasi/[slug]/rab/riwayat/page.tsx` + `form-cari.tsx` (baru),
`.../rab/import/panel-beda.tsx` (angka realisasi jadi tautan),
`.../rab/import/import-form.tsx`, `.../rab/page.tsx` (tombol masuk).
**Uji**: `tests/integration/riwayat-input-item.test.ts` (merah dulu, 14 uji);
satu uji tautan di `tests/unit/pratinjau-beda-adendum.test.tsx` — dibuktikan
merah dengan mengembalikan `panel-beda.tsx` ke versi lama, bukan diasumsikan.

### Dua pagar yang menangkap halaman baru ini – dan satu uji rapuh yang ikut dibetulkan

`tests/unit/rute-terjaga.test.ts` menolak halaman baru yang tidak terdaftar di
sapuan overflow mobile, dan `penanda-sibuk` menolak tombol submit tanpa keadaan
sibuk. Keduanya benar: rutenya didaftarkan, dan tombol Cari memakai
`useTransition` (render server halaman ini memang lama pada lokasi ribuan baris,
dan tombol yang diam membuat orang menekannya berkali-kali).

Selain itu `tests/integration/arsip-sudah-di-mesin.test.ts` gagal sesekali di
suite penuh (`menunggu` terbaca 3, diharapkan 2) tetapi hijau sendirian.
Sebabnya bukan kebetulan: `ringkasArsipAsli()` menghitung SELURUH tabel foto —
memang begitu maunya, layar Sistem melaporkan keadaan sistem — sedangkan
`afterAll` berkas itu hanya menghapus foto lokasinya sendiri, jadi sisa berkas
lain (atau run sebelumnya yang mati di tengah) ikut terhitung. Dibetulkan dengan
mengukur SELISIH terhadap keadaan sebelum fixture, plus TRUNCATE di `afterAll`
seperti berkas integrasi lain. Uji yang sesekali merah akan dimatikan orang, dan
yang hilang bersamanya bukan cuma uji itu.

---

## 603 · Tombol dalam satu baris aksi seragam: variant DISALIN dari tetangganya (2026-09-21)

**Teguran user 2026-09-21**, sambil memotret baris aksi layar RAB:

> *"KAMU INI GOBLOK YA! aku sudah pernah bahas, soal tombol. bukankah sudah
> distandarkan, kenapa ini masih aja modelnya gak jelas"*

Benar. Tombol "Riwayat input item" yang baru saya tambahkan (DECISIONS 602)
memakai `variant="ghost"` dan ikon `size-3.5`, sementara tiga tetangganya
`secondary`/`primary` dengan ikon `size-4`. `ghost` = tanpa bingkai +
`text-ink-muted`, jadi di antara tombol berbingkai ia **terbaca MATI** — tombol
yang paling baru justru yang paling tidak terlihat bisa ditekan.

DECISIONS 592 sudah menetapkan bahwa **tata letak baris aksi dimiliki barisnya**,
tetapi hanya mengatur jarak dan tinggi. Yang tidak tertulis: **variant dan
ukuran ikon pun milik baris, bukan pilihan tiap tombol.** Sekarang tertulis, di
`CLAUDE.md` §Aturan Coding — tempat yang memang dibaca sebelum menulis kode:

> Satu aksi utama boleh `primary`; sisanya `secondary`. `ghost` HANYA untuk aksi
> bawahan di tepi baris (Batal, ← Kembali), tidak pernah untuk aksi setara.

### Kenapa TIDAK ada pemindai untuk ini

Saya menulis dan menjalankan DUA rumusan penjaga atas seluruh `src/**/*.tsx`:

1. *"`ghost` tidak boleh terjepit di antara tombol berbingkai"* → satu tuduhan
   di luar berkas saya: `sistem/penyimpanan-panel.tsx`. Diperiksa: **palsu**.
   Di situ yang ada `[Ya, hapus sekarang] [Batal]` di dalam satu `div`, dan
   `secondary` yang terbaca sebagai tombol ketiga berasal dari cabang lain
   sebuah ternary — regexnya yang melompati batas blok.
2. *"ukuran ikon harus seragam dalam satu baris"* → satu tuduhan:
   `laporan-lokasi/aksi-harian.tsx`. Diperiksa: **palsu**. `size-4` di situ ikon
   TOMBOL PEMICU menu, `size-3.5` ikon ITEM-ITEM menunya — dua konteks berbeda
   yang kebetulan berdekatan di sumber.

Dua-duanya menuduh kode yang benar. Pelajaran yang sudah mahal sekali di sesi
ini (penjaga borang bersarang, DECISIONS 598) berlaku lagi: **penjaga yang
menuduh yang benar akan dimatikan orang, dan yang hilang bersamanya bukan cuma
penjaga itu.** Batas antara "aksi setara" dan "aksi bawahan" adalah penilaian,
bukan pola teks — memaksakannya jadi regex berarti menukar satu cacat rupa
dengan penjaga yang tidak dipercaya. Jadi aturannya ditulis, penjaganya tidak.

### Cara membuktikannya

Barisnya dibuka di browser dan DILIHAT: empat tombol kini sebingkai dan
seukuran — `Unduh Excel`, `Adendum`, `Riwayat input item` (ketiganya
`secondary`), lalu `Impor` (`primary`) sebagai satu-satunya aksi utama.

**Berkas**: `src/app/(app)/lokasi/[slug]/rab/page.tsx`, `CLAUDE.md`.

---

## 604 · Kolom yang di-hide tidak pernah dipakai — aturan baris berlaku juga untuk KOLOM (2026-09-21)

**Teguran user 2026-09-21**, dengan tangkapan layar berkasnya:

> *"import rab/adendum, kenapa kamu baca dari kolom yang di hide. TOLOL!"*
>
> *"ini kolomnya, kenapa hal yang jelas, aku bilang kalau di-hide jangan
> digunakan."*

Benar, dan aturannya memang SUDAH ada — hanya tidak pernah diberlakukan untuk
kolom. Sheet tersembunyi disaring sejak `namaSheetXlsx`; baris tersembunyi
diabaikan DAN dilaporkan sejak permintaan user 2026-09-09. Kolom: tidak sama
sekali.

Akibatnya pada berkas *Perubahan Volume Kedungrejo REVISI 19092026.xlsx*
pratinjau berbunyi **"kolom harga PENAWARAN (kolom M/N)"** — padahal di berkas
itu kolom K–N DISEMBUNYIKAN (judul kolom melompat dari J ke O). Blok yang
terlihat hanya "Kontrak" (E–H) dan "MC-0" (O–P). Nilai kontrak diambil dari blok
yang penyusunnya sendiri sudah membuang.

### Yang diperbaiki

1. **`kolomTersembunyi(ws)`** — `col.hidden === true || col.width === 0`.
   Lebar nol ikut, karena sebagian berkas menyembunyikan kolom dengan
   menyeretnya, bukan lewat menu Hide; bagi yang melihat layar keduanya sama.
   Cadangan defensif yang sama sudah dipakai untuk baris (`row.height === 0`).
2. **Kolom tersembunyi dicoret dari SELURUH pemilihan peran**, bukan hanya
   kolom harga: blok yang disembunyikan membawa VOL dan SAT-nya sendiri, dan
   mengambil volume dari blok yang dibuang sama salahnya.
3. **Blok tersembunyi jadi BATAS, bukan pewaris nama.** Ini yang hampir
   terlewat: `blockAt(c)` menelusur ke KIRI mencari label blok terdekat, jadi
   sesudah perbaikan (1)+(2) kolom O/P yang terlihat tetap memungut label
   "PENAWARAN" milik blok tersembunyi di sebelah kirinya — kolom yang benar
   dilaporkan dengan nama blok yang salah. Sekarang penelusuran BERHENTI di
   kolom tersembunyi pertama.
4. **Dikatakan, tidak didiamkan.** Pratinjau menyebut berapa blok harga
   disembunyikan, namanya, kolom terlihat mana yang dipakai sebagai gantinya,
   dan apa yang harus dilakukan kalau blok itu memang yang dimaksud
   ("tampilkan dulu kolomnya di Excel lalu impor ulang"). Melewati blok
   diam-diam membuat user membaca angka yang benar dengan alasan yang keliru.

### Catatan jujur soal ujinya

Fixture uji pertama saya TIDAK mereproduksi keluhannya: tanpa disembunyikan pun
`priceSource` terbaca `hps`, bukan `penawaran`, karena `BLOCK_RE` menuntut DUA
label blok pada baris grup sementara fixture saya cuma punya satu. Lima uji
"sesudah disembunyikan" lolos — tapi hijau karena alasan yang salah, bukan
karena kodenya benar. Karena itu berkas uji ini membawa satu uji DASAR yang
menuntut keadaan yang dikeluhkan benar-benar terjadi dulu (`penawaran`, kolom
13 = M, persis seperti di layar user). Uji yang hijau tanpa pernah merah untuk
alasan yang benar bukan bukti.

**Berkas**: `src/lib/rab/hps-parser.ts`.
**Uji**: `tests/unit/kolom-tersembunyi-diabaikan.test.ts` (merah dulu, 8 uji).

---

## 605 · Item laporan harian menyebut kategori & sub-kategorinya (2026-09-23)

**Laporan user 2026-09-23**, memotret laporan yang sudah disetujui:

> *"itu ada item pekerjaan, tapi masuk kategori atau sub kategori apa tidak
> diketahui, padahal waktu proses input ada. ini penting untuk dapat sekali
> lihat tanpa harus buka-buka laporan lain lagi"*

Layar mencetak `code` apa adanya: barisnya berbunyi **"1"**. Nomor item hanya
unik DI DALAM induknya, jadi "1" ada di setiap kategori — yang memeriksa harus
membuka layar lain untuk tahu pekerjaan mana yang dimaksud.

Ini keluhan yang **sama** dengan 2026-09-05 (*"2.d, 2.e itu yang mana, ada
banyak kategori di sini, seharusnya sekalian sebutkan parentnya, kalau gak gitu
kan konyol"*). Waktu itu diperbaiki hanya di pratinjau impor RAB, dan layar
laporan harian tidak ikut. Blanko KKP cetak sudah benar sejak 2026-08-02
(judul kategori disisipkan saat kategorinya berganti) — yang tertinggal justru
layarnya.

### Dua bentuk, dua kegunaan

- `jalurKode` — rantai kode termasuk kodenya sendiri (`II · 1 · 1`). Ini yang
  dicocokkan orang dengan dokumen kontrak dan RAB.
- `jalurNama` — rantai NAMA induknya saja (`PEKERJAAN REVETMENT › Pekerjaan
  Tanah`), TANPA nama itemnya sendiri: nama item sudah tercetak besar di
  barisnya, dan mengulanginya memakan tempat yang justru dipakai menjawab
  pertanyaan user.

Kode saja tidak menjawab *"kategori apa"*; nama saja tidak bisa dicocokkan
dengan dokumen kontrak. Karena itu keduanya, bukan salah satu.

### Dicari per NODE ID, bukan per `lineageKey`

`DailyReportItem.rabNodeId` menunjuk node pada revisi tempat barisnya dibuat,
yang bisa sudah `digantikan` oleh adendum. Menelusur dari node itu sendiri
memberi kategori yang benar-benar berlaku ketika pekerjaan itu dilaporkan, dan
tetap bekerja untuk lineage yang sudah tidak ada di revisi aktif. Id yang tidak
ditemukan cukup ABSEN dari peta, bukan lemparan: satu baris tanpa jalur tidak
boleh merobohkan seluruh layar.

Seluruh simpul revisi terkait diambil SEKALI, bukan satu query per tingkat —
laporan harian bisa memuat puluhan item, dan menelusur induk satu per satu
berarti puluhan bolak-balik ke DB untuk menghias satu tabel.

### Cara membuktikannya

Dibuka di browser pada laporan seed Kedung Mutih 18 Sep 2026: dua barisnya kini
berbunyi *"Buat Bedeng Pekerja Dan Gudang / I · 1 / PEKERJAAN PERSIAPAN"* dan
*"Pekerjaan Sewa Rumah untuk Direksi Keetz / I · 2 / PEKERJAAN PERSIAPAN"*.
Uji integrasinya memakai DUA item yang sama-sama berkode "1" di kategori
berbeda — bentuk yang persis membuat layar lama tak terbaca.

**Berkas**: `src/lib/rab/jalur.ts` (baru), `src/lib/daily-report/queries.ts`,
`src/app/(app)/lokasi/[slug]/harian/[date]/page.tsx`.
**Uji**: `tests/integration/jalur-item-laporan.test.ts` (merah dulu, 8 uji).

---

## 606 · Volume/jumlah negatif ditolak MARLIN, bukan oleh Postgres (2026-09-23)

**Laporan user 2026-09-23**, mengunggah `MC 1 BETAH WALANG REVISI BBPI TS
FINAL 2.xlsx` ke impor adendum:

> *"aku import untuk adendum / Gagal mengirim – server menolak permintaan ini…
> Error: An unexpected response was received from the server. / kenapa ini"*

### Apa yang sebenarnya terjadi

Parsernya sehat: 1,4 detik, sheet `RAB`, volume dari blok `CCO - 01` (kolom S),
harga satuan dari blok `MC - 0` (kolom I), 650 node. Yang tumbang penulisannya.

Tepat **satu** node bernilai negatif: `1.4 "Pekerjaan Bekesting Pasangan
Batako"`, volume −42, jumlah −7.511.351. Berkas ini berformat **tambah-kurang**
— kolom penanda memuat TAMBAH / KURANG / TETAP, dan item yang sama muncul tiga
kali (baris 134 TAMBAH +20,4; baris 191 KURANG −42; baris 220 TETAP 4,8).
Blok KURANG menulis pekerjaan-kurang sebagai angka minus.

Saat disimpan, node itu melanggar check constraint
`rab_nodes_amounts_nonneg_ck`. `DriverAdapterError` mentahnya lolos dari server
action, dan Next menjawab browser dengan respons bukan-aksi. Yang dibaca user
karena itu bukan kalimat MARLIN sama sekali — melainkan *"An unexpected
response was received from the server"*, yang tidak menyebut berkas, baris,
maupun sebabnya. Berkas yang tidak layak disimpan adalah keadaan yang **sah**;
galat yang tidak bisa dibaca tidak.

### Aturannya sudah ada — penerapannya yang bolong

Jalur **template adendum** sudah menolak volume negatif sejak dulu, dengan
kalimat yang tepat: *"Volume tidak bisa negatif – pekerjaan-kurang dinyatakan
dengan MENURUNKAN volume."* Jalur **parser HPS** tidak pernah ikut. Pagar yang
ditambahkan di sini menyamakan keduanya, dan diletakkan di titik yang sama
dengan pagar antar-layer `bedaAntarLayer`: tempat angka berhenti jadi bacaan
dan mulai jadi nilai kontrak.

### DITOLAK, bukan dibetulkan diam-diam

Tidak dijadikan 0, tidak di-mutlak-kan, tidak dijumlahkan dengan baris TAMBAH/
TETAP-nya. Angka yang diunggah user dipakai apa adanya (DECISIONS 203), dan
menebak maksud tanda minus berarti menulis nilai kontrak dari tafsiran MARLIN.
Pesannya menyebut **barisnya** (kode, nama, volume, jumlah — maksimal 8, sisanya
dihitung), menyebut cara menyatakan pekerjaan-kurang yang benar, dan menyebut
kemungkinan penyebabnya (berkas berformat tambah-kurang; yang perlu diimpor
blok HASIL-nya).

### Ditolak SEJAK PRATINJAU

Pagar ini berlaku di kedua langkah, jadi penolakannya muncul begitu berkas
diunggah — bukan setelah user menekan "Simpan" dan menunggu berkas 4 MB
diproses untuk kedua kalinya. Tidak ada draft yang terlanjur dibuat.

**Catatan terbuka**: apakah berkas tambah-kurang seperti ini suatu saat harus
dibaca MARLIN sebagai TAMBAH+KURANG+TETAP yang dijumlahkan jadi satu volume
hasil, adalah keputusan MODEL DATA — belum diambil, dan tidak boleh diambil
sepihak.

**Berkas**: `src/app/(app)/lokasi/[slug]/rab/import/actions.ts`.
**Uji**: `tests/integration/impor-kkp-tambah-kurang.test.ts` (merah dulu),
fixture `tests/fixtures/mc1-betah-walang-cco01.xlsx`.

---

## 607 · Tab basi dikenali dari UMUR server, bukan dari kalimat galatnya (2026-09-23)

**Laporan user 2026-09-23**, beberapa menit sesudah rilis 606 masuk `main`:

> *"Gagal mengirim – server menolak permintaan ini… Error: An unexpected
> response was received from the server."*

dan di log server, sembilan kali beruntun:

> *"Failed to find Server Action `6090da7f…`. This request might be from an
> older or newer deployment."*

### Penjaganya sudah ada, dan tetap tidak kena

DECISIONS 292 sudah memasang pagar untuk keadaan ini: ID server action di-hash
per build, jadi sesudah deploy, halaman yang telanjur terbuka membawa ID yang
tidak ada lagi. Pagar itu mengenalinya dari **kalimat** galat
(`UnrecognizedActionError`, *"was not found on the server"*, *"Failed to find
Server Action"*).

Masalahnya: kalimat itu hanya ada di **LOG SERVER**. Yang sampai ke browser
kalimat generik Next — *"An unexpected response was received from the server"* —
sama persis dengan kegagalan transport lain. Jadi pagar yang dibuat khusus untuk
kasus ini justru tidak pernah kena pada bentuknya yang paling sering terjadi,
dan user disuruh *"coba tekan lagi"* untuk sesuatu yang tidak akan pernah
berhasil sebelum halamannya dimuat ulang. Sembilan baris log itu sembilan kali
menekan tombol.

### Pembeda yang benar-benar ada di browser: UMUR

ID aksi hilang tepat ketika server dimulai ulang dengan build baru. Maka:
**server yang sudah jalan lebih SEBENTAR daripada umur halaman ini bukan server
yang mengirim halaman ini.**

- `/api/health` mengabarkan `uptimeMs` (`process.uptime()`).
- Browser membandingkannya dengan `performance.now()` — umur dokumen ini.

Dua DURASI, masing-masing diukur di mesinnya sendiri. Tidak ada satu pun JAM
yang dibandingkan, jadi jam ponsel lapangan yang meleset — hal biasa — tidak
bisa membuat pagar ini salah tuduh. Dibaca ketat (`<`, bukan `<=`).

### Yang sengaja TIDAK dilakukan

- **Tidak menebak saat umur server tak diketahui.** Selama deploy berjalan,
  versi lama masih melayani sebagian permintaan dan balasannya belum memuat
  `uptimeMs`; di situ perilakunya kembali persis seperti sebelum pagar ini ada.
- **Tidak memuat ulang sendiri.** Isian yang belum terkirim ada di layar itu;
  memuat ulang tanpa diminta membuangnya. Yang dilakukan menyebut keadaannya
  dan menyuruh mencatat dulu.
- **Restart biasa (build sama) ikut kena.** Diterima: di situ pun memuat ulang
  tetap jalan keluar yang benar, dan salah-tuduhnya cuma terjadi sesudah sebuah
  kiriman memang gagal.

**Berkas**: `src/lib/aksi-klien.ts`, `src/app/api/health/route.ts`.
**Uji**: `tests/unit/tab-basi-umur-server.test.ts` (merah dulu, 7 uji).

---

## 608 · Pesan penolakan menyebut BARIS Excel-nya, bukan cuma nama item (2026-09-23)

**Permintaan user 2026-09-23**, sesudah membaca pesan penolakan volume negatif
(DECISIONS 606):

> *"lalu penunjuk item juga perlu informasi rows berapa agar spesifik masalah
> di filenya yang mana"*

Benar, dan berkas yang memicunya membuktikannya sendiri. Di `MC 1 BETAH WALANG`,
nama *"Pekerjaan Bekesting Pasangan Batako"* muncul **sembilan kali** di sheet
RAB — baris 940, 976, 1058, 1091, 1100, 1177, 1199, 1288, dan 191 — dan yang
bermasalah cuma SATU: baris 191, blok KURANG, volume −42. Kode itemnya pun tidak
menolong: kode hanya unik DI DALAM induknya, jadi "d" ada di mana-mana.

Pesan yang menyebut nama tanpa baris berbunyi seperti petunjuk padahal bukan: ia
menyuruh orang menyisir 1.300 baris untuk menemukan mana yang dimaksud. Nomor
baris satu-satunya penunjuk yang bisa langsung diketik ke kotak "Go To" Excel.

### Dibawa dari parser, bukan dihitung belakangan

`ParsedRabItem.excel_row` diisi `mkItem` dari `row.number` — satu-satunya tempat
yang masih memegang sheet-nya — lalu diteruskan ke `FlatNode.excelRow`. Sesudah
`flatten`, baris aslinya tidak ada lagi di mana pun: yang tersisa cuma pohon
kode. Kategori dan sub sengaja bernilai `null`; yang ditunjuk orang saat
membetulkan angka selalu ITEMnya.

Jalur template adendum ikut mengisinya (`excelRow: r`), supaya kedua jalur impor
memberi petunjuk yang sama bentuknya.

### Dipakai di dua pesan

- Penolakan volume/jumlah negatif (DECISIONS 606).
- Peringatan "punya volume & harga satuan tetapi kolom JUMLAH kosong" — sama
  saja: ia menyuruh membuka Excel dan mengisi sesuatu, jadi tanpa nomor baris
  perintahnya tidak bisa dijalankan.

**Berkas**: `src/lib/rab/parsed.ts`, `src/lib/rab/hps-parser.ts`,
`src/lib/rab/flatten.ts`, `src/lib/rab/adendum-template-parse.ts`,
`src/app/(app)/lokasi/[slug]/rab/import/actions.ts`.
**Uji**: `tests/unit/baris-excel-disebut.test.ts` (merah dulu, 4 uji) +
`tests/integration/impor-kkp-tambah-kurang.test.ts` (menuntut "baris 191").
**Dilihat di layar**: pratinjau Kedung Mutih berbunyi *"Impor dihentikan: 1
baris ber-volume/jumlah NEGATIF, yaitu baris 191 – 1.4 "Pekerjaan Bekesting
Pasangan Batako" (volume -42, jumlah -7.511.351)…"*.

---

## 609 · Layar lokasi bertanya ke resolver grup, bukan ke kolom paket (2026-09-24)

**Laporan user 2026-09-24**, di tab Laporan sebuah lokasi:

> *"laporan yang final disajikan, atas lokasi itu group wa kabupatennya sudah
> ditentukan, tapi kirim laporan by wa tidak aktif tombolnya, memang group
> paket tidak di isi, tapi group wa kabupaten sudah"*

Persis kegagalan yang **diramalkan DECISIONS 596** ketika `grupUntukLokasi`
dibuat:

> *"Sebelum ini jawabannya disalin di belasan tempat sebagai
> `lokasi.package.waGroupId` — dan selama hanya ada satu jawaban, menyalinnya
> tidak terasa salah. Begitu grup kabupaten ada, tiap salinan jadi satu tempat
> yang bisa ketinggalan."*

Aksi kirimnya sudah diperbaiki waktu itu; **pagar TOMBOLnya tidak**. Tiga layar
masih menghitung sendiri "lokasi ini punya grup?" dari `package.waGroupId`:
tab Laporan, Laporan lengkap, dan Kegiatan Lapangan. Akibatnya lokasi yang grup
kabupatennya sudah dipasang melihat tombol **mati** untuk kiriman yang
sebenarnya akan berhasil — kegagalan yang paling sulit dilaporkan, karena tidak
ada pesan galat apa pun untuk dikutip.

### Yang diperbaiki

Ketiga layar memanggil `grupUntukLokasi(location.id)`. Pagar barunya juga
menjaga aturannya, bukan ketiga berkas itu saja: `tests/unit/
layar-lokasi-tanya-resolver-grup.test.ts` menolak SETIAP berkas di bawah
`app/(app)/lokasi/` yang menyentuh `waGroupId` sendiri. Halaman Paket tetap
boleh — di situlah kolom itu memang disunting.

### Kalimat yang menyebut tujuan SALAH ikut dibetulkan

Satu kelas yang sama, ditemukan saat melihat layarnya:

- KPI "Sudah ke WhatsApp" bersubjudul *"Kabar ke grup paket"* → memakai
  `grup.label`, jadi kini berbunyi *"Kabar ke grup WhatsApp kabupaten Demak"*.
- Pesan sukses kirim laporan lengkap selalu menyebut nama grup PAKET, walau
  kirimannya masuk ke grup kabupaten → memakai `grup.label` + nama sebenarnya.
- Tombol kembali di form kegiatan (*"Kirim ke grup paket"*) dan dialog
  konfirmasi di status harian (*"langsung masuk ke grup paket"*) → tidak lagi
  menyebut "paket" untuk tujuan yang belum tentu paket.

Kalimat yang menyebut tujuan yang salah untuk kiriman yang benar sama
merugikannya dengan tombol mati: pembacanya tidak punya cara tahu.

### Cara membuktikannya

Keadaan yang dilaporkan dibuat ulang di basis data dev — grup kabupaten "KNMP
Demak" dipasang ke lokasi Kedung Mutih, `Package.waGroupId` DIKOSONGKAN — lalu
ketiga layar dibuka di browser: tombol WA hidup di ketiganya, dan KPI-nya
berbunyi *"Kabar ke grup WhatsApp kabupaten Demak"*.

**Berkas**: `src/app/(app)/lokasi/[slug]/laporan-lokasi/page.tsx`,
`.../laporan-lengkap/page.tsx`, `.../kegiatan/page.tsx`,
`.../kegiatan/kegiatan-forms.tsx`,
`src/app/(app)/laporan/status-harian/tabel-status.tsx`,
`src/lib/lokasi-lengkap/actions.ts`.
**Uji**: `tests/unit/layar-lokasi-tanya-resolver-grup.test.ts` (merah dulu).

---

## 610 · Akun Executive View disembunyikan dari panel Akses lokasi (2026-09-24)

**Ketetapan user 2026-09-24**:

> *"hanya super admin dan PD yang boleh tahu siapa saja yang ditugaskan di
> paket itu untuk login eksekutif."*

"Hanya Super Admin + Program Director" persis capability **`user.manage`**.

### Halaman Pengguna sudah memenuhinya — tidak disentuh

`user.manage` melihat seluruh akun organisasi; peran lain hanya melihat akun
**yang ia buat sendiri**, dan AM/PM/SM tidak pernah boleh membuat akun
Executive View (`ROLE_CREATE_MATRIX`). Jadi jalur itu memang sudah tertutup.

### Yang TIDAK memenuhinya: panel Akses di halaman lokasi

Panel dari DECISIONS 571 mendaftar semua yang punya akses ke lokasi **lengkap
dengan perannya**, dan ditampilkan kepada siapa pun yang bisa membuka lokasi
itu — Site Manager, Pelaksana, Wakil PPK. `user.manage` hanya membatasi tombol
KELOLAnya, bukan daftarnya.

Dan akun Executive View pasti ada di daftar itu bila ia memantau lokasi
tersebut: `exec_viewer` sengaja BUKAN peran lintas-lokasi (DECISIONS 190), jadi
satu-satunya cara ia melihat lokasi adalah DITUGASKAN ke lokasi itu.

### Disaring di server

`aksesLokasi(locationId, orgId, { sembunyikanEksekutif })`. Barisnya tidak ikut
terkirim ke klien — CLAUDE.md: *"frontend hanya menyembunyikan menu"*.
Bawaannya TIDAK menyembunyikan: pemanggil wajib menyatakan niatnya, supaya
layar Super Admin tidak pernah diam-diam memotong daftarnya sendiri.

### Catatannya TETAP, tidak menghitung

Panel menulis *"Akun Executive View tidak ditampilkan di daftar ini."* — **baik
ada maupun tidak ada** akunnya. Dua bahaya yang saling berlawanan ditutup
sekaligus:

- Diam sama sekali membuat daftar yang kurang terbaca **lengkap** — persis yang
  diperingatkan doc `akses-lokasi.ts` sendiri.
- Menyebut JUMLAH yang disembunyikan membocorkan justru yang disembunyikan:
  ada atau tidak ada eksekutif di lokasi ini. Karena itu kalimatnya tetap,
  bukan hitungan (menyimpang dari kebiasaan "sebut jumlahnya" di CLAUDE.md,
  dan alasannya ditulis di sini supaya tidak "diperbaiki" kembali).

### Yang TIDAK dikerjakan, atas perintah user

- Wewenang membuat/menugaskan akun eksekutif **tidak diubah** sama sekali.
- Tidak ada panel baru di halaman Paket ("cukup di halaman Pengguna").
- Celah kedua yang ditemukan dan **dibiarkan**: `setUserRole` tidak menyentuh
  `createdById`, jadi akun yang dibuat Site Manager lalu dijadikan Executive
  View oleh Super Admin tetap muncul di daftar Pengguna si pembuat.

**Berkas**: `src/lib/users/akses-lokasi.ts`,
`src/app/(app)/lokasi/[slug]/page.tsx`, `.../akses-panel.tsx`.
**Uji**: `tests/integration/akses-lokasi-eksekutif.test.ts` (merah dulu, 5 uji).
**Dilihat di layar**: lokasi Kedung Mutih dibuka sebagai `admin` (baris
"Pengawas KKP · Executive View" ada) dan sebagai `sm-01` (baris itu hilang,
catatan tetapnya muncul).

---

## 611 · Satu draft adendum per lokasi: impor baru menggantikan, laporan ikut pindah (2026-09-24)

**Laporan + ketetapan user 2026-09-24**, memotret satu lokasi dengan DUA draft
dan tombol "Buang" yang menjawab galat mentah Postgres:

> *"kenapa bisa ada dua draft revisi pada satu lokasi, padahal seharusnya hanya
> boleh ada satu, tinggal diaktifkan atau dibuang. lalu ketika dibuang kenapa
> ada notifikasi seperti itu."*

> *"seharusnya jika ada import baru, itu yang digunakan sebagai draft terbaru
> dan 'draft aktif', jadi item yang sudah diinput dicari padanannya pada draft
> paling akhir. jika beresiko rancu, ada list laporan harian mana saja yang
> telah diinput dari draft."*

### Satu akar, dua gejala

`DailyReportItem.rabNodeId` menunjuk node RAB lewat FK **RESTRICT**. Laporan
harian yang diinput terhadap draft adendum (`basis = "draft_adendum"`,
DECISIONS 210) menunjuk node milik DRAFT itu. Maka:

1. **"Buang" ditolak Postgres**, dan kalimat mentahnya lolos ke layar —
   *"update or delete on table "rab_nodes" violates RESTRICT setting of foreign
   key constraint "daily_report_items_rab_node_id_fkey""*. Kelas yang sama
   dengan DECISIONS 606.
2. **Draftnya jadi dua.** Impor draft berjalan create-dulu-baru-buang. Urutan
   itu SENGAJA (audit 2026-09-15 F-2: urutan sebaliknya meninggalkan lokasi
   TANPA draft bila pembuatan gagal). Yang tidak diantisipasi: langkah
   membuangnya sendiri bisa gagal, dan keduanya bukan satu transaksi. Draft
   baru masuk, draft lama tetap tinggal.

### Yang berlaku sekarang

`gantiDraftLama(draftLamaId, draftBaruId)` dalam **satu transaksi**: baris
laporan dipindahkan ke node draft BARU yang `lineageKey`-nya sama, lalu draft
lama dibuang. Tidak ada lagi yang menahan penghapusan, dan kegagalan apa pun
meninggalkan tepat satu draft.

Bila pemindahan ditolak, draft yang baru saja dibuat ikut dibatalkan — impor
yang gagal tidak boleh meninggalkan lokasi dengan dua draft, yaitu keadaan yang
dilaporkan user.

### Yang TIDAK berpadanan: daftarnya, bukan penghapusan diam-diam

Baris laporan tanpa padanan di draft baru ditolak dengan menyebut **tanggal dan
nama item**-nya (maks 8, sisanya dihitung). Menghapusnya berarti membuang
laporan lapangan tanpa ditanya; menggantungkannya mustahil — kolomnya wajib
menunjuk sebuah node. Yang memutuskan harus orang, dan untuk memutuskan ia
perlu daftarnya.

### Kenapa ujinya menembak fungsinya, bukan tombolnya

Lewat tombol impor, `samakanLineage` lebih dulu MENYELARASKAN identitas berkas
baru ke RAB aktif, jadi padanannya nyaris selalu ketemu. Uji yang menembak
lewat sana hijau tanpa pernah menyentuh pagar ini — dan versi pertama uji ini
memang begitu, hijau karena alasan yang salah. Yang dijaga sekarang kontrak
`gantiDraftLama` itu sendiri.

**Berkas**: `src/lib/rab/import.ts`,
`src/app/(app)/lokasi/[slug]/rab/import/actions.ts`.
**Uji**: `tests/integration/draft-adendum-tunggal.test.ts` (merah dulu, 3 uji).

---

## 612 · Folder laporan tidak ditarik balik dari Drive (2026-09-24)

**Ketetapan user 2026-09-24**, memotret pratinjau impor Drive yang menawarkan
belasan *"Laporan Mingguan ke-N Desa Banjar"*:

> *"untuk apa laporan mingguan di menu impor dari drive diunduh ke server,
> apakah ini file yang berbeda atau bagaimana, seharusnya kan kamu abaikan
> saja."*

### Jawaban atas "apakah ini file yang berbeda"

Ya — berkas pada tangkapan layar itu **bukan** keluaran MARLIN. MARLIN menamai
miliknya `Laporan Mingguan Minggu ke-1 - Banjar.pdf`; yang di layar
`Laporan Mingguan ke 1 Desa Banjar Pdf.pdf`, penamaan tim lapangan.

Dan isinya memang **disalin ke R2**, bukan sekadar ditautkan — itu kebijakan
DECISIONS 184: Drive itu milik KKP, dan bila mereka memindah, menghapus, atau
mencabut aksesnya, arsip MARLIN tidak boleh ikut hilang.

### Kenapa tetap dilewati

MARLIN **menerbitkan laporan harian/mingguan/bulanan sendiri**, dan MARLIN pula
yang mengunggahnya ke folder-folder itu (DECISIONS 313). Jadi folder 3/4/5
adalah tempat MARLIN MENULIS, bukan tempat ia membaca. Menariknya balik berarti
mengarsipkan keluaran sendiri seolah berkas pihak lain — dan karena pratinjau
**mencentang semua yang siap secara otomatis**, satu tekan tombol sudah cukup
untuk melakukannya berkali-kali.

Polanya sama persis dengan folder 6 (DOKUMENTASI) yang sudah lama dilewati:
bukan karena berkasnya tidak berharga, melainkan karena sudah ada modul lain
yang memilikinya.

### Yang dilewati, dan yang tidak

Folder **3. LAPORAN HARIAN**, **4. LAPORAN MINGGUAN**, **5. LAPORAN BULANAN** —
tepat tiga jenis yang MARLIN terbitkan sendiri (`GDriveUpload.kind`). Folder
kontrak, PCM, termin, shop drawing, dan as built tetap ditawarkan seperti biasa:
itu berkas yang MARLIN memang tidak punya.

Barisnya tetap TAMPIL di pratinjau dengan alasannya tertulis, tidak dihilangkan
diam-diam — supaya yang membuka layar tahu berkasnya terbaca, bukan mengira
foldernya kosong.

**Berkas**: `src/lib/gdrive/classify.ts`.
**Uji**: `tests/unit/gdrive-classify.test.ts` (merah dulu, 4 uji baru).

## 613 · Adendum satu pintu: CCO lahir saat diberlakukan, nilainya dari RAB (2026-09-24)

**Keluhan user 2026-09-24**: *"menu paket -> kontrak & adendum, ada catat
adendum. apa gunanya itu? lalu hubungan dengan impor rab baru… kalau
berhubungan, kenapa kamu tidak integrasikan? lalu kenapa cabut lokasi, harus
ada pilihan adendum cco? … sungguh sebuah alur yang membingungkan. … jika ada
draft rab adendum bisa langsung import, kenapa cabut lokasi juga tidak ada
draftnya dulu"*.

### Yang salah

Satu peristiwa hukum (CCO) punya tiga pintu yang tidak saling kenal:

1. **Catat adendum** di Kontrak mengetik nilai CCO dengan tangan. Angka itu
   yang masuk "Nilai kontrak berjalan" — tanpa hubungan apa pun dengan RAB.
2. **Draft RAB adendum** diaktifkan per lokasi, tanpa nomor CCO. `amendmentId`
   di revisi tidak pernah diisi siapa pun.
3. **Cabut/tambah lokasi** menuntut CCO yang SUDAH tercatat, padahal draft RAB
   tidak — urutan yang terbalik dari kenyataan: nomor CCO terbit setelah isi
   perubahannya disepakati.

Akibatnya nilai CCO dan selisih RAB bisa berbeda tanpa ada yang tahu
(`batas-adendum.ts` bahkan sudah menghindari `valueDelta` karena tak bisa
dipercaya).

### Ketetapan user (tiga jawaban, 2026-09-24)

- **Nilai CCO**: *"ambil dari RAB tapi tetap bisa diubah ketikan, ini penting
  jika selisih cuma ratusan sampai ribuan rupiah gara-gara selisih koma"*.
- **Lokasi dicabut**: mengurangi *"seluruh nilai RAB-nya"*.
- **Nomor CCO**: *"saat aktivasi keseluruhan, karena bisa saja aktivasi cuma
  1-2 lokasi, atas lokasi yang perubahannya sudah final"*.

### Bentuknya sekarang

- **Draft dulu, tanpa nomor.** Draft revisi RAB (impor/editor) dan usulan
  cabut/tambah lokasi sama-sama hidup sebagai draft tanpa CCO, disetujui empat
  mata masing-masing (DECISIONS 234). Persetujuan lengkap = *siap*, bukan
  *berlaku*. `location_scope_changes.amendment_id`/`effective_date` kini boleh
  kosong selama draft.
- **Satu pintu: Kontrak & Adendum › Berlakukan adendum.** Menggantikan "Catat
  adendum". Draft yang lengkap dicentang (bawaan: semua yang lengkap; yang
  belum lengkap tampil terkunci dengan yang kurang disebut), lalu nomor CCO,
  tanggal berlaku, perubahan waktu, alasan. Tanpa centangan = CCO waktu saja.
  Di sinilah `ContractAmendment` lahir, revisi diaktifkan
  (`activateRevision` + kurva-S dibuat ulang) lalu ditautkan ke CCO-nya, dan
  perubahan lingkup diberlakukan dengan tanggal CCO. Lokasi tambahan yang
  RAB-nya sudah aktif ikut dibuatkan ulang kurva-S-nya (mulai tanggal CCO).
- **Nilai**: `selisihNilaiAdendum` di `finance/calc.ts` — per lokasi
  (sesudah − sebelum) di dalam kontrak; lokasi tambah masuk dengan RAB yang
  akan berlaku, lokasi cabut keluar dengan seluruh RAB aktifnya; PPN dikenakan
  sekali atas total. Disimpan di kolom baru `contract_amendments.value_delta_rab`;
  `value_delta` = angka resmi = turunan bila tidak diketik. Layar menghitungnya
  lewat server (pratinjau), tidak di komponen; selisih ketikan vs RAB disebut
  di layar, di pesan sukses, dan di riwayat ("dari RAB …").
- **Semua diperiksa di depan**: persetujuan, draft milik paket, satu draft per
  lokasi, revisi lokasi yang dicabut tidak boleh ikut, RAB pertama lokasi
  tambahan hanya bersama usulan tambahnya. Menolak di depan satu-satunya cara
  menghindari CCO setengah jadi, karena aktivasi revisi punya transaksinya
  sendiri.
- **Tombol "Aktifkan" per lokasi** untuk draft yang menggantikan RAB aktif
  kini jadi tautan "Berlakukan di Kontrak"; server menolaknya dengan arahan
  yang sama. RAB AWAL (belum ada yang aktif) tetap diaktifkan dari lokasinya.

### Konsekuensi yang disebut apa adanya

- **Memberlakukan adendum kini `amendment.manage`** (Super Admin, Program
  Director, Area Manager, Project Manager) — dulu aktivasi revisi cukup
  `rab.manage`, jadi Site Manager bisa menekannya. SM tetap bisa mengimpor,
  mengedit, dan MENYETUJUI draft; yang pindah hanya penekan tombol terakhir,
  karena tombol itu sekarang juga menerbitkan nomor CCO.
- **CCO lama tidak disentuh**: `value_delta_rab` kosong, angkanya tetap angka
  ketikan. Usulan lingkup lama yang masih draft (ber-CCO lama) diberlakukan
  lewat pintu baru dan mendapat CCO baru.

**Berkas**: `src/lib/package/aktivasi-adendum.ts` (baru),
`src/lib/finance/calc.ts`, `src/lib/package/lingkup-lokasi.ts`,
`src/lib/package/actions.ts`, `src/app/(app)/paket/[id]/kontrak/*`,
`src/app/(app)/paket/[id]/lingkup-panel.tsx`, `src/app/(app)/lokasi/[slug]/rab/*`,
migrasi `20260924100000_adendum_cco_saat_aktivasi`.
**Uji**: `tests/unit/finance-calc.test.ts` (merah dulu, 5 uji),
`tests/integration/adendum-empat-mata.test.ts`,
`tests/integration/lingkup-lokasi-adendum.test.ts`,
`tests/integration/lingkup-lokasi-akses.test.ts`.

## 614 · Dua persetujuan = berlaku; nomor CCO menyusul (2026-09-24)

**Koreksi user atas 613, hari yang sama** (dengan tangkapan layar usulan cabut
Junganyar berlabel "Siap diberlakukan"): *"sama dengan RAB yang jika sudah dua
orang yang approve maka perubahan RAB bisa diaktifkan, seharusnya lokasi juga
begitu. karena kalau tidak apa gunanya jika harus menunggu pengaktifan adendum
total … intinya jika sudah disetujui 2 orang, maka atas lokasi itu sudah
aktif, tinggal administrasi resminya perlu pengaktifan, supaya kelihatan dan
jelas bahwa lokasi itu sudah tidak diikutkan laporan begitu sudah disetujui 2
orang"*.

### Yang salah di 613

613 menjadikan nomor CCO SYARAT berlakunya perubahan. Akibatnya lokasi yang
sudah disepakati dua pejabat keluar tetap ikut laporan dan angka paket sampai
seluruh adendum tuntas — berminggu-minggu. Draft RAB adendum pun tidak bisa
lagi diaktifkan dari lokasinya. Empat mata adalah keputusannya; CCO adalah
dokumen yang meresmikan keputusan itu, bukan keputusan kedua.

### Bentuknya sekarang

- **Lingkup lokasi**: persetujuan kedua yang melengkapi empat mata LANGSUNG
  memberlakukannya — `status aktif`, `effectiveDate` = tanggal Jakarta hari
  itu, tanpa CCO. Lokasi tambahan yang RAB-nya sudah aktif dibuatkan ulang
  kurva-S-nya saat itu juga. Panel menandainya "Nomor CCO menyusul".
- **RAB adendum**: tombol "Aktifkan" di lokasi kembali seperti sebelum 613
  (tetap tergembok empat mata). Revisi yang menggantikan RAB aktif ditandai
  `awaiting_cco` sampai nomornya dicatat.
- **Kontrak & Adendum › Catat CCO** (pintu 613 tetap satu): daftar kini memuat
  perubahan yang *sudah berlaku, nomor CCO menyusul* (tercentang bawaan) di
  samping draft yang lengkap. Mencatat CCO hanya menempelkan nomornya —
  tidak mengaktifkan ulang, dan **tanggal berlaku tidak digeser** ke tanggal
  CCO. Nilai tetap diturunkan dari RAB, kini dari **RAB tercakup CCO
  terakhir** (revisi terakhir yang tidak `awaiting_cco`), supaya rantai
  aktivasi per lokasi sebelum CCO terhitung sekali. Semua revisi menunggu CCO
  di lokasi yang disentuh ikut ditautkan.
- **Data lama**: migrasi `20260925090000` memberlakukan usulan yang sudah
  lengkap empat matanya di bawah aturan 613 (seperti Junganyar) sejak
  persetujuan terakhirnya, dan mencatat `package_id` setiap usulan — dulu
  paketnya tersirat dari CCO, kini baris boleh berlaku tanpa CCO.

### Bug yang ikut ketemu

`lingkupLokasi` membandingkan `effectiveDate` (kolom tanggal = tengah malam
UTC) dengan jam sekarang. Antara 00:00–07:00 WIB tanggal hari ini masih
"masa depan", jadi lokasi yang dicabut hari itu baru keluar dari angka pukul
07:00. Kini dibandingkan per tanggal Jakarta. Uji merah-dulu:
`lingkup-lokasi-adendum` ("01:00 WIB sudah tanggal berlakunya").

**Berkas**: `src/lib/package/lingkup-lokasi.ts`, `src/lib/package/aktivasi-adendum.ts`,
`src/lib/package/actions.ts`, `src/app/(app)/lokasi/[slug]/rab/*` (dikembalikan ke
sebelum 613 + penanda `awaiting_cco`), `src/app/(app)/paket/[id]/kontrak/*`,
`src/app/(app)/paket/[id]/lingkup-panel.tsx`.
**Uji**: `tests/integration/lingkup-lokasi-adendum.test.ts`,
`tests/integration/adendum-empat-mata.test.ts`, E2E `adendum-empat-mata` kembali ke
tombol Aktifkan.
